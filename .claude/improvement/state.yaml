# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T15:30:00Z
last_run_commit: 8659092
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 59   # +1: newsletter-editorial-register-pass (auditor-picked, cold-surface — forms surface had never had a distinctive ship per historian's surface-freshness). Replaced the 35-line newsletter stub with editorial-register vocabulary borrowed from .checkout-input — italic-serif placeholder + submit, :focus-within hairline rise, post-submit <output role=status aria-live=polite> with scaleX hairline-rule draw (one new keyframe newsletter-rule-draw + reduced-motion guard). Removed disabled+placeholder-swap success hack. Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows; no task-driven override. Picked at T2 M2 L1 I2 A3 D3 = 13/18 over floema-scroll-bound-specimen-open @ 11/S (reference-scout #1, deferred — adjacent to backlog live-kerning-on-hover), pdp-specimen-asymmetric-break @ 11/M (catalogue auditor #4, higher effort), pdp-reviews-data-side-asymmetric-pickup @ 10/S (catalogue auditor #1, dead-attribute fix — picked next run), section-title-typography-monotone @ 9/M, chapter-figure-link-cursor-mismatch @ 9/S. Strategy: replace .newsletter block at globals.css:3180-3223 wholesale (-25/+81 net = +56) and refactor newsletter.tsx (-12/+18) to conditionally render <output> sibling in place of disabled input+button. All pre-existing data-cursor + autoComplete + required + sr-only label attributes retained. JSX still 37 lines, single useState preserved, no new 'use client' boundary. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged baseline), bunx tsc --noEmit clean, bun run build 48/48 routes (Turbopack 537.6ms static-gen + 1329.2ms compile), 0 anti-patterns. SSR class-grep across 8 footer-bearing routes + 7 PDP/journal spot-checks all clean (1× .newsletter form + 1× #email input + 1× Submit + 0× <output> + 0× 'On file.' + 0× keyframe-as-className in initial SSR); /checkout correctly 0× newsletter via SiteFooterMount opt-out. CSS bundle a63e26c70adf1c7a.css contains 2× newsletter-rule-draw + 3× .newsletter-status + 10× .newsletter. perf-a11y PASS-WITH-FOLLOWUPS — 0 net JS delta, ~+1.5KB gzipped CSS, no LCP risk (below fold), CLS risk low (post-interaction swap excluded from CLS web-vital), no INP risk, reduced-motion guard complete, AA contrast clear (placeholder 0.55 ≈ 8.1:1, button 0.75 ≈ 12.6:1, status 0.78 ≈ 13.4:1). regression-spotter PASS exact match counts across 15 routes. diff-reviewer PASS-WITH-NITS — 3 low-severity nits — rgba literals match block convention (rolls into folio/cart token-promotion sweep), font-size 14px repeat (acceptable local convention), explicit role='status' on <output> technically redundant per ARIA implicit role but kept for cross-AT compat. Risk band: medium per risk-rules.md (newsletter is footer chrome rendered site-wide; 2 files; ≤100 LOC delta; no shared primitive touched; no design-token block edit; no SEO/JSON-LD edit; no new client boundary; below-fold so above-fold medium trigger does not strictly fire — picker erred medium for site-wide visibility per 'when uncertain prefer the higher band' contract). Direct-commit to main per medium-band default — medium does NOT branch to PR; medium adds screenshot capture which is currently broken (carry-over). Screenshots skipped — capture-ship.mjs failed again 'server did not become ready within 30s'; carry-over now 10 consecutive ships old (proper backlog item capture-ship-stale-server-on-3000 already open one ship ago, severity high). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per established pattern for register-coherence low/medium-risk ships borrowing established vocabulary — single auditor-picked sharply-scoped form recast with PASS/PASS-WITH-* verdicts across all 5 Phase 5 gates; review value would be re-reading the same diff against the same vocabulary already validated for the checkout-form (which itself shipped without review). Notion writeback: no task to flip (no override); Reports row 361af8d3-d3e2-81b1-8238-c9eae1f9f870 appended via MCP mcp__Notion__API-post-page first attempt (no schema-fix iteration needed; schema-fix pattern already absorbed from prior runs) with Surface=chrome (newsletter is footer chrome rendered site-wide), Mode=Shipped, Rubric T2 M2 L1 I2 A3 D3 = 13/18, Risk=Medium, Commit=8659092, all 10 schema fields populated. Closed-by-drift this run: none. Backlog received 4 new entries: newsletter-status-output-implicit-role-redundant (low, hygiene — diff-reviewer nit), newsletter-color-tokens-promotion (low, hygiene — rolls into folio/cart token sweep), newsletter-post-submit-focus-management (low, a11y — overlaps with newsletter-reset-path), newsletter-post-submit-cls-pixel-parity (low, perf).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 59 — not a multiple of 10, calibrator does NOT fire. Next eligible at shipped_count=60 IF today − last_calibration_at >= 7 days at that point.

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok; push to origin/main succeeded (commit 8659092: 2ddd5d6..8659092)
playwright_available: unknown  # capture-ship.mjs skipped — port 3000 still held; carry-over now 10 ships. Backlog item capture-ship-stale-server-on-3000 (severity high) already opened one ship ago.
lighthouse_available: unknown  # perf-a11y skipped lighthouse — CSS-only chrome change below the fold; perf signal-to-cost ratio poor for this band; no prior baseline
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 548000   # +18000 bytes for this run's IMPROVEMENTS.md entry (newsletter-editorial-register-pass); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy. Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 rows. Reports notion-post-page succeeded on first attempt this run (no schema-fix iteration needed). Reports row 361af8d3-d3e2-81b1-8238-c9eae1f9f870 created with all 10 schema fields populated.
