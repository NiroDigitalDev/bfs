# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T14:26:00Z
last_run_commit: a055974
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 58   # +1: legal-folio-on-legal-routes (auditor + reference-scout convergence — extends the editorial running-folio masthead onto /privacy /terms /cookies via a new server component LegalFolio mounted in legal-page-frame.tsx, lowercase Roman i/ii/iii to typeset the legal routes as appendices to Vol. III rather than chapters of it; no shared primitive touched, register-symmetric mirror of journal-folio shipped 1 run earlier). Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows; no task-driven override. Picked at T2 M2 L2 I2 A2 D3 = 13/18 over journal-prose-figure-pull-quote-marginalia-vocabulary @ 15/M (bundles 3 distinct primitives — would trip task-split heuristic, deferred), codex-row-keyboard-parity @ 11/S (I=3 strong but lower total), footer-nav-editorial-register-pass @ 12/M (chrome hot — chrome is most-touched surface in shipped.yaml), 404-wordmark-stroke-and-ribbon-wit @ 12/S, FAQ-dot-leader-and-first-item-open @ 11/S. Cold-surface tiebreaker also favoured legal (zero direct ships ever per historian's surface-freshness — legal routes have never been the focus of a ship). Strategy was a direct mirror of journal-folio: created new server component <LegalFolio> rendered inside <main> in LegalPageFrame; running-folio.tsx + site-chrome.tsx + journal-folio.tsx all remain untouched. Single variant with internal ordinal map (privacy→i, terms→ii, cookies→iii; total=iii) — left edge '§ · Statutory · <Slug>' with italic-serif slug, right edge 'p. <ord> / iii · MMXXVI'. Lowercase Roman is the deliberate distinction from journal's uppercase Roman (I-XI) — book typography uses lowercase for front-matter / appendix / statutory pages, and the three legal routes are appendices to Vol. III rather than chapters of it. Reuses every .folio-* class + both keyframes (folio-rise 700ms, folio-typeset 540ms) + reduced-motion guard at globals.css:4781-4787 — zero new motion vocabulary, zero new keyframes. CSS delta: +21 lines net at globals.css:~4812 (.folio[data-variant='legal'] .folio-legal-slug + .folio-sep-fraction italic-serif 14px 0.6 opacity matching journal-variant register). Three files: NEW src/components/legal-folio.tsx (42 LOC), src/components/legal-page-frame.tsx (+3 lines — import + mount), src/app/globals.css (+21 lines). Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 routes (Turbopack 574.7ms static-gen + 1115.0ms compile), 0 anti-patterns. SSR class-grep: /privacy renders 1× <aside class='folio' data-variant='legal'> with text fragments §, Statutory, Privacy, p., i, /, iii, MMXXVI; /terms renders 1× legal-variant with Terms, ii, iii; /cookies renders 1× with Cookies, iii, iii; /journal continues to render 1× [data-variant='journal'] folio (no regression); sample /journal/letterforms-in-low-light continues to render 1× journal-variant post folio; homepage / unchanged — 1× <aside class='folio'> with NO data-variant attribute (existing chapter-driven RunningFolio intact); /about renders 0× [data-variant='legal']; representative PDP /supplies/abyssal-cardstock renders 0× [data-variant='legal'] and 0× .folio inside main.pdp. perf-a11y PASS — 0 bytes JS delta (server component), ~+0.5KB gzipped CSS, no LCP/CLS/INP risk, keyboard parity intact (aria-hidden + pointer-events:none inherited), reduced-motion override complete via existing guard (no new keyframes), contrast — italic-serif main text rgba(255,255,255,0.55-0.78) under mix-blend-difference on matte-black ≈ AAA-clear, new italic-serif '/' separator at rgba(255,255,255,0.7) × opacity 0.6 ≈ 4.3:1 under difference blend (aria-hidden decorative — same trade-off as journal variant). regression-spotter PASS — exact counts on 14 routes (3 legal + 1 journal index + 1 post sample + 1 homepage + 1 about + 1 PDP); 0 stray [data-variant='legal'] outside legal routes. diff-reviewer PASS-WITH-NITS — +66 / -0 LOC under 80 budget, 0 any/@ts-ignore/console.*/!important, 0 dead selectors, 0 scope creep; 2 low-severity nits — (a) font-size: 14px + rgba(255,255,255,0.7) literals match established .folio block convention (rolls into existing folio-color-token-promotion sweep with journal-variant counterpart), (b) Slug = 'privacy' | 'terms' | 'cookies' redeclared locally (no canonical union exists in @/lib/site). Risk band: low per risk-rules.md — no shared primitive touched, 3 files, ≤80 LOC, no new client boundary, no SEO/JSON-LD edit, no design-token block edit. Direct-commit to main per low-band default — direct mirror of journal-folio's just-shipped low-risk profile. Screenshots skipped — capture-ship.mjs failed again with 'server did not become ready within 30s'; carry-over now 9 ships old. Promoted from shipped.yaml notes to proper backlog item this run as capture-ship-stale-server-on-3000 (severity high — historic documentation gap closed). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per established pattern for register-symmetric low-risk ships — focused new-component ship with PASS verdicts across all 5 Phase 5 gates; direct mirror of journal-folio (which itself shipped without review); review value would be re-reading the same diff against the same primitives. Notion writeback: no task to flip (no override); Reports row 361af8d3-d3e2-8121-ac72-c9371c55e881 appended via MCP API-post-page first attempt with Surface=chrome (single-select; folio is bottom-edge masthead chrome), Mode=Shipped, Rubric T2 M2 L2 I2 A2 D3 = 13/18, Risk=Low, Commit=a055974, all 10 schema fields populated cleanly (schema-fix pattern already absorbed from prior runs). Closed-by-drift this run: journal-folio-legal-route-variants flipped open → shipped (this ship is the canonical implementation). Housekeeping: backlog received 3 new entries (legal-folio-slug-type-canonicalisation low/hygiene, legal-folio-fraction-sep-hardcoded-colors low/hygiene, capture-ship-stale-server-on-3000 high/infra — promoted from carry-over).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 58 — not a multiple of 10, calibrator does NOT fire. Next eligible at shipped_count=60 IF today − last_calibration_at >= 7 days at that point.

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok; push to origin/main succeeded (commit a055974: c616513..a055974)
playwright_available: unknown  # capture-ship.mjs skipped — port 3000 still held; carry-over 9 ships now. Promoted to proper backlog item this run (capture-ship-stale-server-on-3000, severity high).
lighthouse_available: unknown  # perf-a11y skipped lighthouse — CSS-only + below-the-fold + no prior baseline; signal-to-cost poor for this band
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 530000   # +18000 bytes for this run's IMPROVEMENTS.md entry (legal-folio-on-legal-routes); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy. Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 rows. Reports notion-post-page succeeded on first attempt this run (no schema-fix iteration needed). Reports row 361af8d3-d3e2-8121-ac72-c9371c55e881 created with all 10 schema fields populated.
