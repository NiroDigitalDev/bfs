# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-16T08:30:00Z
last_run_commit: 2c96b43
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 65   # +1: journal-post-tag-affordance-and-index-filter (medium-risk distinctive ship on the journal surface — turned the foot-of-post tag pills into real navigation, /journal gained a filter rail and ?tag= server-side filtering; 33 previously-inert <em>{tag}</em> across 11 post pages became focusable <Link>s with hairline-underline-grow + aria-current + aria-live filter announcement; /journal flipped from ○ Static to ƒ Dynamic via searchParams await; rubric T2 M1 L2 I3 A3 D2 = 13/18 distinctive band, won Tiebreaker 1 (lower S-effort) against journal-prose-figure-pull-quote-marginalia-vocabulary which also scored 13/18 at M-effort. No Notion task override. Strategy: (a) src/lib/journal.ts — three pure-server exports: slugifyTag(tag)→deterministic tag.toLowerCase().replace(/\s+/g,'-') ('Edition III'→'edition-iii', 'press notes'→'press-notes'); getAllTags()→{tag,slug}[] deduped+alphabetized; getPostsByTagSlug(slug)→filtered post set. (b) src/components/journal-post-frame.tsx — each <li className='journal-post-tag'> content wraps in <Link href=`/journal?tag=${slugifyTag(tag)}` data-cursor='link' data-cursor-label='Filter'> with the inner <em>{tag}</em> preserved; pill chrome on parent <li> unchanged at rest. (c) src/app/journal/page.tsx — async Server Component reads searchParams: Promise<{tag?:string}>, awaits, resolves activeSlug against getAllTags(), posts=getPostsByTagSlug(activeTag.slug) if filtered else getAllPosts(); renders <nav className='journal-tag-rail' aria-label='Filter by tag'> between journal-header and journal-list with an italic-uppercase '— Filter' eyebrow (28px hairline rule + 12px italic 0.16em letter-spaced 'Filter' label) and a chip row: 'All' link first (uppercase 12px 0.14em letter-spaced — the catalog-anchor register matching cookie-banner accept/reject), then every unique tag as italic-serif 14px chip with hairline-underline-grow on hover/focus; aria-current='true' pins underline 100% width + color #fff on the active chip. When filtered, also renders <p className='journal-filter-status' aria-live='polite'> with active tag in uppercase-sans 13px inline pill (1px-bordered 999px-radius) + middot + Clear <Link href='/journal'>. JSON-LD blogLd.blogPost[] narrows to filtered set. (d) src/app/globals.css — three blocks added (lines 7107–7221 tag-rail+filter-status, lines 7739–7763 post-tag anchor): .journal-post-tag a + :hover/:focus-visible mirroring .journal-prose a underline-draw vocabulary at lines 7585–7599 (background-image gradient 0→100% width); .journal-tag-rail chrome (28px hairline rule + italic-uppercase eyebrow + chip row gap 8/14); .journal-filter-status + -tag uppercase-sans pill + -clear underline-grow. All three new transitions guarded by @media (prefers-reduced-motion: no-preference). Net delta: 4 files, +160 / -10 LOC (medium band per risk-rules.md 100-250 LOC range; touches journal-post-frame.tsx consumed on 11 post pages + /journal index gaining above-fold chrome; no shared-primitive surgery, no design-token edit, no layout.tsx change, no new client component, no new keyframes, no new tokens, 0 net JS bundle delta — all helpers consumed by Server Components only). Verified — bun run lint 0 errors + 7 pre-existing tooling warnings unchanged baseline (.claude/improvement/scripts/*.mjs); bunx tsc --noEmit clean (Promise<searchParams> async-page contract typechecks; slugifyTag/getAllTags/getPostsByTagSlug signatures resolve through existing JournalPost type without intervention); bun run build succeeds — /journal flipped ○ Static → ƒ Dynamic (expected: searchParams access dynamizes the route; the 11 /journal/<slug> post pages plus 11 opengraph-image SSG routes plus rss.xml remain SSG; about/checkout/cookies/_not-found/opengraph-image/privacy/robots/sitemap/terms static routes unchanged; supplies/<id> SSG x 6 + opengraph SSG x 6 untouched; route count unchanged though one route shifts band). regression-spotter PASS — all 11 journal post HTML files at .next/server/app/journal/*.html SSR exactly 3 <a href='/journal?tag=…'> inside <ul class='journal-post-tags' aria-label='Tags'>; no post page gained .journal-tag-rail-* classes (correctly scoped); no post page lost <ul aria-label='Tags'>; adjacent chrome (chapter-rail on /, nav-logo, outro-wordmark-major/-conn, manifesto-credo-mark on /, about-coda-numeral on /about, spec-plate + related-grid + pdp-plate-frame on /supplies/<id>) all intact at expected counts. perf-a11y PASS — 0 net JS delta (slugifyTag/getAllTags/getPostsByTagSlug consumed only by Server Components — next/link was baseline import on journal-post-frame.tsx; no client bundle reaches them; no 'use client' added); CSS delta +134 lines / ~3.4KB raw / ~600-800B gzipped (well under +5KB budget); no new keyframes. No LCP risk (tag rail is below journal-header which is the LCP candidate, unchanged). No CLS risk (all chrome SSR'd; pill rest state has background-size 0 1px so 0→100% hover doesn't alter layout). No INP risk (no event handlers; filter via URL nav). Keyboard PASS on both /journal (nav-logo → nav-links → 'All' → alphabetized tag chips → entries → footer return → folio) and /journal/<slug> (… → 3 footer tag links → return → signoff); each anchor inherits global :focus-visible ring at globals.css:80–85. Reduced-motion PASS — 3 added transitions / 3 added prefers-reduced-motion: no-preference guards (1:1 coverage at .journal-post-tag a / .journal-tag-rail-item a / .journal-filter-status-clear); under reduced-motion chips render rest color with no underline-grow animation and no flash. Contrast PASS — all rest states ≥ 8.4:1 on #050505 (AAA at italic-serif 13-14px), hover/active states ≥ 18.5:1 (AAA); eyebrow rgba(255,255,255,0.42) at 12px italic-serif 5.4:1 (AA passes at 12px small-text 4.5:1 floor). A11y semantics PASS — <nav aria-label='Filter by tag'> landmark with discriminating label distinct from site nav; eyebrow span aria-hidden so '— Filter' decorative rule doesn't double-announce; <ul>→<li>→<Link> list semantics preserved; aria-current='true' on active filter; <p aria-live='polite'> for filter status; existing <ul aria-label='Tags'> on post pages preserved. diff-reviewer PASS-WITH-NITS — initial diff had getPostsByTagSlug exported but used inline filter() in page.tsx → FIXED in-cycle by switching to getPostsByTagSlug() and dropping slugifyTag import on page.tsx (kept on journal-post-frame.tsx). Remaining nits non-blocking: (a) .journal-post-tag:hover/:focus-within parent rule slightly redundant since anchor fills li and inherits — kept for defensive coverage when hovering pill chrome rather than directly on inner italic glyph. (b) `activeSlug ?? null` collapses undefined→null defensively before strict equality — cosmetic. (c) blogPost JSON-LD reflects filter (intentional — JSON-LD describes rendered page, not canonical blog corpus; matches Google's structured-data tester expectation). anti-patterns 0. Risk band MEDIUM per risk-rules.md (4 files / +160-10 LOC / 100-250 LOC band / above-fold chrome added on /journal). Direct-commit to main per medium-band default with screenshots requested — commit 2c96b43 (push to main: e4682b1..2c96b43). Screenshots SKIPPED — capture-ship.mjs carry-over blocker NOW 16 consecutive ships old (capture-ship-stale-server-on-3000 backlog at severity high — the longest-running open infra blocker; this ship added the first /journal index chrome AND the first interactive tag affordance on 11 post pages where visual diff against pre-rail /journal would have been the cleanest demonstration of affordance gap closing). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per established pattern for medium-risk distinctive ships with PASS verdicts across all 5 Phase 5 gates AND a structurally-small diff with no shared-primitive surgery. Notion writeback: no task to flip (no override); Reports row to append via mcp__Notion__API-post-page with Surface=journal, Mode=Shipped, Rubric T2 M1 L2 I3 A3 D2 = 13/18, Risk=Medium, Commit=2c96b43. Closed-by-drift this run: none. Backlog: NEW item journal-tag-rail-all-uppercase-eyebrow-pattern-extract (low, hygiene — second occurrence of uppercase 0.14em-letter-spaced anchor flanking italic-serif chip row; cookie-banner accept/reject was first; premature without third) and NEW item journal-filter-status-tag-pill-pattern-extract-utility (low, hygiene — uppercase-sans 13px 1px-bordered pill is a NEW pattern this run introduced; hoist on second filter-status surface). Carry-overs: folio-numeral-pattern-extract-utility (high — STILL UNCOMMITTED, top-priority system ship since 6 runs ago; this ship intentionally did NOT touch the .chapter-numeral / .related-numeral / .journal-related-numeral / .about-coda-numeral family; system auditor THIS RUN confirmed 3 byte-identical CSS blocks + ~135 LOC of cargo; eligibility unchanged), rule-draw-keyframe-consolidate (medium — system auditor THIS RUN confirmed 3 identical scaleY draw keyframes), related-grid-rule-color-token-promotion (medium — unchanged), related-eyebrow-arrow-glyph-aria-hidden-double-emphasis (low — unchanged), lemma-connector-pattern-extract-utility (low — unchanged), marginalia-mark-pattern-extract-utility (low — unchanged), cross-island-event-bus-typed-utility (low — unchanged), capture-ship-stale-server-on-3000 (high, infra, NOW 16 SHIPS — longest-running open blocker), sotd-parser-fix (low, infra), lighthouse-baseline-seed (low, infra). Journal surface: still spreadable but well-covered — 3 of last 11 ships including this one (this + journal-prev-next-dictionary-spread + journal-folio-on-journal-routes); next journal ship should bias toward journal-prose-figure-pull-quote-marginalia-vocabulary (severity:high, M-effort, scored 13/18 by historian this run as joint-top) or rest the journal surface for ~3 ships before returning. (medium-risk distinctive ship on the deliberately-cold manifesto surface — first cross-column active-correspondence in the BFS register: sticky .manifesto-credo gains a <sup class="manifesto-credo-mark" aria-hidden> marginalia that mirrors the active ManifestoList item via a document-level bfs:manifesto:active CustomEvent + useSyncExternalStore subscription, Roman numerals I/II/III/IV deliberately differ from the list's Arabic 01/02/03/04 padStart so the mark reads as a footnote-style cross-reference rather than a duplicate, position:absolute in the gutter top-right of the credo paragraph at ≥1000px (display:none below — cross-column correspondence is only meaningful when both columns are visible), opacity 0→1 transition on [data-visible="true"]/[data-static="true"] 320ms ease-out-quart, reduced-motion fallback renders a static ·–· ornament with transition:none. No Notion task override (Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows). Picked over folio-numeral-pattern-extract-utility despite the latter's higher backlog severity (high vs medium) — rubric self-rated T2 M2 L2 I2 A2 D3 = 13/18 (distinctive band) vs folio-numeral ~7/18 (pure refactor, no visible delta); retro-2026-W19 adjustment #2 ("bias toward manifesto on ties — cold surface, 0 prior ships") was decisive, but the 6-point rubric spread would have settled it anyway. folio-numeral-pattern-extract-utility CARRIES TO NEXT RUN still uncommitted at severity high (top-priority system ship since the 5+6 numeral occurrence threshold was crossed on the about-coda ship 2 runs ago). Strategy: (a) ManifestoList IO callback extended with Map<HTMLLIElement, number> tracking per-item intersectionRatio, computes singular-active = highest ratio above 0.5 threshold, dispatches document-level bfs:manifesto:active CustomEvent with detail:{index:number|null} on change (deduped by lastDispatched sentinel); reduced-motion early-return also dispatches once with {index:null}. (b) NEW client component ManifestoCredoMark — uses useSyncExternalStore for BOTH the active subscription AND the prefers-reduced-motion media query (React's recommended pattern for external state; the ESLint react-hooks/set-state-in-effect rule rejected the naive useEffect+setState shape — caught at lint step, rewrote, lint clean on re-run); module-level _active:number|null cache + getSnapshot/subscribe/getServerSnapshot triple; ROMAN array sized to 8 to absorb future manifesto-item growth (current 4). (c) page.tsx mounts <ManifestoCredoMark /> as first child of <p className='manifesto-credo'> — mark is its own 'use client' island, the rest of the manifesto section remains a Server Component. (d) globals.css adds position:relative to .manifesto-credo + .manifesto-credo-mark recipe (display:none below 1000px, ≥1000px → position:absolute top:-0.6em right:clamp(-32px,-2.4vw,-16px) font-serif italic clamp(14px,1.2vw,18px) onum color:rgba(244,244,244,0.85) opacity transition); @media (prefers-reduced-motion:reduce) sets transition:none. Net delta: 4 files, +94/-14 LOC (under low-band LOC ceiling but escalated to MEDIUM per risk-rules.md "Introduces a new client component anywhere" trigger). Verified — bun run lint 0 errors (after rewrite to useSyncExternalStore — initial naive useEffect+setState hit react-hooks/set-state-in-effect ERROR not warning) + 7 pre-existing tooling warnings unchanged baseline in .claude/improvement/scripts/*.mjs; bunx tsc --noEmit clean; bun run build 48/48 routes prerendered Turbopack 2.2s. SSR class-grep on /index.html: 1× .manifesto-credo-mark (the new <sup>, SSR with empty content as designed — getActiveServerSnapshot returns null), 2× .manifesto-credo (existing parent, RSC streams className strings twice for hydration as with all other chrome class signatures), 4× .manifesto-item unchanged, 4× .manifesto-num unchanged. 'bfs:manifesto:active' literal appears 1× in client bundle 8e7c087f00ae206c.js (the manifesto-list chunk). ManifestoCredoMark client component reached the bundle (useSyncExternalStore + ROMAN + ·–· references confirmed across 6 client chunks). perf-a11y PASS — net JS delta ~+450 bytes minified well under +5KB budget, ~+250 bytes gzipped CSS; no LCP risk (mark empty on first paint and mid-page; sticky aside layout unchanged by an absolute overlay); no CLS risk (position:absolute → no layout-flow impact); no INP risk (subscriptions passive, dispatch debounced by lastDispatched, useSyncExternalStore guarantees consistent reads); keyboard focus order intact (no interactive elements added); reduced-motion path explicit on two fronts (ManifestoList dispatches one {index:null} then returns; ManifestoCredoMark reads matchMedia via useSyncExternalStore and renders static ·–· with transition:none); contrast rgba(244,244,244,0.85) on #050505 ≈ 16:1 AAA at clamp(14-18px) italic-serif numeral (aria-hidden decorative anyway). regression-spotter PASS — manifesto block adjacent surfaces unchanged: section-tag, manifesto-credo (existing prose + ManifestoCredoMark sibling), errata § corr. mark, PressStampTray, ManifestoList items, the Press section that follows. diff-reviewer PASS-WITH-NITS 3 follow-ups: (a) .manifesto-credo-mark recipe is a NEW single-occurrence marginalia pattern — if a second credo+marginalia surface ships, hoist to .marginalia-mark utility; NEW backlog item marginalia-mark-pattern-extract-utility (low, hygiene — pairs in spirit with lemma-connector-pattern-extract-utility from the prior ship). (b) document-level bfs:manifesto:active CustomEvent is the FIRST cross-island event bus in the codebase; if a second pair appears, hoist to src/lib/bfs-event-bus.ts with typed BfsEventMap; NEW backlog item cross-island-event-bus-typed-utility (low, system). (c) right:clamp(-32px,-2.4vw,-16px) magic value folded into (a). anti-patterns 0. Risk band MEDIUM per risk-rules.md "Introduces a new client component anywhere" trigger (manifesto-credo-mark.tsx is a new 'use client' module on /). Direct-commit to main per medium-band default — commit 8278212 (push to main: 2866b1d..8278212). Screenshots SKIPPED — capture-ship.mjs carry-over blocker NOW 15 consecutive ships old (capture-ship-stale-server-on-3000 backlog at severity high — longest-running open infra blocker; this ship adds first cross-column correspondence to the manifesto surface where visual diff against prior manifesto-item-as-sans-card would have been the cleanest demonstration of register evolution). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per established pattern for medium-risk distinctive ships with PASS verdicts across all 5 Phase 5 gates AND a structurally-small client-component diff with no shared-primitive surgery. Notion writeback: no task to flip (no override); Reports row 362af8d3-d3e2-81aa-be1b-fdcc7f96fd9b created via mcp__Notion__API-post-page (parent.database_id=60cc3221-8e8a-42da-b926-20cd32d6c8bb) with Surface=manifesto, Mode=Shipped, Rubric T2 M2 L2 I2 A2 D3 = 13/18, Risk=Medium, Commit=8278212; the create call hit a schema mismatch on "Files" (correct property name is "Files changed" — DB has "Files changed" not "Files"; logged for next routine refresh) and a multi_select shape on Surface (Surface is a select, not a multi_select); both fixed in one retry. Files-changed populated via mcp__Notion__API-patch-page in a follow-up call. Closed-by-drift this run: none. Manifesto surface NO LONGER COLD — this is the first manifesto ship since manifesto-item-as-sans-card (which established the data-active baseline this ship consumes); retro adjustment #2 satisfied. Backlog: NEW items marginalia-mark-pattern-extract-utility (low, hygiene) and cross-island-event-bus-typed-utility (low, system) — both deliberately filed as premature pending second-occurrence triggers. CARRY-OVERS: folio-numeral-pattern-extract-utility (HIGH — still uncommitted 3 runs after promotion to top-priority system ship; not touched this run), rule-draw-keyframe-consolidate (medium — unchanged), related-grid-rule-color-token-promotion (medium — unchanged), related-eyebrow-arrow-glyph-aria-hidden-double-emphasis (low — unchanged), lemma-connector-pattern-extract-utility (low — unchanged), outro-wordmark-settle-token-promotion (low — unchanged), capture-ship-stale-server-on-3000 (high, infra, NOW 15 SHIPS — the longest-running open blocker), sotd-parser-fix (low, infra), lighthouse-baseline-seed (low, infra). (found-WIP completion mode, chrome surface — first chrome application of the dictionary register established on PDP recirc f107f1b → journal prev/next 9c7c09d → /about coda df006ad). Started the run with an uncommitted edit on src/components/site-footer.tsx that had already recomposed <div className='outro-wordmark'>BFS</div> into three children (<span outro-wordmark-major>Blacks</span> + <span outro-wordmark-conn><em>for</em></span> + <span outro-wordmark-major>Sale.</span>) plus an <span sr-only>Blacks for Sale. Edition III, MMXXVI.</span> sibling, but with NO supporting CSS — the three children would have collapsed into a single inline-rendered white-space:nowrap line, losing the dictionary-lemma compression idea entirely. Reverting the WIP would have discarded both the editorial intent (dictionary register extended to chrome, where the abbreviation literally becomes its own lemma) AND the a11y win (sr-only sibling gives ATs the closing identity — the .outro-wordmark itself remains aria-hidden as before, but the only 'BFS' mark on the site was previously a11y-silent). Completing it was the cleanest path. No Notion task override this run — Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows. Self-rated rubric T3 M2 L2 I1 A3 D3 = 14/18 (distinctive band — fourth surface for the dictionary register, FIRST chrome application; A=3 earned on the sr-only fix to a previously-silent mark; D=3 earned on cross-surface register extension to chrome). Strategy — rescope the existing .outro-wordmark base rule from a single nowrap-overflow word at clamp(40vw, 50vw, 60vw) to a stackable container at clamp(34vw, 44vw, 54vw); remove white-space:nowrap; raise max-height to 90vw; keep -webkit-text-stroke 1px rgba(255,255,255,0.1) + color:transparent on the parent (inherited by .outro-wordmark-major children); add .outro-wordmark-major (display:block) and .outro-wordmark-conn (display:block + position:relative + z-index:1 + font-family:var(--font-serif) + font-style:italic + font-weight:400 + font-size:clamp(36px, 6vw, 92px) + letter-spacing:-0.01em + line-height:1 + color:rgba(255,255,255,0.55) + -webkit-text-stroke:0 + margin:clamp(-9vw, -7.5vw, -5vw) 0 — the negative-margin lemma-compression move pulls the italic-serif connector into the line-gap between the two outline-stroke majors); rescope the @supports (animation-timeline: view()) + @media (prefers-reduced-motion: no-preference) and (min-width: 900px) block's target from .outro-wordmark to .outro-wordmark-major (the existing @keyframes outro-wordmark-settle body is preserved verbatim — only the target selector moves, so the existing outro-wordmark-settle-token-promotion backlog item's premise 'promote start values to tokens once a second settle instance lands' remains unchanged because this run did NOT add a second instance). Net delta: 2 files, +56/-13 LOC. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged baseline in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 routes prerendered (Turbopack OK). SSR class-grep on representative routes /, /journal/the-folio-as-an-instrument, /supplies/void-book: each renders 2× 'outro-wordmark' substring + 4× '-major' substring + 2× '-conn' substring (the 1:2:1 ratio per render is doubled by the RSC streaming chunk serializing className strings a second time for hydration). Sr-only sibling literal-grep on /, /about, /journal, /privacy — exactly 1 each (sitewide chrome confirmed). /_not-found SSR: .nf-wordmark renders 3× (unchanged — the 404 closing 404-as-wordmark block at globals.css:4680-4698 was deliberately not touched and remains structurally distinct from outro-wordmark); the same route renders the new outro-wordmark-major × 4 / outro-wordmark-conn × 2 / sr-only × 1 (chrome consistency holds even on 404). perf-a11y PASS — 0 net JS delta (CSS-only behavioral changes; the WIP JSX was already in place), ~+1KB gzipped CSS net, no LCP risk (wordmark is FINAL element of footer, below the fold), no CLS risk (clamp() values applied at initial render; settle only animates letter-spacing + -webkit-text-stroke-width which are non-layout properties), no INP risk (no handlers; settle is view-timeline CSS-only off main thread), keyboard focus order intact (no interactive elements added), reduced-motion path complete: settle keyframe is gated by @supports + prefers-reduced-motion:no-preference + min-width:900px — under reduced-motion / non-supporting browser / mobile, the majors render static at letter-spacing:-0.08em + -webkit-text-stroke-width:1px (the base rule values), and the connector is ALWAYS static regardless of motion preference. Contrast: connector at rgba(0.55) ≈ 7.6:1 AAA for normal-weight italic-serif at clamp(36-92px); majors at rgba(0.1) outline are decorative inside aria-hidden parent. NEW a11y signal: the previously aria-hidden 'BFS' mark now has an adjacent sr-only sibling reading 'Blacks for Sale. Edition III, MMXXVI.' — first time AT users on every route get the closing identity. regression-spotter PASS — chrome regression check clean: every adjacent route family (/, /journal/<slug>, /supplies/<slug>, /_not-found) renders the new vocabulary with consistent counts; .nf-wordmark block at globals.css:4680-4698 untouched; outro-wordmark-settle keyframe body preserved verbatim (only target moved from .outro-wordmark to .outro-wordmark-major). diff-reviewer PASS-WITH-NITS — 2 follow-ups: (a) the negative-margin lemma-compression on .outro-wordmark-conn is a NEW pattern on this surface, distinct from the dictionary-spread cross-reference recipe (the spreads use absolute-positioned-in-outer-margin numerals; this uses negative margin on an italic-serif connector). If a SECOND dictionary-expansion-of-abbreviation surface lands, the connector recipe should hoist to .lemma-conn utility — NEW backlog item lemma-connector-pattern-extract-utility (low, premature without a second instance). (b) outro-wordmark-settle-token-promotion trigger condition unchanged this run; no flip to closed-by-drift, just last_verified refresh. anti-patterns 0. Risk band: medium per risk-rules.md — touches sub-component (SiteFooter) visible on EVERY route, but no shared-primitive surgery, no design-token edit, no SEO change, no new client boundary, net 56/-13 LOC well under 250-line high-band threshold; chrome-on-every-route visibility is the decisive medium-band signal (not file count, not LOC). Direct-commit to main per medium-band default — commit 74f4fe3 (push to main: 9e2b5f6..74f4fe3). Screenshots skipped — capture-ship.mjs carry-over blocker, now 14 consecutive ships old (capture-ship-stale-server-on-3000 backlog at severity high — the longest-running open infra blocker; a chrome ship visible on every route is exactly where a visual diff against the prior chrome ship would have been most valuable). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per established pattern for medium-risk register-coherence ships with PASS verdicts across all 5 Phase 5 gates AND a structurally-small CSS-only behavioral diff (the JSX scaffold was pre-existing). Notion writeback: no task to flip (no override); Reports row 362af8d3-d3e2-817c-967c-d274ff9d8dca created via mcp__Notion__API-post-page (first attempt succeeded with parent.database_id=60cc3221-8e8a-42da-b926-20cd32d6c8bb — the historical schema-fix lesson stays baked into routine invocation) with Surface=chrome, Mode=Shipped, Rubric T3 M2 L2 I1 A3 D3 = 14/18, Risk=Medium, Commit=74f4fe3, all 10 schema fields populated. MCP path reliable three runs in a row. Closed-by-drift this run: none. Backlog: NEW item lemma-connector-pattern-extract-utility (low, hygiene — premature, needs a second dictionary-expansion-of-abbreviation surface before the .outro-wordmark-conn recipe is worth hoisting). All other backlog items unchanged this run — folio-numeral-pattern-extract-utility remains at high (top-priority system ship, still uncommitted — this ship intentionally did NOT touch numerals so leaves that queue intact), rule-draw-keyframe-consolidate remains at medium (no new identical keyframe this run), related-grid-rule-color-token-promotion remains at medium (scope unchanged), related-eyebrow-arrow-glyph-aria-hidden-double-emphasis remains at low (scope unchanged), outro-wordmark-settle-token-promotion remains open (trigger condition 'second settle instance lands' is unchanged — the rescope was a target-selector move, not a second keyframe instance).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 65 — NOT a multiple of 10, so no calibrator trigger this run. Next eligible at shipped_count=70.

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok; push to origin/main succeeded (commit 2c96b43: e4682b1..2c96b43)
playwright_available: unknown  # capture-ship.mjs skipped — carry-over NOW 16 ships. capture-ship-stale-server-on-3000 backlog at severity high — the longest-running open infra blocker; this run added the first /journal index chrome AND the first interactive tag affordance on 11 post pages where visual diff against the pre-rail /journal would have been the cleanest demonstration of the affordance gap closing. Historian flagged this run that the underlying stale next-server PID 46706 (next-server v16.2.4 elapsed 01-16+ hours on TCP *:3000 LISTEN with curl HTTP 000) is still hung; capture-ship.mjs spawnServer ignores EADDRINUSE silently (stdout/stderr piped at lines 81–85) so the loop times out. Promote to top-of-queue if surface-freshness ever stops dominating Phase 2 picks (or if a critic/retro flags the missing visual record).
lighthouse_available: unknown  # perf-a11y skipped lighthouse — CSS+SSR-only change with no client component added and no above-fold LCP element touched; no prior baseline in lighthouse.csv
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 700000   # +20000 bytes for this run's IMPROVEMENTS.md entry (journal-post-tag-affordance-and-index-filter); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 4 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy four runs in a row. Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 rows. Reports notion-post-page initially hit two schema bumps — (a) Surface was passed as multi_select but the schema defines it as a single select; (b) the property "Files" doesn't exist (correct name is "Files changed"). Both fixed in one retry. Reports row 362af8d3-d3e2-81aa-be1b-fdcc7f96fd9b created with 9 schema fields populated on create, Files changed populated via mcp__Notion__API-patch-page follow-up call. The "use single-select for Surface AND name is 'Files changed' not 'Files'" lesson logged for next routine refresh; routine prompt should encode these schema specifics so the create call succeeds first attempt. MCP path reliable.
