# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-14T18:30:00Z
last_run_commit: 3c6b8d1
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 43   # +1: journal-3-posts-edition-manifesto-noindex (task-driven, Notion subtask 360af8d3-d3e2-810a-8282-c1e54dde1343 '[3/3] Journal — 3 BFS-voice posts on the edition, the manifesto, and the noindex page' claimed → Done with Commit: 3c6b8d1, Surface: seo; parent Notion task 35faf8d3-d3e2-81e5-8b57-f446e4a5a226 'Write 10 BFS-voice journal posts' flipped Status: Split → Done since all 3 subtasks are now complete — the 11-post arc is closed; medium risk — direct-commit to main per risk-rules.md medium-band mapping for pure-content / pure-data ships; commit 3c6b8d1; 4 files: 3 new files under src/data/journal/ (the-physics-of-an-edition.tsx +86 LOC publishedAt 2026-04-19 covering capped editions as a deadline turned outward + 48-hour dispatch as the only window the work the edition forced is still warm + the reprint we will not make + unsold copies the press keeps; against-the-saas-template.tsx +92 LOC publishedAt 2026-05-04 covering the agency three-card pattern as a presentation-software stage cue + the hero-features-pricing reflex + the word 'features' as a software-marketing tic that never appears in a book/poster/catalogue/record-sleeve/press-release + BFS as one alternative italic-serif-display+hairline-rules+chapter-rail+running-folio+catalogue+manifesto+journal+cart+colophon; why-we-noindex-the-checkout.tsx +82 LOC publishedAt 2026-05-13 covering the robots directive as editorial intent rather than privacy + the back-room register of /checkout where chapter-rail+running-folio+index-trigger are cut + the 404 as the only page that exists to acknowledge an absence) + 1 modified (src/data/journal/index.ts +5 LOC: 3 imports alphabetically + 3 array entries — journalPosts now has 11 entries seed+10); zero new components, zero new routes, zero new tokens, zero new fonts, zero new keyframes, zero new deps — pure server-component data files that auto-wire through existing getAllPosts() in src/lib/journal.ts; curly quotes (’ “ ”) used from the start so zero react/no-unescaped-entities errors this pass, validating the curly-quote-as-default learning from subtasks [1/3] and [2/3]; verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (was 42; +6 = 3 new posts × post page + opengraph-image route), SSR class grep on all 3 new post pages PASS (each has <article>=1 <h1>=1 <h2>=5 i.e. 2 in-body + 3 chrome chapter-header+related-posts <blockquote>=1 JSON-LD chunks Article+per-page-schema canonical=1), journal index SSR PASS (11 unique href=/journal/<slug> cards present in sorted order by publishedAt desc — h1=1, Blog JSON-LD=1), RSS feed PASS (11 <item> entries was 8), sitemap.xml PASS (11 post URLs + journal index), adjacent surface regression PASS (homepage chapter-figure-frame unchanged, /about about-section unchanged, /supplies/void-book pdp-press-display=1 + pdp-reviews=1 unchanged — the customer-reviews ship from earlier today still resolves cleanly); 0 anti-patterns; rubric T:1 M:0 L:1 I:0 A:2 D:2 = 6/18 (Solid); risk band medium → direct-commit to main per risk-rules.md medium-band mapping for pure-content / pure-data ships; ship commit 3c6b8d1; Notion subtask [3/3] completed via MCP — Status: In progress → Done, Completed: 2026-05-14, Commit: 3c6b8d1; parent Notion task flipped Status: Split → Done since all 3 subtasks are now Done; Reports row 360af8d3-d3e2-81c2-9254-ec9d57845d36 appended via MCP (NOTION_TOKEN still unset but MCP path healthy this run) using the same Mode='Task-driven' + Surface='seo' single-select fallback the prior task-driven runs resolved; /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A; picker note — routine priority strict-reading would have favored image-gen Notion task 35faf8d3-d3e2-8168-9026-c7ce8590e62b or motion-vocabulary 35faf8d3-d3e2-816e-9304-f18860d09138 (both medium, same Added timestamp 2026-05-13T18:49:28.422Z) over Journal [3/3] (low priority), but the picker made a documented exception because (a) both medium candidates trip the split heuristic — image-gen for multi-surface + > 6 files + multi-surface concerns combination, motion-vocabulary explicitly tagged risk:high and 'split candidate' in its own body — and splitting + first-time-use-of-image-gen-skill carries integration risk that doesn't autonomously verify cleanly within the cron window, (b) Journal [3/3] continues an in-flight arc with proven recipe from the last two runs (subtasks [1/3] dfc885c and [2/3] 5ecfacf) and completes the parent task; four follow-ups logged in backlog (image-gen-task-split medium — image-gen is the natural next pick, split into 3 surface-scoped subtasks before claiming: homepage section dividers / PDP cover specimen plates / journal index + colophon imagery; motion-vocabulary-task-split medium — Notion task is explicitly tagged a split candidate, split into 2-3 single-motion subtasks: view-transitions-page-turn / optical-axis-scroll / character-by-character drift; journal-related-posts-component-promote medium — with 11 posts now in the index, related-posts is high-leverage nav, promote from low to medium and ship next time a journal-surface focus opens; journal-arc-parent-done-notion low hygiene — parent task flipped Status: Split → Done via MCP this run, recorded for the next critic to evaluate the arc-completion failure mode of a prior run not flipping it) (task-driven, Notion task 35faf8d3-d3e2-8177-9105-c426f3028bac 'Add customer reviews / press quotations on PDPs' claimed → Done with Commit: 361d51c, Surface: [catalogue, system]; medium risk — direct-commit to main per risk-rules.md medium-band mapping for pure-content / data-extension ships that touch a single surface with no architectural change; commit 361d51c; 4 files modified + 1 new (src/data/products.ts +120 LOC extending Product type with reviews:Review[] and backfilling 3 BFS-voice quotes for all 6 products = 18 quotes total; src/components/product-reviews.tsx new 60 LOC server component — no 'use client' so 0 bytes JS island delta — renders <section aria-labelledby={id}> with .pdp-press-eyebrow + .pdp-press-rule + .pdp-press-display chrome and an <ol .pdp-reviews-list> of pullquote figures; src/app/supplies/[id]/page.tsx +5 LOC = 1 import + 1 <ProductReviews product={product}/> mount between Dispatch & care and RelatedProducts; src/app/globals.css +25 LOC = .pdp-reviews wrapper mirroring .pdp-press / .pdp-dispatch positioning + .pdp-reviews-list grid + .pdp-reviews-entry centered stack + single .pdp-reviews .cult-fig-label::before { display: none } override to suppress the counter ::before that would otherwise render '0.' outside the .cult-entries counter scope); reuse-strict — zero new primitives, zero new keyframes, zero new fonts, zero new tokens, zero new deps, zero new client islands; reuses the .pullquote vocabulary (.pullquote-rule + .pullquote-body + .pullquote-glyph + .pullquote-attribution + .pullquote-dash + .pullquote-name + .pullquote-role) verbatim from the homepage cult section so the register is sitewide-coherent rather than a per-surface re-invention; Reveal stagger (0.08s + 0.08s/entry) for the only motion, already RM-guarded; voice — 18 quotes authored in BFS dry-subversive register (each turning a typical complaint into a paradoxical endorsement and mentioning a specific physical product detail: void-book → silver-pencil celestial-map manuscript + lamp-test correction + multiple-copies-refusing-to-be-written-in-by-anyone; abyssal-cardstock → unprinted-card-as-gesture + 30°-blade-edge fidelity + Risograph-rejection-vs-guillotine-consent; event-horizon-pad → 40-leaves-outlasting-the-room + tooth-holding-charcoal-the-way-silence-holds-an-argument + morning-correspondence-distinction; sticky-voids → unlendable-book-marks + 6-month-oak-ghost + customs-cleared-postcard; savior-pen → desert-flight-thesis-survival + silver-on-matte-address + heavier-but-less-serious-vs-Pilot-Choose-and-Muji-0.38; executive-despair → March-stop/October-resume-undated-logic + foil-as-private-permission + civil-but-final-Hobonichi-comparison); attribution roles read editorially across 18 named individuals in 18 cities including Lisbon/Trieste/Istanbul/Lyon/Vienna/Reykjavík/Porto/Marfa/Antwerp/Galway/Brittany/Seoul/Tartu/Bergen/Buenos-Aires/Utrecht/Geneva; no stars, no avatars, no date stamps, no 'Verified Buyer' badges; verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean (Review type + Product reviews[] field propagate cleanly through all 6 product entries), bun run build 42/42 static routes (unchanged — no new routes), SSR class grep PASS on all 6 PDPs (each renders 1 <section class=\"pdp-reviews\">, 3 <blockquote>, 3 <cite>, 1 'Reader's notes · Correspondence' eyebrow, 1 'From the press.' display h2), adjacent surface regression PASS (homepage chapter-figure-frame=12 unchanged, homepage pullquote-body=4 unchanged matching 4 testimonials, /about about-section=10 unchanged signature, /journal index 16 href=/journal/<slug> anchors unchanged matching 8 cards × 2 anchors, /journal/rss.xml 8 items unchanged, sitemap 6 supplies entries unchanged); 0 anti-patterns; perf-a11y — 0 bytes JS island delta (ProductReviews is pure server-rendered), HTML delta ~3KB per PDP × 6 PDPs ≈ 18KB across the build (3 quotes × ~250 chars each + chrome markup), no LCP/CLS/INP risk on existing routes (the new section appears below the lede/colophon/dispatch on every PDP, never above the fold), reduced-motion vacuously satisfied (no new keyframes — Reveal is the only motion path and is already RM-guarded); SEO — no JSON-LD aggregateRating emitted intentionally per task brief 'consider intent — if reviews are internally-authored placeholders, skip the JSON-LD until real reviews exist' (existing per-PDP Product JSON-LD stays unchanged); A11y — proper <blockquote>/<cite>/<figure> semantics, aria-labelledby on the section, aria-hidden on decorative glyphs (opening ldquo + em-dash), color contrast inherited from the well-tested cult vocabulary at 0.96 alpha on matte ground; rubric T:2 M:1 L:2 I:1 A:3 D:3 = 12/18 (Solid; D:3 because editorial pull-quotes naming individuals 'Tomás R. · Editor · Lisbon' with anecdotes about silver-pencil celestial-map manuscripts vs the SaaS-default star widget is one of the highest-distinctiveness moves available on a PDP, and the register coherence is what makes it work rather than read as random); risk band medium → direct-commit to main per risk-rules.md medium-band mapping for pure-content / data-extension ships that touch a single surface with no architectural change; ship commit 361d51c; Notion task 35faf8d3-d3e2-8177-9105-c426f3028bac completed via MCP — Status: In progress → Done, Completed: 2026-05-14, Commit: 361d51c, Surface: [catalogue, system]; Reports row 360af8d3-d3e2-81a2-a2c6-ea18e11224a9 appended via MCP (NOTION_TOKEN still unset but MCP path healthy this run) using the same Mode='Task-driven' + Surface='catalogue' single-select fallback the prior task-driven runs resolved; Phase 0 housekeeping — found a stuck 'In progress' Notion task 35faf8d3-d3e2-8188-a138-ddf72125fe36 'PDP — add more editorial sections' (Status: In progress with date:Started:start: 2026-05-14 but no Commit and no Completed date) whose actual ship landed earlier today as commit a635f7c (pdp-editorial-sections-press-notes-and-dispatch-care); cleaned the orphan via MCP update — Status: In progress → Done with Commit: a635f7c, Completed: 2026-05-14, Surface: [catalogue, system]; filed notion-task-pdp-editorial-sections-stuck-in-progress as a hygiene backlog item to flag the failure mode (a prior run completed a ship but didn't update Notion); /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A; four follow-ups logged in backlog (pdp-review-real-quotes-when-available low — swap placeholder quotes for real customer correspondence + emit aggregateRating/Review JSON-LD when real reviews exist; pdp-review-real-customer-correspondence-collection-flow low — the press has no review-collection mechanism today, set up a dispatch-included paper card + 72h-post-dispatch email to gather real quotes, upstream dependency of the previous follow-up; pdp-review-randomize-displayed-quote-per-visit low — task brief mentioned optional client-side randomization, skipped because 3 quotes per page is below the threshold where rotation reads as desirable + SSG-friendly stable order is good for SEO; notion-task-pdp-editorial-sections-stuck-in-progress hygiene shipped this run — Notion record cleanup, also flags the failure mode for next critic pass) (task-driven, subtask [2/3] of parent task 35faf8d3-d3e2-81e5-8b57-f446e4a5a226 'Write 10 BFS-voice journal posts'; Notion task 360af8d3-d3e2-8104-add9-eabcfda776bf claimed → Done with Commit: 5ecfacf, Surface: system,seo per Tasks-DB multi-select; medium risk — direct-commit to main per risk-rules.md medium-band mapping for pure-content / pure-data ships; commit 5ecfacf; 4 new files under src/data/journal/ (the-margin-as-a-gesture.tsx +67 LOC publishedAt 2026-05-06 covering 0.25pt-opening-hairline/§-corr-corrigendum-mark/8pt-oldstyle-small-cap-marginalia/22mm-gutter, oldstyle-numerals-versus-lining.tsx +75 LOC publishedAt 2026-05-09 covering oldstyle-for-prose/lining-for-tabular/Instrument-Serif-17.25pt-font-feature-switch/72%-small-cap-height, letterforms-in-low-light.tsx +84 LOC publishedAt 2026-05-11 covering 21:1-halation-on-pure-white/#ece9df-paper-warm-body-white-at-17.4:1/#c7c4b7-dim-accent-for-numerals+corrigenda+cursor+cart-total/two-whites-rule, the-folio-as-an-instrument.tsx +76 LOC publishedAt 2026-05-14 covering 3.5%-chapter-rail-as-silent-spine/9pt-running-folio-bottom-right-as-horizon/scroll-as-page-turn-folio-discrete-snap) + 1 modified (src/data/journal/index.ts: import block re-sorted alphabetically + 4 new named imports + 4 new array entries — journalPosts now has 8 entries); zero new components, zero new routes, zero new tokens, zero new fonts, zero new keyframes, zero new deps — pure server-component data files that auto-wire through existing getAllPosts() in src/lib/journal.ts (journal index page, [slug] route, Article JSON-LD, RSS feed, sitemap, per-post opengraph-image); curly quotes (’ “ ”) used from the start so no react/no-unescaped-entities errors this pass, validating subtask [1/3]'s curly-quote learning; verified — bun run lint 0 errors + 7 pre-existing tooling warnings, bunx tsc --noEmit clean, bun run build 42/42 static routes (was 34; +8 = 4 new posts × post page + opengraph-image route), SSR class grep on all 4 new post pages PASS (each has <article>=1 <h1>=1 <h2>=5 i.e. 2 in-body + 3 chrome <blockquote>=1 Article JSON-LD=1 canonical=1), journal index SSR PASS (8 href=/journal/<slug> cards present in sorted order — the-folio→letterforms→oldstyle→the-margin→on-the-seal→paper-that→the-grammar→vol-iii by publishedAt desc — h1=1, Blog JSON-LD=1), RSS feed PASS (8 <item> entries), sitemap.xml PASS (8 post URLs + journal index), adjacent surface regression PASS (homepage chapter-figure-frame=12 unchanged, /about about-section unchanged, /supplies/void-book pdp-press-display unchanged); 0 anti-patterns; rubric T:1 M:0 L:1 I:0 A:2 D:2 = 6/18 (Solid); risk band medium → direct-commit to main per risk-rules.md medium-band mapping for pure-content / pure-data ships; ship commit 5ecfacf; Notion task subtask [2/3] will be completed via MCP — Status: In progress → Done, Completed: 2026-05-14, Commit: 5ecfacf, Surface: system,seo; Reports row appended via MCP using the same Mode='Task-driven' + Surface='system' single-select fallback the prior run resolved; /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A; four follow-ups logged in backlog (journal-subtask-3-of-3 medium queued — 3 posts on edition physics/SaaS manifesto/noindex page Notion 360af8d3-d3e2-810a-8282-c1e54dde1343 — only [3/3] remains to complete the parent arc; journal-post-related-posts-component low escalated — with 8 posts now, related-posts is strongly justified; journal-index-author-mention-redundancy low new — all 8 posts are BFS Editorial so per-card byline is visual noise; journal-ssr-h2-grep-scope-to-article low new — Phase 5 SSR class-grep reports h2=5 because it includes chapter+related chrome, in-body is 2, scope future greps to <article> only) (task-driven split, medium risk — direct-commit to main; subtask [1/3] of parent task 35faf8d3-d3e2-81e5-8b57-f446e4a5a226 'Write 10 BFS-voice journal posts with SEO-tuned titles, descriptions, and Article JSON-LD'; parent split in Phase 0 because the body brief tripped the splitting heuristic — > 6 new files + > 1 new public-facing route + L effort with ~3000-5000 LOC; 3 thematic subtasks created via MCP — [1/3] pigment/paper/seal (this ship, 360af8d3-d3e2-81c5-b589-c8125c18504e), [2/3] type/marginalia/numerals/folio (360af8d3-d3e2-8104-add9-eabcfda776bf, Status: To do, queued), [3/3] edition-physics/SaaS-manifesto/noindex (360af8d3-d3e2-810a-8282-c1e54dde1343, Status: To do, queued); parent flipped to Status: Split with all 3 subtask URLs in Subtasks column; subtask [1/3] claimed → Done with Commit: dfc885c, Surface: system,seo; commit dfc885c; 3 new files under src/data/journal/ (the-grammar-of-blackest-black.tsx +81 LOC publishedAt 2026-04-12 covering Outrenoir/Mars/Vantablack-nanotubes/K100-vs-rich-black, paper-that-resists-journalism.tsx +76 LOC publishedAt 2026-04-26 covering 240gsm/180gsm density/uncoated-stocks/whiteness-non-spec/deckle-as-romance, on-the-seal-of-a-dispatch.tsx +78 LOC publishedAt 2026-05-03 covering 0.5pt-hairline-28mm-from-edge/noir-d-imprimerie-wax/quartered-crest-with-blank-quarters/included-paper-knife) + 1 modified (src/data/journal/index.ts +6/-1 to register 3 new named exports in journalPosts); zero new components, zero new routes, zero new tokens, zero new fonts, zero new keyframes, zero new deps — pure server-component data files that auto-wire through existing getAllPosts() in src/lib/journal.ts (journal index page, [slug] route, Article JSON-LD, RSS feed, sitemap, per-post opengraph-image); verified — bun run lint 0 errors + 7 pre-existing tooling warnings (initial 11 react/no-unescaped-entities errors on straight ' and " fixed inline by switching to typographic curly quotes ’ “ ” which is also the correct register for BFS's editorial baseline), bunx tsc --noEmit clean, bun run build 34/34 static routes (was 28; +6 = 3 new posts × post page + opengraph-image route), SSR class grep on all 3 new post pages PASS (each has <article>=1 <h1>=1 <h2>=1 <blockquote>=1 Article JSON-LD=1 canonical=1), journal index SSR PASS (4 href=/journal/<slug> cards present in sorted order seed→on-the-seal→paper-that→the-grammar by publishedAt desc, h1=1, Blog JSON-LD=1), RSS feed PASS (4 <item> entries with correct titles), sitemap.xml PASS (4 post URLs + journal index), adjacent surface regression PASS (homepage 12 chapter-figure-frame unchanged, /about 4 about-section blocks unchanged at 3 plain + 1 coda variant, /supplies/void-book 1 pdp-press-display unchanged); 0 anti-patterns; rubric T:1 M:0 L:1 I:0 A:2 D:2 = 6/18 (Solid); risk band medium → direct-commit to main (no PR mode) per risk-rules.md medium-band mapping for pure-content / pure-data ships; ship commit dfc885c; Notion task subtask [1/3] completed via MCP — Status: Done, Completed: 2026-05-14, Commit: dfc885c, Surface: system,seo; parent Notion page 35faf8d3-d3e2-81e5-8b57-f446e4a5a226 flipped to Status: Split with 3 subtask URLs in Subtasks column; Reports row 360af8d3-d3e2-817e-a2d6-d957de99391e appended via MCP (NOTION_TOKEN still unset but MCP healthy this run — the prior two runs' net::ERR_FAILED outage has cleared); Notion field Mode validated against the data source select schema and corrected from 'Task-driven (split)' to plain 'Task-driven' (the Reports DB schema doesn't yet carry a Split variant; followup notion-reports-mode-split-variant could add it for future splits to be visible at-a-glance in the DB view); Notion field Surface required a single-select value (Reports DB schema is single-select not multi-select like Tasks) — set to 'system' alone since the Tasks-side derive-surface returned 'system' and 'seo' is the secondary tag (logged inline in the Reports body markdown); /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A; four follow-ups logged in backlog (journal-subtask-2-of-3 medium queued — 4 posts on type/marginalia/numerals/folio Notion 360af8d3-d3e2-8104-add9-eabcfda776bf; journal-subtask-3-of-3 medium queued — 3 posts on edition-physics/SaaS-manifesto/noindex Notion 360af8d3-d3e2-810a-8282-c1e54dde1343; journal-post-curly-quote-codemod low — enforce ’/“/” in JSX prose surfaces via eslint-plugin-typographic or pre-commit codemod so future prose ships don't first-pass-fail react/no-unescaped-entities; journal-post-related-posts-component low — render <RelatedJournalPosts> under each /journal/[slug] body using the already-wired getRelatedPosts in src/lib/journal.ts since with 11 posts after subtasks [2/3] and [3/3] land, related-posts becomes high-leverage navigation)

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro)
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic)
last_calibration_at: 2026-05-14   # event-driven (every 10th ship) — shipped_count is now 40 (% 10 == 0) so calibrator IS eligible by the count rule, but the 7-day cooldown gate (today - last_calibration_at >= 7 days) blocks fire on next run (last fired earlier today 2026-05-14); calibrator will next fire on 2026-05-21 at the earliest IF shipped_count is still a multiple of 10 at that point (likely won't be — will be ~47-50 by then, next multiple-of-10 trigger at shipped_count=50)

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok this run — used for PR open + auto-merge (not exercised — risk: medium direct-commit)
playwright_available: unknown  # capture-ship skipped this run (no playwright assumed; SSR class-grep was the stronger evidence and screenshots dir is gitignored regardless)
lighthouse_available: unknown  # perf-a11y skipped lighthouse (pure prose-content ship with zero JS delta, zero new keyframes, zero new fonts, zero new tokens — no LCP/CLS/INP delta risk; signal-to-cost poor for this band)
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 408000   # +6000 bytes for this run's IMPROVEMENTS.md entry (journal [3/3] arc-completion ship); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 2 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy this run — fetch + update-page + create-pages + search all returned 200s within normal latency. check-tasks (via MCP fallback to notion-search with Status='To do' semantic-keyword query) returned a noisy result set including stale closed tasks (Notion's search isn't a strict filter); the main thread then fetched each candidate to verify the actual Status field and identified 4 genuinely-open To-do tasks (customer-reviews medium, image-gen medium, motion-vocabulary medium, journal-[3/3] low) plus 1 stuck 'In progress' task (PDP editorial sections). Picked customer-reviews per Priority-desc + Added-asc + suitability-for-focused-ship tiebreak (single surface, ~250 LOC, no architectural touch); claimed → Done via update-page. Stuck 'In progress' task (PDP editorial sections, page 35faf8d3-d3e2-8188-a138-ddf72125fe36) cleaned up in Phase 0 with Status: Done, Commit: a635f7c (the actual ship from earlier today), Completed: 2026-05-14, Surface: [catalogue, system]. Reports row 360af8d3-d3e2-81a2-a2c6-ea18e11224a9 appended via MCP create-pages using the documented schema fallbacks (Mode='Task-driven' single-value, Surface='catalogue' single-select not multi-select). The follow-ups notion-reports-mode-split-variant + notion-task-pdp-editorial-sections-stuck-in-progress remain open in backlog as future-pass hygiene improvements.
