# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-14T11:19:00Z
last_run_commit: af804b3
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 36   # +1: site-footer-extract-mount-all-routes (task-driven, high risk — Notion page 35faf8d3-d3e2-811d-a834-d03772a81905 "Show the footer (newsletter + colophon + outro) on all routes except /checkout"); architectural chrome ship that lifts the homepage outro (colophon dl + newsletter form + outro-grid + statutory nav + closing BFS wordmark, ~130 LOC) out of src/app/page.tsx into a reusable Server Component at src/components/site-footer.tsx (144 LOC, all client islands intact — Reveal/Magnetic/SplitText/Newsletter hydrate as before) and mounts it layout-level via a thin client gate src/components/site-footer-mount.tsx (9 LOC, "use client" + usePathname → null on /checkout, otherwise renders children passed from server layout); layout.tsx (+5 LOC) imports both and mounts `<SiteFooterMount><SiteFooter /></SiteFooterMount>` between {children} and SiteChrome; page.tsx (-132 LOC, -1 unused Newsletter import) loses the inline footer block; anchor normalization converts every `<a href="#supplies|#manifesto|#cult|#faq">` to `<Link href="/#supplies">` etc. (also the magnetic 'Return to the catalogue' CTA) so the same nav works from /journal or /privacy → incidentally closes the `outro-anchor-normalize` backlog item (low, 2026-05-13) as closed-by-drift; zero new CSS / zero new tokens / zero new keyframes / zero new deps — all .outro* / .newsletter / .outro-colophon-* / .outro-links* / .outro-wordmark / .outro-signoff / .outro-disclaimer styles already exist in globals.css from the prior homepage-only mount; per-route mini-footers (journal-foot on /journal, journal-post-foot on post pages, PDP return-link footer on /supplies/<id>, legal-page-foot on /privacy/terms/cookies) stay intact as small route-specific signoffs preceding the global colophon; verified — bun run lint 0 errors + 7 pre-existing tooling warnings, bunx tsc --noEmit clean, bun run build 27/27 static routes (same count as prior ship); SSR class grep PASS — `class="outro"` count = 1 on /, /journal, /journal/<slug>, /supplies/void-book, /privacy, /terms, /cookies AND 0 on /checkout; newsletter form count matches identically (1/0); homepage non-footer markers (from-journal, cult-entries, faq-list, chapter-frame, outro-grid, outro-colophon, outro-wordmark, outro-signoff, outro-disclaimer, newsletter) all 1 — no regression; 0 anti-patterns; rubric T:1 M:2 L:3 I:1 A:2 D:1 = 10/18 (system ship — reusability + cross-route surface-coherence are the explicit goal; D:1 because footer markup renders identically to the prior homepage rendering — pure architectural lift, no new visual moment); risk band high triggered PR-mode → branch improvement/sitefooter-extract-mount-all-routes-except-checkout-20260514-1119 → PR #6 → auto-merged via Phase 6 Step 8.5 on the first mergeability poll (mergeable=MERGEABLE mergeStateStatus=CLEAN, no CI gates configured for this repo); ship commit ae9c70a, merge commit af804b3 (this row); Step 7.5 review skill skipped (graceful — local diff review on direct-to-main risk:low/medium ships isn't this skill's intended scope, and the routine explicitly permits skipping); Notion task complete — Status: Done, Completed: 2026-05-14, Commit: af804b3 (merge commit, not ship commit, since merge commit is what landed on main); Reports row 360af8d3-d3e2-8103-9262-fb4e42a4fb03 appended via MCP fallback (NOTION_TOKEN unset; Surface single-select set to 'chrome' as the dominant surface — the task's multi-select Surface[chrome,system,colophon,outro] doesn't fit a single-select destination, follow-up logged for a future schema sync); three follow-ups logged in backlog for future passes (site-footer-flight-payload-size low — RSC serializes children into Flight payload on every page incl. /checkout, ~5KB; site-footer-reveal-on-non-homepage low — verify Reveal/SplitText read naturally on short legal pages; site-footer-mini-footer-stacking low — stacked mini + global footer may read redundant on /journal/<slug> + /supplies/<id> + legal pages)

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro)
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic)
last_calibration_at: 2026-05-14   # event-driven (every 10th ship) — fired 2026-05-14 morning run; next at shipped_count=40

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok this run — used for PR open + auto-merge
playwright_available: unknown  # capture-ship skipped this run (SSR-only refactor; the SSR class-grep was the stronger evidence and the screenshots dir is gitignored regardless)
lighthouse_available: unknown  # perf-a11y skipped lighthouse (chrome refactor with identical SSR markup vs prior — no LCP/CLS/INP delta expected; signal-to-cost poor for this band)
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 219000   # approx after this run's entry; >200KB threshold — archive-improvements.mjs will rotate >60d entries on next run if any qualify (nothing old enough today: oldest entries are 2026-05-12, only 2 days old)

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # MCP fallback used this run (NOTION_TOKEN not set) — check-tasks via notion-search + claim/complete via notion-update-page + append-report via notion-create-pages all succeeded
