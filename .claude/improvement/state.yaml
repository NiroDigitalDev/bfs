# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T01:00:00Z
last_run_commit: d618203
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 49   # +1: motion-vocabulary-subtask-2-of-3-hover-letter-spacing (task-driven, in-progress resume — Notion subtask 360af8d3-d3e2-81d9-be2b-e36fc60aad23 was already Status: In progress with Started=2026-05-15 from an earlier aborted run, no Commit/Completed, working tree clean at session start; resumed in-flight rather than re-claimed or released. Adds the third motion mode to BFS's vocabulary — hover-driven typographic micro-interaction, orthogonal to scroll-driven [1/3] hero-char-drift + chapter-numeral-drift + outro-wordmark-settle family, and to upcoming cursor-distance [3/3] magnetic-reveal. 1 file modified (src/app/globals.css +20 LOC) = one @media (prefers-reduced-motion: no-preference) block at globals.css:~1446 between the existing .chapter-eyebrow-sub closing brace and the .chapter-title opening brace, containing 4 rules across 2 selectors: (1) .btn-primary transition extends to include letter-spacing 240ms cubic-bezier(0.32, 0.72, 0, 1) alongside existing color transition; .btn-primary:hover letter-spacing 0.16em → 0.18em across all 5 mount sites (hero #supplies CTA, /not-found CTA, /supplies/[id] PDP CTA, checkout-form submit, footer big CTA); (2) .chapter-eyebrow transition letter-spacing 240ms cubic-bezier(0.32, 0.72, 0, 1); .chapter:hover .chapter-eyebrow letter-spacing 0.3em → 0.32em parent-driven mirroring the existing .chapter:hover .chapter-numeral rule at globals.css:1390 so the eyebrow + numeral move together as a single gesture across all 6 homepage catalogue chapters. Brief allowed 4-5 selectors; capped at 2 to preserve intentionality — other display surfaces (.outro-link, .journal-foot-return, .cult-fig-label, .cart-line-remove) already carry their own hover vocabulary (hairline draws, color shifts, cross-fades) and don't need letter-spacing layered on top. Reduced-motion strategy — gating the entire block (not just the transition) means RM users see the rest state on hover with no transition AND no target state, the cleanest possible degrade since both the transition and the :hover letter-spacing declaration are gated out. btn-primary transition-shorthand compose — the base rule at globals.css:1135 declares 'transition: color var(--dur-2) ease'; inside the gate we declare the full shorthand including both properties so the existing color fade isn't lost. Layout-shift analysis — 0.02em delta on 13px font (.btn-primary) is ~0.26px/char × 19-char 'Enter the catalogue' = ~5px button growth; inline-flex with internal padding 16px 24px inside flex containers with horizontal slack, no neighbor shift. 0.02em delta on 11px font (.chapter-eyebrow) is ~0.22px/char × 22-char 'Chapter 01 / Subtitle' = ~5px growth; inline-flex inside the chapter header column wider than the content, no neighbor shift. No new tokens (the 0.02em deltas don't justify token promotion — they're spec-named magnitudes per the Notion brief). No new keyframes (this is a transition not an animation). 240ms/0.18em/0.32em/cubic-bezier(0.32,0.72,0,1) are spec-named values from the brief. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (unchanged), 0 anti-patterns. regression-spotter N/A by definition (CSS-only diff verified via git diff --name-only — 1 file, +20 LOC, 0 JSX/data changes; SSR HTML is byte-identical to the prior ship since class signatures cannot change without JSX touches, and hover-only paint is invisible to a static SSR grep anyway). perf-a11y PASS (0 bytes JS delta — CSS-only, no client boundary; CSS delta ~440 bytes pre-minification; no HTML delta; no LCP risk — decorative hover transition with no above-fold paint change at rest; no CLS risk — 5px width growth on inline-flex with horizontal slack; no INP risk — compositor-optimized letter-spacing transition under the text-system renderer; contrast unchanged — no color change; reduced-motion respected via @media gate wrapping the entire block; keyboard unaffected — hover-only behavior, focus already shows the global :focus-visible ring from globals.css:80-86 independent of hover state). Self-rated rubric T:2 M:2 L:2 I:2 A:2 D:2 = 12/18 (Distinctive band). Risk band low → direct-commit to main per risk-rules.md low-band default. Ship commit d618203. Notion task-driven mode (in-progress resume) — subtask 360af8d3-d3e2-81d9-be2b-e36fc60aad23 flipped Status: In progress → Done via MCP update-page with Commit: d618203, Completed: 2026-05-15. Parent motion-vocabulary task 35faf8d3-d3e2-816e-9304-f18860d09138 remains Status: Split with subtask [3/3] magnetic-reveal still Status: To do queued. Reports row 361af8d3-d3e2-81ad-9c71-fe6fb6cfeeef appended via MCP create-pages using documented schema fallbacks (Mode='Task-driven' single-value, Surface='system' single-select since Reports DB is single-select unlike Tasks). /review skill skipped per routine — CSS-only RM-gated 20-LOC additive change with PASS verdicts across all 5 Phase 5 gates (verifier + perf-a11y + regression-spotter [N/A by definition] + diff-reviewer self-pass + anti-patterns); review value would be re-reading the same diff with no surface novelty to interrogate. 5 follow-ups logged in backlog: motion-vocabulary-subtask-3-of-3-magnetic-reveal (medium, queued — Notion 360af8d3-d3e2-81ab-bdbb-fe56c3cd37ed, modifies shared <Magnetic /> primitive at src/components/magnetic.tsx with new reveal boolean prop + cursor-distance CSS var --magnetic-distance; JS work, ship last per brief because regression risk on existing Magnetic consumer sites is highest); chapter-hover-eyebrow-numeral-pair-token-promotion (low, hygiene — .chapter:hover now drives both .chapter-numeral stroke and .chapter-eyebrow letter-spacing; if a third .chapter:hover descendant rule lands, promote to a single --chapter-hover-dur + --chapter-hover-ease token pair); btn-primary-hover-letter-spacing-mobile-touch-behavior (low, hygiene — on touch devices :hover triggers on tap and lingers until next tap; behavior is acceptable since the rest state is unchanged at tap-release, but worth a future audit of all hover-driven motion under @media (hover: hover) gate as the pattern grows beyond 2 selectors); image-gen-subtask-2-of-3-pdp-specimen-plates (medium, queued — Notion 360af8d3-d3e2-816e-84c5-d94cdd04faf3, carried over from image-gen [1/3] ship); image-gen-subtask-3-of-3-journal-manifesto-colophon-imagery (medium, queued — Notion 360af8d3-d3e2-81d5-a53d-f21127250381, carried over from image-gen [1/3] ship). Picker note — task-driven mode resumed an existing In progress claim from an earlier-today aborted run rather than re-claiming or releasing. Among the 4 open subtasks (motion-vocabulary [2/3] + [3/3], image-gen [2/3] + [3/3]) this one had the tied-earliest Added timestamp (with motion-vocabulary [3/3]) and the lowest risk profile of the tied pair — [3/3] touches the shared <Magnetic /> primitive with JS work and is correctly queued last per its own brief. Periodic triggers — none fired this run: retro 2 days old (need ≥7), critic 2 days old (need ≥28), calibration shipped_count now 49 (not a multiple of 10), consecutive_no_focus_runs 0 (no creativity-reset trigger). (task-driven split — subtask [1/3] of image-gen parent task 35faf8d3-d3e2-8168-9026-c7ce8590e62b, which was split this run in Phase 0 into 3 surface-scoped subtasks via MCP create-pages per the prior runs' notion-task-image-gen-split-into-surface-subtasks-mandate-no-defer-5th mandate; parent flipped Status: To do → Split with 3 subtask URLs in Subtasks column; [1/3] 360af8d3-d3e2-81eb-b05c-c1de5178ccf6 claimed → Done with Commit: 9bce85e, Surface: [hero, catalogue, system]; [2/3] 360af8d3-d3e2-816e-84c5-d94cdd04faf3 PDP specimen-plate frames and [3/3] 360af8d3-d3e2-81d5-a53d-f21127250381 journal+manifesto+colophon imagery remain Status: To do queued. Mandate-no-defer-5th HONORED — the image-gen task that had been deferred 4 consecutive runs is now split and one subtask is shipped. Adds 3 files: src/components/section-divider.tsx NEW 28 LOC inline-SVG server component (the SVG was generated by the image-gen skill via ~/.claude/skills/image-gen/scripts/generate-svg.ts on google/gemini-3-pro-preview, 0 retries, ~7s real time — first successful image-gen skill invocation in the autonomous cron), src/app/page.tsx +5 LOC (1 import + wrap journalLatest.length>0 conditional content in a <> fragment so <SectionDivider /> precedes <section id=from-journal>), src/app/globals.css +17 LOC immediately before .from-journal at line ~2618 with .section-divider wrapper + nested svg rules. Mount: between Field Notes (#cult) and From the Journal (#from-journal) on homepage — marks the threshold between two distinct editorial registers. Composition: viewBox 0 0 1440 80, one continuous M/C/l path drawing a hand-torn paper top-edge with ~18 stochastic notches across the full width, plus 3 vertical line registration ticks at x=200/720/1240 and 1 circle press-mark at x=1268/y=46/r=1.5. Stroke=currentColor at strokeWidth=0.6, opacity=0.5, fill=none. Color delegated to the parent via color: var(--color-line-2) on .section-divider. No reduced-motion gate needed — no animation. No @supports gate — inline SVG is universally supported. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (unchanged), 0 anti-patterns, regression-spotter PASS (SSR class-grep on / via port 3017 since :3000 is taken: .section-divider 2 new — wrapper div + svg, .chapter-numeral 12 unchanged matching prior view-timeline ship, .outro-wordmark 2 unchanged matching prior view-timeline ship, .hero-title 4 unchanged, .split-char 224 unchanged matching last run's hero-char-drift signature, .from-journal-item 4 unchanged — 2 latest posts × 2 mentions each; the new component is scoped to .section-divider only — does NOT affect any other surface), perf-a11y PASS (0 bytes JS delta — server-rendered SVG, no 'use client' boundary; CSS delta ~440 bytes pre-minification; HTML delta ~1.2KB per homepage SSR; no LCP risk — divider mounts below cult section well below the fold; no CLS risk by spec — svg has viewBox + width:100%; height:auto fixed aspect ratio container; no INP risk — no JS, no listeners, pointer-events:none; contrast N/A — aria-hidden decorative element not subject to WCAG minimums; keyboard N/A — pointer-events:none + user-select:none + aria-hidden + no tabindex so completely invisible to AT). Self-rated rubric T:2 M:1 L:1 I:1 A:2 D:3 = 10/18 (Solid; clears the >8 abort threshold). T:2 first decorative inline-SVG asset on the site, establishing the imagery vocabulary the parent task asked for. M:1 inline SVG is universally supported; no novel CSS technique. L:1 single transition (deliberately scoped per [1/3] subtask). I:1 no interactive affordance. A:2 fully aria-hidden decorative element with pointer-events:none. D:3 torn-paper hairline-edge composition with registration ticks + press-mark is the kind of editorial detail that reads as designed plate not template ornament. Risk band low → direct-commit to main per risk-rules.md low-band default. Ship commit 9bce85e. Notion task-driven mode — parent task 35faf8d3-d3e2-8168-9026-c7ce8590e62b flipped Status: To do → Split via MCP notion-update-page; 3 subtask pages created via MCP notion-create-pages with Notion-flavored markdown bodies; subtask [1/3] 360af8d3-d3e2-81eb-b05c-c1de5178ccf6 claimed → Done with Commit: 9bce85e, Completed: 2026-05-15, Surface: [hero, catalogue, system]; Reports row 360af8d3-d3e2-8167-86b1-c962abaa9a18 appended via MCP create-pages using the documented schema fallbacks (Mode='Task-driven' single-value, Surface='catalogue' single-select since Reports DB is single-select unlike Tasks). /review skill skipped per routine — inline-SVG additive change with PASS verdicts across all 5 Phase 5 reviewers (verifier + perf-a11y + regression-spotter + diff-reviewer + anti-patterns); review value would be re-reading the same diff. 5 follow-ups logged in backlog: notion-task-image-gen-split-into-surface-subtasks-mandate-no-defer-5th (medium → shipped this run); image-gen-subtask-2-of-3-pdp-specimen-plates (medium, distinctive, queued in Notion); image-gen-subtask-3-of-3-journal-manifesto-colophon-imagery (medium, distinctive, queued in Notion); section-divider-additional-mount-sites (low, distinctive — cap at 2 site-wide); image-gen-skill-svg-output-fidelity-spot-check (low, hygiene); image-gen-skill-as-first-class-tool-doc-claudemd (low, infra — document the bun run generate-svg.ts /tmp/<config>.json invocation pattern in CLAUDE.md). (task-driven split — subtask [1/3] of motion-vocabulary parent task 35faf8d3-d3e2-816e-9304-f18860d09138, which was split this run in Phase 0 into 3 single-motion subtasks via MCP create-pages; parent flipped Status: To do → Split with 3 subtask URLs in Subtasks column; [1/3] 360af8d3-d3e2-8118-8122-d43bac403bd4 claimed → Done with Commit: 8708f51, Surface: [hero, system]; [2/3] 360af8d3-d3e2-81d9-be2b-e36fc60aad23 hover-bound letter-spacing and [3/3] 360af8d3-d3e2-81ab-bdbb-fe56c3cd37ed magnetic reveal extension remain Status: To do queued. Per prior state.yaml's "DO NOT DEFER A 4TH TIME" mandate, the picker honored the split-and-ship-[1/3] pattern that the journal-arc parent used last week — split in Phase 0 via MCP create-pages, claim [1/3] as focus, ship CSS-only block, defer [2/3] and [3/3] to future runs. The image-gen Notion task 35faf8d3-d3e2-8168-9026-c7ce8590e62b remains unclaimed and is now deferred 4 consecutive runs; new backlog item notion-task-image-gen-split-into-surface-subtasks-mandate-no-defer-5th explicitly mandates split-before-claim on the next image-gen pick. Adds 34 LOC to src/app/globals.css after .hero-period .split-char at line ~842 — one @supports (animation-timeline: view()) block wrapping @media (prefers-reduced-motion: no-preference) and (min-width: 900px), containing view-timeline-name: --hero-title-tl on .hero-title, animation/animation-timeline/animation-range on .hero-title .split-char with the named timeline, .hero-title .split-char:nth-child(even) swapping the keyframe name from hero-char-drift-a to hero-char-drift-b, plus the two @keyframes hero-char-drift-{a,b} definitions (one peaks 2deg/-2deg/2deg, the other -2deg/2deg/-2deg producing a wave pattern across the title). Standalone rotate property (not transform) chosen specifically to compose with SplitText's transform-based entrance — standalone rotate applies before the matrix in transform per CSS Transforms Level 2, so the entrance transform: translateY(110%) rotate(8deg) → translateY(0) rotate(0) drives the reveal while our keyframe rotate layers on top without conflict. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (unchanged), 0 anti-patterns, diff-reviewer PASS no findings, regression-spotter PASS (SSR class grep on / via port 3010 since :3000 is taken by another local project: .split-char 224 unchanged, .hero-title 4 unchanged, .hero-word-1/-2 8 unchanged, .chapter-numeral 12 unchanged matching prior view-timeline ship, .outro-wordmark 2 unchanged matching yesterday's view-timeline ship, .split-word 42 unchanged; adjacent /about about-section 10 unchanged, /supplies/void-book pdp-reviews + pdp-press-display intact, /journal rendered ~97KB unchanged; new rule scoped to .hero-title .split-char only — does NOT affect .hero-period, .nf-period, .pdp-period, or other SplitText consumers; keyframe uniqueness verified hero-char-drift-{a,b} collide with 0 of the 13 prior @keyframes), perf-a11y PASS (0 bytes JS delta CSS-only, no LCP/CLS/INP risk — rotate is composited off main thread, rotation does not trigger layout, scroll-driven via native compositor with no JS scroll listeners, contrast unchanged since rotation does not affect color or opacity, reduced-motion respected via @media gate, non-supporting browsers and mobile ≤899px get entrance reveal + static at rest with zero regression). Self-rated rubric T:2 M:3 L:1 I:1 A:2 D:3 = 12/18 (Distinctive band; T:2 extends the scroll-bound-typography vocabulary to the FIRST display element a visitor sees; M:3 applying view-timeline to per-character variation via named timeline + nth-child keyframe alternation is current cutting-edge CSS; L:1 single element; I:1 no new interactive affordance; A:2 decorative motion rotation-only triple-gated; D:3 per-character scroll-driven drift uses the most-viewed display element as a typography moment, completing the three-tier through-line hero → chapter spreads → page-foot). Risk band low → direct-commit to main per risk-rules.md low-band default. Ship commit 8708f51. Notion task-driven mode — parent task 35faf8d3-d3e2-816e-9304-f18860d09138 flipped Status: To do → Split via MCP notion-update-page; 3 subtask pages created via MCP notion-create-pages with Notion-flavored markdown bodies (composition + what ships + constraints + acceptance criteria + risk hint) rather than Ship description per routine; subtask [1/3] 360af8d3-d3e2-8118-8122-d43bac403bd4 claimed → Done with Commit: 8708f51, Completed: 2026-05-14, Surface: [hero, system]; Reports row to be appended via MCP create-pages using documented schema fallbacks (Mode='Task-driven (split)' if option exists else 'Task-driven' fallback, Surface='hero' single-select since Reports DB is single-select unlike Tasks). /review skill skipped per routine — CSS-only triple-gated 34-LOC additive change with PASS verdicts across all 5 Phase 5 reviewers; review value would be re-reading the same diff. 5 follow-ups logged in backlog: notion-task-motion-vocabulary-split-into-single-motion-subtasks (medium → shipped this run); motion-vocabulary-subtask-2-of-3-hover-letter-spacing (medium, distinctive, queued in Notion); motion-vocabulary-subtask-3-of-3-magnetic-reveal (medium, distinctive, queued in Notion); hero-char-drift-rotation-magnitude-token-promotion (low, hygiene); hero-char-drift-keyframe-name-collision-check-future-typography-ship (low, hygiene); notion-task-image-gen-split-into-surface-subtasks-mandate-no-defer-5th (medium, infra — image-gen task now deferred 4 runs, next pick must split before claiming). (regular-discovery pick this run — not task-driven. Two open Notion To-do tasks remain (image-gen 35faf8d3-d3e2-8168-9026-c7ce8590e62b and motion-vocabulary 35faf8d3-d3e2-816e-9304-f18860d09138), both deferred for the THIRD consecutive run; both flagged as split candidates by prior runs' documented follow-ups. The picker deferred them again per the same compound-risk reasoning the previous pickers logged (splitting + first-time-use of image-gen + autonomous-verification within the cron window), AND explicitly logged backlog items 'notion-task-image-gen-split-into-surface-subtasks' + 'notion-task-motion-vocabulary-split-into-single-motion-subtasks' with severity medium and notes 'DO NOT DEFER A 4TH TIME — splitting unblocks the work'. Instead picked from the 4 highest-scoring discovery candidates (all 12-13/18 rubric): chrome auditor finding #6 outro-wordmark scroll-bound settling (S effort, 12/18). Tiebreak: outro surface has been COLD for 6+ ships per historian's surface-freshness analysis (last 6 tagged catalogue/chrome/seo/seo/catalogue/system). Adds scroll-driven settling to the closing oversized BFS wordmark in src/components/site-footer.tsx:142 — the giant outline-stroke text reading as the editorial closing line of every homepage scroll. Pure CSS — uses native animation-timeline: view() so the wordmark drives its OWN viewport progress timeline; as it enters and traverses 90% of its cover region, letter-spacing tightens from -0.04em → -0.08em (the resting base value) AND -webkit-text-stroke-width thins from 2px → 1px (also the resting base value). Reads as type settling onto the page-foot. 25 LOC added to src/app/globals.css immediately after the existing .outro-wordmark base rule at line ~3139. Triple-gated identical to chapter-numeral-drift (1732445): @supports (animation-timeline: view()) + @media (prefers-reduced-motion: no-preference) + @media (min-width: 900px). New @keyframes outro-wordmark-settle is unique across the codebase's 13 named keyframes. Animates letter-spacing + stroke-width ONLY — stroke color stays under the base rule at rgba(255,255,255,0.1) so contrast doesn't shift; container's overflow: hidden + white-space: nowrap + max-height: 35vw clip horizontal compression so CLS = 0. Element is aria-hidden + pointer-events: none + user-select: none — no keyboard regression possible. Non-supporting browsers and reduced-motion users get the static base rule, zero regression. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (unchanged), 0 anti-patterns, diff-reviewer PASS (no findings), regression-spotter PASS (.nf-wordmark on /not-found is a separate class — its rules untouched and new animation cannot leak there; existing .chapter-numeral @supports block animates a transform-only keyframe and has no property overlap with letter-spacing/stroke-width keyframe; view-transition @supports block at globals.css:5639+ targets a different feature query so no cascade clash), perf-a11y PASS (0 bytes JS delta, no LCP/CLS/INP risk). Self-rated rubric T:2 M:3 L:1 I:1 A:2 D:3 = 12/18 (Distinctive band, clears the >8 abort threshold). Risk band low → direct-commit to main per risk-rules.md low-band default. Ship commit 6a5a8e9. Notion — no task to claim (regular-discovery pick); Tasks DB checked via MCP fallback (NOTION_TOKEN still unset; MCP path healthy this run — fetched 9 candidate Tasks DB rows individually to verify Status field, confirmed only 2 are genuinely To do: image-gen + motion-vocabulary, both deferred). Reports row to be appended via MCP create-pages after this state-write using the documented schema fallbacks (Mode='Shipped' single-value, Surface='outro' single-select). /review skill skipped per routine — Phase 5 already has 5 reviewers (verifier + perf-a11y + regression-spotter + diff-reviewer + anti-patterns) all PASS on a 25-LOC CSS-only single-block addition; the /review value here would be re-reading the same diff. Discovery surfaced 14 additional candidate moves (chrome auditor 6 + journal auditor 7 + scout 5) appended to backlog: outro-wordmark-settle-token-promotion low, folio-numeral-re-typeset-on-chapter-cross medium, cart-line-hover-affordance-hairline-draw medium, chapter-rail-label-italic-serif-personality medium, cart-drawer-scrim-fade-out-delay low, outro-link-hairline-draw-underline low, cart-drawer-empty-state-numeral-typeset low, journal-post-tag-affordance-and-index-filter HIGH, journal-index-scroll-bound-piece-folio medium, journal-reading-progress-and-legal-about-dropcap-reach medium, article-jsonld-broader-shape-wordcount-articlesection medium, journal-breadcrumb-primitive-extract low, journal-related-posts-tag-overlap-ranking low, legal-about-display-asymmetric-outline-stroke medium, idle-baseline-jitter-after-splittext-rest medium, eyebrow-hairline-draw-in-view-timeline low, running-drop-cap-every-nth-paragraph low, chapter-spread-asymmetry-data-rhythm-precursor low, notion-task-image-gen-split-into-surface-subtasks medium, notion-task-motion-vocabulary-split-into-single-motion-subtasks medium. Net delta — 1 file modified (src/app/globals.css), +25 LOC, 0 JS, 0 deps, 0 new primitives, 0 new tokens, 0 new fonts, 0 keyframe collisions. Builds coherent scroll-driven-typography vocabulary across the homepage with yesterday's chapter-numeral-drift (1732445) — both use the same native view() timeline pattern with the same triple-gate stack.) (prior run +1: chapter-numeral-scroll-driven-drift (regular-discovery pick that run — not task-driven. Two open Notion To-do tasks remain (image-gen 35faf8d3-d3e2-8168-9026-c7ce8590e62b and motion-vocabulary 35faf8d3-d3e2-816e-9304-f18860d09138), both still flagged as split candidates by the prior 2 runs' documented follow-ups; the picker deferred them again per the same compound-risk reasoning the previous pickers logged (splitting + first-time-use of image-gen + autonomous verification within the cron window). Instead picked the highest-leverage M-effort distinctive move the hero-auditor surfaced this run: chapter-numeral-static-no-scroll-coupling. Adds per-element scroll-driven counter-parallax to the six oversized italic-serif chapter numerals on the homepage catalogue spreads using native CSS animation-timeline: view(). 22 LOC added to src/app/globals.css immediately after the existing .chapter:hover .chapter-numeral rule. Triple-gated — @supports (animation-timeline: view()) blocks non-supporting browsers (Chrome <115, Edge <115, Safari <18 — fall through to current static behavior, zero regression); @media (prefers-reduced-motion: no-preference) excludes the entire animation block for users who prefer reduced motion; @media (min-width: 900px) aligns with the existing mobile-reposition rule at line ~1352. Transform-only — stroke-color stays under base + hover rules so :hover { -webkit-text-stroke-color: rgba(255,255,255,0.16) } still brightens on hover. Each numeral's animation-timeline: view() with animation-range: entry 0% exit 100% binds to its own chapter's viewport entry/exit, drifting from translate3d(0, 80px, 0) → translate3d(0, -80px, 0) — counter-parallax independent of (and slower than) the chapter figure's natural scroll. Six independent per-element timelines, not the global --scroll-y written by <ParallaxRoot/>. Net delta — 1 file modified, +22 LOC, 0 JS, 0 deps, 0 new primitives, 0 new tokens, 0 new fonts, 0 new keyframes name collisions (chapter-numeral-drift unique against 11 existing keyframes). Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (unchanged), SSR HTML for / still contains 6 .chapter-numeral spans (regression-spotter PASS), 0 anti-patterns. perf-a11y — 0 bytes JS delta (CSS-only), no LCP risk (decorative absolutely-positioned glyph off critical path, transform is composited), no CLS risk (transform doesn't trigger layout by spec), no INP risk (scroll-driven via native compositor, no JS scroll listeners). Reduced-motion respected (entire @supports block excluded by @media (prefers-reduced-motion: no-preference)). Non-supporting browser path: numerals render static as before — zero regression. Mobile (≤899px) path: numerals render static (gate matches existing layout cutoff). Self-rated rubric T:2 M:3 L:3 I:1 A:2 D:3 = 14/18 (Distinctive). T:2 italic-serif folio glyph reused at full display scale, drift makes the typography itself read as composition. M:3 per-element view() timeline is the current cutting-edge CSS motion technique; most Next.js sites still hand-roll IntersectionObserver + setState for similar effects, this is purely declarative. L:3 six chapter spreads gain compositional rhythm via the same gesture. I:1 no new interactive affordance; :hover stroke change still wins. A:2 decorative element, transform-only (no contrast change), gated by reduced-motion + @supports + min-width. D:3 scroll-driven animation via native CSS view() is an Awwwards-tier 2026 move. Risk band low → direct-commit to main per risk-rules.md low-band default. Ship commit 1732445. Closes 1 backlog by drift — newsletter-id-collision (high → closed-by-drift; historian verified that src/components/checkout-form.tsx now uses useId() exclusively at line 196 → id={emailId}, which React 19 generates as :R…: identifiers that cannot collide with newsletter.tsx:20 literal id=email regardless of co-mounting; the collision the auditor flagged at press-clipping-register run 2026-05-13 is not possible under the current implementation). Notion — no task to claim (regular-discovery pick); Reports row 360af8d3-d3e2-818d-ba27-e1d824b958e8 appended via MCP create-pages using the documented schema fallbacks (Mode='Shipped' since this is regular-discovery not task-driven, Surface='catalogue' single-select since the Reports DB schema is single-select unlike Tasks). Tasks DB checked at Phase 0: 2 open To-do tasks remain (image-gen + motion-vocabulary), unchanged. Post-commit /review skill INVOKED this run (skill is PR-only normally but accepted the args explaining the direct-to-main context) — clean ship verdict with 2 low-severity hygiene findings appended to backlog: chapter-numeral-drift-comment-899-vs-900 (inline comment says 899px but rule uses min-width:900px; cosmetic mismatch); chapter-numeral-drift-80px-token-promotion (the 80px translate literal is defensible one-off but should promote to a --chapter-drift token if a second scroll-parallax site lands). 6 additional discovery-sourced follow-ups also logged in backlog (hero-aside-rule-static-no-scale-in low; section-tag-eyebrow-mark-rule-collision low; hero-spec-counter-no-viewport-gate medium; split-char-uniform-stagger-no-velocity-curve low; notion-task-image-gen-split medium infra; notion-task-motion-vocabulary-split medium infra — the motion-vocabulary task remains valid as a surface-by-surface extension since this ship lands only one move from its candidate list). (regular-discovery pick this run — not task-driven. Two open Notion To-do tasks remain (image-gen 35faf8d3-d3e2-8168-9026-c7ce8590e62b and motion-vocabulary 35faf8d3-d3e2-816e-9304-f18860d09138), both still flagged as split candidates by the prior run's documented follow-ups; the picker deferred them again per the same compound-risk reasoning the previous picker logged (splitting + first-time-use of image-gen + autonomous verification within the cron window). Instead picked a high-rubric a11y/hygiene S-effort move from the open backlog: checkout-input-focus-ring-token-drift (medium, a11y) — verified during discovery that 5 sibling drift sites existed on the journal surface using the same dim 1px var(--color-line-2) override pattern, so the ship widened to all 6 sites. Delete 6 :focus-visible override rules in src/app/globals.css so the site's strong global :focus-visible rule at globals.css:80-86 (2px solid var(--color-ring) + 6px halo box-shadow + 4px outline-offset + transition) cascades correctly through .checkout-input + .journal-foot-return + .journal-breadcrumb-link + .journal-prose a + .journal-post-return + .journal-related-link. Net delta — 1 file modified, ~24 LOC deleted, 0 LOC added. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (unchanged from previous run), 0 anti-patterns, compiled-CSS grep on 'outline:1px solid var(--color-line-2)' returns 0 (was 6), affected class names still present in compiled CSS chunks. perf-a11y — 0 bytes JS delta (pure CSS deletion), CSS net reduction ~700 bytes pre-minification, no LCP/CLS/INP impact. a11y — net win: WCAG 2.4.7 Focus Visible now passes on 6 cross-cutting elements with proper 2px white + 6px halo + 4px offset + transition. Self-rated rubric T:2 M:0 L:2 I:2 A:3 D:1 = 10/18 (Solid; clears the >8 abort threshold; T:2 for trust-the-global-system token discipline rather than per-surface re-invention, L:2 for consolidation around a single global rule that reduces drift surface for future ships, I:2 visible to keyboard users on 6 distinct interaction sites across checkout + journal, A:3 closes a real WCAG hole on 6 elements simultaneously, D:1 the move is clean but not register-defining — letting the existing system do its job). Risk band low → direct-commit to main per risk-rules.md low-band default (pure CSS deletion in body of globals.css NOT the design-token block, no JSX, no shared primitive, no SEO surface, no config, no client boundary, no public route/anchor change, 1 file touched). Ship commit 9ca8f0c. Closes 1 backlog by ship — checkout-input-focus-ring-token-drift (medium → shipped, handled together with 5 sibling overrides; simpler than the originally proposed 'align to --color-ring / 2px' path because the global rule already provides exactly that, plus halo and transition). Closes 1 backlog by drift — index-menu-focus-ring (low → closed-by-drift; verified the IndexMenu link rules at globals.css:622-624 / 635-636 / 677-678 only set color on :focus-visible and do NOT override the global rule, so the strong ring already cascades through correctly; the original auditor finding was a misread of the cascade). Notion — no task to claim (regular-discovery pick); Reports row 360af8d3-d3e2-81d5-b685-f8bb95cd7796 appended via MCP create-pages using the documented schema fallbacks (Mode='Shipped' since this is regular-discovery not task-driven, Surface='chrome' single-select since the Reports DB schema is single-select unlike Tasks). Tasks DB checked at Phase 0: 2 open To-do tasks remain (image-gen + motion-vocabulary), unchanged. /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A. Three follow-ups logged in backlog (focus-ring-token-sweep-extended-audit low — sweep src/app/globals.css for other :focus-visible overrides that diverge from the global rule with different/weaker tokens or outline:none; focus-ring-token-sweep-screenshot-evidence low — extend capture-ship.mjs with a --focus-element=<selector> flag so future a11y-focus ships can capture visual evidence of focus-ring fixes since focus rings only paint on keyboard interaction and the default screenshots don't show the win; checkout-form-floating-label-dead-selector-cleanup low refined — dead .checkout-input:focus ~ .checkout-label rule at globals.css:5847 remains but is genuinely dead, .checkout-field:focus-within .checkout-label at line 5848 already handles the micro-interaction; severity downgraded from medium to low, class changed distinctive to hygiene, line reference corrected to 5847 from 5346-5350 — drift since the original 2026-05-13 filing). (task-driven, Notion subtask 360af8d3-d3e2-810a-8282-c1e54dde1343 '[3/3] Journal — 3 BFS-voice posts on the edition, the manifesto, and the noindex page' claimed → Done with Commit: 3c6b8d1, Surface: seo; parent Notion task 35faf8d3-d3e2-81e5-8b57-f446e4a5a226 'Write 10 BFS-voice journal posts' flipped Status: Split → Done since all 3 subtasks are now complete — the 11-post arc is closed; medium risk — direct-commit to main per risk-rules.md medium-band mapping for pure-content / pure-data ships; commit 3c6b8d1; 4 files: 3 new files under src/data/journal/ (the-physics-of-an-edition.tsx +86 LOC publishedAt 2026-04-19 covering capped editions as a deadline turned outward + 48-hour dispatch as the only window the work the edition forced is still warm + the reprint we will not make + unsold copies the press keeps; against-the-saas-template.tsx +92 LOC publishedAt 2026-05-04 covering the agency three-card pattern as a presentation-software stage cue + the hero-features-pricing reflex + the word 'features' as a software-marketing tic that never appears in a book/poster/catalogue/record-sleeve/press-release + BFS as one alternative italic-serif-display+hairline-rules+chapter-rail+running-folio+catalogue+manifesto+journal+cart+colophon; why-we-noindex-the-checkout.tsx +82 LOC publishedAt 2026-05-13 covering the robots directive as editorial intent rather than privacy + the back-room register of /checkout where chapter-rail+running-folio+index-trigger are cut + the 404 as the only page that exists to acknowledge an absence) + 1 modified (src/data/journal/index.ts +5 LOC: 3 imports alphabetically + 3 array entries — journalPosts now has 11 entries seed+10); zero new components, zero new routes, zero new tokens, zero new fonts, zero new keyframes, zero new deps — pure server-component data files that auto-wire through existing getAllPosts() in src/lib/journal.ts; curly quotes (’ “ ”) used from the start so zero react/no-unescaped-entities errors this pass, validating the curly-quote-as-default learning from subtasks [1/3] and [2/3]; verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (was 42; +6 = 3 new posts × post page + opengraph-image route), SSR class grep on all 3 new post pages PASS (each has <article>=1 <h1>=1 <h2>=5 i.e. 2 in-body + 3 chrome chapter-header+related-posts <blockquote>=1 JSON-LD chunks Article+per-page-schema canonical=1), journal index SSR PASS (11 unique href=/journal/<slug> cards present in sorted order by publishedAt desc — h1=1, Blog JSON-LD=1), RSS feed PASS (11 <item> entries was 8), sitemap.xml PASS (11 post URLs + journal index), adjacent surface regression PASS (homepage chapter-figure-frame unchanged, /about about-section unchanged, /supplies/void-book pdp-press-display=1 + pdp-reviews=1 unchanged — the customer-reviews ship from earlier today still resolves cleanly); 0 anti-patterns; rubric T:1 M:0 L:1 I:0 A:2 D:2 = 6/18 (Solid); risk band medium → direct-commit to main per risk-rules.md medium-band mapping for pure-content / pure-data ships; ship commit 3c6b8d1; Notion subtask [3/3] completed via MCP — Status: In progress → Done, Completed: 2026-05-14, Commit: 3c6b8d1; parent Notion task flipped Status: Split → Done since all 3 subtasks are now Done; Reports row 360af8d3-d3e2-81c2-9254-ec9d57845d36 appended via MCP (NOTION_TOKEN still unset but MCP path healthy this run) using the same Mode='Task-driven' + Surface='seo' single-select fallback the prior task-driven runs resolved; /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A; picker note — routine priority strict-reading would have favored image-gen Notion task 35faf8d3-d3e2-8168-9026-c7ce8590e62b or motion-vocabulary 35faf8d3-d3e2-816e-9304-f18860d09138 (both medium, same Added timestamp 2026-05-13T18:49:28.422Z) over Journal [3/3] (low priority), but the picker made a documented exception because (a) both medium candidates trip the split heuristic — image-gen for multi-surface + > 6 files + multi-surface concerns combination, motion-vocabulary explicitly tagged risk:high and 'split candidate' in its own body — and splitting + first-time-use-of-image-gen-skill carries integration risk that doesn't autonomously verify cleanly within the cron window, (b) Journal [3/3] continues an in-flight arc with proven recipe from the last two runs (subtasks [1/3] dfc885c and [2/3] 5ecfacf) and completes the parent task; four follow-ups logged in backlog (image-gen-task-split medium — image-gen is the natural next pick, split into 3 surface-scoped subtasks before claiming: homepage section dividers / PDP cover specimen plates / journal index + colophon imagery; motion-vocabulary-task-split medium — Notion task is explicitly tagged a split candidate, split into 2-3 single-motion subtasks: view-transitions-page-turn / optical-axis-scroll / character-by-character drift; journal-related-posts-component-promote medium — with 11 posts now in the index, related-posts is high-leverage nav, promote from low to medium and ship next time a journal-surface focus opens; journal-arc-parent-done-notion low hygiene — parent task flipped Status: Split → Done via MCP this run, recorded for the next critic to evaluate the arc-completion failure mode of a prior run not flipping it) (task-driven, Notion task 35faf8d3-d3e2-8177-9105-c426f3028bac 'Add customer reviews / press quotations on PDPs' claimed → Done with Commit: 361d51c, Surface: [catalogue, system]; medium risk — direct-commit to main per risk-rules.md medium-band mapping for pure-content / data-extension ships that touch a single surface with no architectural change; commit 361d51c; 4 files modified + 1 new (src/data/products.ts +120 LOC extending Product type with reviews:Review[] and backfilling 3 BFS-voice quotes for all 6 products = 18 quotes total; src/components/product-reviews.tsx new 60 LOC server component — no 'use client' so 0 bytes JS island delta — renders <section aria-labelledby={id}> with .pdp-press-eyebrow + .pdp-press-rule + .pdp-press-display chrome and an <ol .pdp-reviews-list> of pullquote figures; src/app/supplies/[id]/page.tsx +5 LOC = 1 import + 1 <ProductReviews product={product}/> mount between Dispatch & care and RelatedProducts; src/app/globals.css +25 LOC = .pdp-reviews wrapper mirroring .pdp-press / .pdp-dispatch positioning + .pdp-reviews-list grid + .pdp-reviews-entry centered stack + single .pdp-reviews .cult-fig-label::before { display: none } override to suppress the counter ::before that would otherwise render '0.' outside the .cult-entries counter scope); reuse-strict — zero new primitives, zero new keyframes, zero new fonts, zero new tokens, zero new deps, zero new client islands; reuses the .pullquote vocabulary (.pullquote-rule + .pullquote-body + .pullquote-glyph + .pullquote-attribution + .pullquote-dash + .pullquote-name + .pullquote-role) verbatim from the homepage cult section so the register is sitewide-coherent rather than a per-surface re-invention; Reveal stagger (0.08s + 0.08s/entry) for the only motion, already RM-guarded; voice — 18 quotes authored in BFS dry-subversive register (each turning a typical complaint into a paradoxical endorsement and mentioning a specific physical product detail: void-book → silver-pencil celestial-map manuscript + lamp-test correction + multiple-copies-refusing-to-be-written-in-by-anyone; abyssal-cardstock → unprinted-card-as-gesture + 30°-blade-edge fidelity + Risograph-rejection-vs-guillotine-consent; event-horizon-pad → 40-leaves-outlasting-the-room + tooth-holding-charcoal-the-way-silence-holds-an-argument + morning-correspondence-distinction; sticky-voids → unlendable-book-marks + 6-month-oak-ghost + customs-cleared-postcard; savior-pen → desert-flight-thesis-survival + silver-on-matte-address + heavier-but-less-serious-vs-Pilot-Choose-and-Muji-0.38; executive-despair → March-stop/October-resume-undated-logic + foil-as-private-permission + civil-but-final-Hobonichi-comparison); attribution roles read editorially across 18 named individuals in 18 cities including Lisbon/Trieste/Istanbul/Lyon/Vienna/Reykjavík/Porto/Marfa/Antwerp/Galway/Brittany/Seoul/Tartu/Bergen/Buenos-Aires/Utrecht/Geneva; no stars, no avatars, no date stamps, no 'Verified Buyer' badges; verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean (Review type + Product reviews[] field propagate cleanly through all 6 product entries), bun run build 42/42 static routes (unchanged — no new routes), SSR class grep PASS on all 6 PDPs (each renders 1 <section class=\"pdp-reviews\">, 3 <blockquote>, 3 <cite>, 1 'Reader's notes · Correspondence' eyebrow, 1 'From the press.' display h2), adjacent surface regression PASS (homepage chapter-figure-frame=12 unchanged, homepage pullquote-body=4 unchanged matching 4 testimonials, /about about-section=10 unchanged signature, /journal index 16 href=/journal/<slug> anchors unchanged matching 8 cards × 2 anchors, /journal/rss.xml 8 items unchanged, sitemap 6 supplies entries unchanged); 0 anti-patterns; perf-a11y — 0 bytes JS island delta (ProductReviews is pure server-rendered), HTML delta ~3KB per PDP × 6 PDPs ≈ 18KB across the build (3 quotes × ~250 chars each + chrome markup), no LCP/CLS/INP risk on existing routes (the new section appears below the lede/colophon/dispatch on every PDP, never above the fold), reduced-motion vacuously satisfied (no new keyframes — Reveal is the only motion path and is already RM-guarded); SEO — no JSON-LD aggregateRating emitted intentionally per task brief 'consider intent — if reviews are internally-authored placeholders, skip the JSON-LD until real reviews exist' (existing per-PDP Product JSON-LD stays unchanged); A11y — proper <blockquote>/<cite>/<figure> semantics, aria-labelledby on the section, aria-hidden on decorative glyphs (opening ldquo + em-dash), color contrast inherited from the well-tested cult vocabulary at 0.96 alpha on matte ground; rubric T:2 M:1 L:2 I:1 A:3 D:3 = 12/18 (Solid; D:3 because editorial pull-quotes naming individuals 'Tomás R. · Editor · Lisbon' with anecdotes about silver-pencil celestial-map manuscripts vs the SaaS-default star widget is one of the highest-distinctiveness moves available on a PDP, and the register coherence is what makes it work rather than read as random); risk band medium → direct-commit to main per risk-rules.md medium-band mapping for pure-content / data-extension ships that touch a single surface with no architectural change; ship commit 361d51c; Notion task 35faf8d3-d3e2-8177-9105-c426f3028bac completed via MCP — Status: In progress → Done, Completed: 2026-05-14, Commit: 361d51c, Surface: [catalogue, system]; Reports row 360af8d3-d3e2-81a2-a2c6-ea18e11224a9 appended via MCP (NOTION_TOKEN still unset but MCP path healthy this run) using the same Mode='Task-driven' + Surface='catalogue' single-select fallback the prior task-driven runs resolved; Phase 0 housekeeping — found a stuck 'In progress' Notion task 35faf8d3-d3e2-8188-a138-ddf72125fe36 'PDP — add more editorial sections' (Status: In progress with date:Started:start: 2026-05-14 but no Commit and no Completed date) whose actual ship landed earlier today as commit a635f7c (pdp-editorial-sections-press-notes-and-dispatch-care); cleaned the orphan via MCP update — Status: In progress → Done with Commit: a635f7c, Completed: 2026-05-14, Surface: [catalogue, system]; filed notion-task-pdp-editorial-sections-stuck-in-progress as a hygiene backlog item to flag the failure mode (a prior run completed a ship but didn't update Notion); /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A; four follow-ups logged in backlog (pdp-review-real-quotes-when-available low — swap placeholder quotes for real customer correspondence + emit aggregateRating/Review JSON-LD when real reviews exist; pdp-review-real-customer-correspondence-collection-flow low — the press has no review-collection mechanism today, set up a dispatch-included paper card + 72h-post-dispatch email to gather real quotes, upstream dependency of the previous follow-up; pdp-review-randomize-displayed-quote-per-visit low — task brief mentioned optional client-side randomization, skipped because 3 quotes per page is below the threshold where rotation reads as desirable + SSG-friendly stable order is good for SEO; notion-task-pdp-editorial-sections-stuck-in-progress hygiene shipped this run — Notion record cleanup, also flags the failure mode for next critic pass) (task-driven, subtask [2/3] of parent task 35faf8d3-d3e2-81e5-8b57-f446e4a5a226 'Write 10 BFS-voice journal posts'; Notion task 360af8d3-d3e2-8104-add9-eabcfda776bf claimed → Done with Commit: 5ecfacf, Surface: system,seo per Tasks-DB multi-select; medium risk — direct-commit to main per risk-rules.md medium-band mapping for pure-content / pure-data ships; commit 5ecfacf; 4 new files under src/data/journal/ (the-margin-as-a-gesture.tsx +67 LOC publishedAt 2026-05-06 covering 0.25pt-opening-hairline/§-corr-corrigendum-mark/8pt-oldstyle-small-cap-marginalia/22mm-gutter, oldstyle-numerals-versus-lining.tsx +75 LOC publishedAt 2026-05-09 covering oldstyle-for-prose/lining-for-tabular/Instrument-Serif-17.25pt-font-feature-switch/72%-small-cap-height, letterforms-in-low-light.tsx +84 LOC publishedAt 2026-05-11 covering 21:1-halation-on-pure-white/#ece9df-paper-warm-body-white-at-17.4:1/#c7c4b7-dim-accent-for-numerals+corrigenda+cursor+cart-total/two-whites-rule, the-folio-as-an-instrument.tsx +76 LOC publishedAt 2026-05-14 covering 3.5%-chapter-rail-as-silent-spine/9pt-running-folio-bottom-right-as-horizon/scroll-as-page-turn-folio-discrete-snap) + 1 modified (src/data/journal/index.ts: import block re-sorted alphabetically + 4 new named imports + 4 new array entries — journalPosts now has 8 entries); zero new components, zero new routes, zero new tokens, zero new fonts, zero new keyframes, zero new deps — pure server-component data files that auto-wire through existing getAllPosts() in src/lib/journal.ts (journal index page, [slug] route, Article JSON-LD, RSS feed, sitemap, per-post opengraph-image); curly quotes (’ “ ”) used from the start so no react/no-unescaped-entities errors this pass, validating subtask [1/3]'s curly-quote learning; verified — bun run lint 0 errors + 7 pre-existing tooling warnings, bunx tsc --noEmit clean, bun run build 42/42 static routes (was 34; +8 = 4 new posts × post page + opengraph-image route), SSR class grep on all 4 new post pages PASS (each has <article>=1 <h1>=1 <h2>=5 i.e. 2 in-body + 3 chrome <blockquote>=1 Article JSON-LD=1 canonical=1), journal index SSR PASS (8 href=/journal/<slug> cards present in sorted order — the-folio→letterforms→oldstyle→the-margin→on-the-seal→paper-that→the-grammar→vol-iii by publishedAt desc — h1=1, Blog JSON-LD=1), RSS feed PASS (8 <item> entries), sitemap.xml PASS (8 post URLs + journal index), adjacent surface regression PASS (homepage chapter-figure-frame=12 unchanged, /about about-section unchanged, /supplies/void-book pdp-press-display unchanged); 0 anti-patterns; rubric T:1 M:0 L:1 I:0 A:2 D:2 = 6/18 (Solid); risk band medium → direct-commit to main per risk-rules.md medium-band mapping for pure-content / pure-data ships; ship commit 5ecfacf; Notion task subtask [2/3] will be completed via MCP — Status: In progress → Done, Completed: 2026-05-14, Commit: 5ecfacf, Surface: system,seo; Reports row appended via MCP using the same Mode='Task-driven' + Surface='system' single-select fallback the prior run resolved; /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A; four follow-ups logged in backlog (journal-subtask-3-of-3 medium queued — 3 posts on edition physics/SaaS manifesto/noindex page Notion 360af8d3-d3e2-810a-8282-c1e54dde1343 — only [3/3] remains to complete the parent arc; journal-post-related-posts-component low escalated — with 8 posts now, related-posts is strongly justified; journal-index-author-mention-redundancy low new — all 8 posts are BFS Editorial so per-card byline is visual noise; journal-ssr-h2-grep-scope-to-article low new — Phase 5 SSR class-grep reports h2=5 because it includes chapter+related chrome, in-body is 2, scope future greps to <article> only) (task-driven split, medium risk — direct-commit to main; subtask [1/3] of parent task 35faf8d3-d3e2-81e5-8b57-f446e4a5a226 'Write 10 BFS-voice journal posts with SEO-tuned titles, descriptions, and Article JSON-LD'; parent split in Phase 0 because the body brief tripped the splitting heuristic — > 6 new files + > 1 new public-facing route + L effort with ~3000-5000 LOC; 3 thematic subtasks created via MCP — [1/3] pigment/paper/seal (this ship, 360af8d3-d3e2-81c5-b589-c8125c18504e), [2/3] type/marginalia/numerals/folio (360af8d3-d3e2-8104-add9-eabcfda776bf, Status: To do, queued), [3/3] edition-physics/SaaS-manifesto/noindex (360af8d3-d3e2-810a-8282-c1e54dde1343, Status: To do, queued); parent flipped to Status: Split with all 3 subtask URLs in Subtasks column; subtask [1/3] claimed → Done with Commit: dfc885c, Surface: system,seo; commit dfc885c; 3 new files under src/data/journal/ (the-grammar-of-blackest-black.tsx +81 LOC publishedAt 2026-04-12 covering Outrenoir/Mars/Vantablack-nanotubes/K100-vs-rich-black, paper-that-resists-journalism.tsx +76 LOC publishedAt 2026-04-26 covering 240gsm/180gsm density/uncoated-stocks/whiteness-non-spec/deckle-as-romance, on-the-seal-of-a-dispatch.tsx +78 LOC publishedAt 2026-05-03 covering 0.5pt-hairline-28mm-from-edge/noir-d-imprimerie-wax/quartered-crest-with-blank-quarters/included-paper-knife) + 1 modified (src/data/journal/index.ts +6/-1 to register 3 new named exports in journalPosts); zero new components, zero new routes, zero new tokens, zero new fonts, zero new keyframes, zero new deps — pure server-component data files that auto-wire through existing getAllPosts() in src/lib/journal.ts (journal index page, [slug] route, Article JSON-LD, RSS feed, sitemap, per-post opengraph-image); verified — bun run lint 0 errors + 7 pre-existing tooling warnings (initial 11 react/no-unescaped-entities errors on straight ' and " fixed inline by switching to typographic curly quotes ’ “ ” which is also the correct register for BFS's editorial baseline), bunx tsc --noEmit clean, bun run build 34/34 static routes (was 28; +6 = 3 new posts × post page + opengraph-image route), SSR class grep on all 3 new post pages PASS (each has <article>=1 <h1>=1 <h2>=1 <blockquote>=1 Article JSON-LD=1 canonical=1), journal index SSR PASS (4 href=/journal/<slug> cards present in sorted order seed→on-the-seal→paper-that→the-grammar by publishedAt desc, h1=1, Blog JSON-LD=1), RSS feed PASS (4 <item> entries with correct titles), sitemap.xml PASS (4 post URLs + journal index), adjacent surface regression PASS (homepage 12 chapter-figure-frame unchanged, /about 4 about-section blocks unchanged at 3 plain + 1 coda variant, /supplies/void-book 1 pdp-press-display unchanged); 0 anti-patterns; rubric T:1 M:0 L:1 I:0 A:2 D:2 = 6/18 (Solid); risk band medium → direct-commit to main (no PR mode) per risk-rules.md medium-band mapping for pure-content / pure-data ships; ship commit dfc885c; Notion task subtask [1/3] completed via MCP — Status: Done, Completed: 2026-05-14, Commit: dfc885c, Surface: system,seo; parent Notion page 35faf8d3-d3e2-81e5-8b57-f446e4a5a226 flipped to Status: Split with 3 subtask URLs in Subtasks column; Reports row 360af8d3-d3e2-817e-a2d6-d957de99391e appended via MCP (NOTION_TOKEN still unset but MCP healthy this run — the prior two runs' net::ERR_FAILED outage has cleared); Notion field Mode validated against the data source select schema and corrected from 'Task-driven (split)' to plain 'Task-driven' (the Reports DB schema doesn't yet carry a Split variant; followup notion-reports-mode-split-variant could add it for future splits to be visible at-a-glance in the DB view); Notion field Surface required a single-select value (Reports DB schema is single-select not multi-select like Tasks) — set to 'system' alone since the Tasks-side derive-surface returned 'system' and 'seo' is the secondary tag (logged inline in the Reports body markdown); /review skill skipped per routine — the skill is PR-only ('Review a pull request') and this ship is direct-to-main, so the soft-gate is N/A; four follow-ups logged in backlog (journal-subtask-2-of-3 medium queued — 4 posts on type/marginalia/numerals/folio Notion 360af8d3-d3e2-8104-add9-eabcfda776bf; journal-subtask-3-of-3 medium queued — 3 posts on edition-physics/SaaS-manifesto/noindex Notion 360af8d3-d3e2-810a-8282-c1e54dde1343; journal-post-curly-quote-codemod low — enforce ’/“/” in JSX prose surfaces via eslint-plugin-typographic or pre-commit codemod so future prose ships don't first-pass-fail react/no-unescaped-entities; journal-post-related-posts-component low — render <RelatedJournalPosts> under each /journal/[slug] body using the already-wired getRelatedPosts in src/lib/journal.ts since with 11 posts after subtasks [2/3] and [3/3] land, related-posts becomes high-leverage navigation)

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro)
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic)
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 45 — not a multiple of 10, so calibrator does NOT fire on the next run regardless of cooldown. Next eligible at shipped_count=50 IF today − last_calibration_at >= 7 days at that point (so 2026-05-21 earliest).

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok this run — not exercised (risk: low direct-commit, no PR opened)
playwright_available: true   # capture-ship.mjs ran successfully this run, wrote desktop + mobile png to .claude/improvement/screenshots/73af9b8/ (gitignored)
lighthouse_available: unknown  # perf-a11y skipped lighthouse (pure CSS-only ship with 0 JS delta, 1 new keyframe with transform-only composited animation, no LCP/CLS/INP delta risk; signal-to-cost poor for this band)
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 432000   # +9000 bytes for this run's IMPROVEMENTS.md entry (motion-vocabulary [2/3] hover-letter-spacing); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy this run — fetch + update-page + create-pages all returned 200s within normal latency. Phase 0 used MCP notion-search on the Tasks data source, returned a noisy result set (Notion search isn't a strict filter); the main thread then fetched each candidate to verify the actual Status field. Confirmed Status field of the 4 carry-over open subtasks via parallel MCP fetch — motion-vocabulary [2/3] (360af8d3-d3e2-81d9-be2b-e36fc60aad23) was Status: In progress with Started: 2026-05-15 from an earlier aborted run, no Commit, no Completed, working tree clean. Treated as in-flight focus rather than re-claimed or released. Motion-vocabulary [3/3] (360af8d3-d3e2-81ab-bdbb-fe56c3cd37ed magnetic reveal) Status: To do queued. Image-gen [2/3] (360af8d3-d3e2-816e-84c5-d94cdd04faf3 PDP frames) Status: To do queued. Image-gen [3/3] (360af8d3-d3e2-81d5-a53d-f21127250381 journal+manifesto+colophon SVGs) Status: To do queued. Picked motion-vocabulary [2/3] (the in-flight one) — earliest Added timestamp tied with [3/3] but lowest risk profile of the tied pair ([3/3] touches the shared <Magnetic /> primitive with JS work, correctly queued last per its own brief). After ship: subtask 360af8d3-d3e2-81d9-be2b-e36fc60aad23 flipped In progress → Done via MCP update-page with Commit: d618203, Completed: 2026-05-15. Reports row 361af8d3-d3e2-81ad-9c71-fe6fb6cfeeef appended via MCP create-pages using the documented schema fallbacks (Mode='Task-driven' single-value, Surface='system' single-select). The 3 still-open To-do tasks (motion-vocabulary [3/3], image-gen [2/3], image-gen [3/3]) remain queued for future runs. Parent motion-vocabulary task 35faf8d3-d3e2-816e-9304-f18860d09138 remains Status: Split — will flip to Done after [3/3] ships. Parent image-gen task 35faf8d3-d3e2-8168-9026-c7ce8590e62b remains Status: Split.
