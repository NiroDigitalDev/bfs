# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T11:23:00Z
last_run_commit: 8771e67
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 55   # +1: cart-drawer-cta-editorial-register (auditor-pick from catalogue/PDP pass — Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows; no task-driven override). Picked at T3 M1 L2 I2 A3 D2 = 13/18 over reference-scout's Floema folio-ghost @ 11, scroll-progress-route-aware @ 11, cursor-mode-vocabulary @ 11, section-title-italic-serif-variant @ 11 (last lost on surface-freshness — manifesto/hero red-hot). Cart was coldest surface tied with chrome at 6 ships ago. Recast the cart-drawer foot CTA from a generic dark uppercase pill (`Proceed to checkout ↗`, border-radius 999px, font-weight 700, text-transform uppercase, letter-spacing 0.16em) to a 2-row left-aligned editorial composition that mirrors the drawer head's vocabulary: row 1 carries `.cart-drawer-cta-eyebrow` with a 5×5 hairline mark + ord `006` + `·` + sub `Bind` (the head is `005 · Manifest`, the foot is `006 · Bind` — manifest is the held selection, binding is the act of committing it); row 2 carries `<em>Proceed</em>` at var(--font-serif) italic clamp(24px,3vw,30px) alongside `.cart-drawer-cta-lede-tail` caps-sans `to checkout` 11px 0.3em; right side carries a 24×24 ↗ glyph that translates +4/-4 on hover. Border-radius → 2px (editorial rectangle). Existing fill-from-bottom `::before` translateY(101%) → translateY(0) hover preserved with color inversion to #000 on the white fill; hover now also propagates to eyebrow-mark (fill+border #000), eyebrow-ord/sub (rgba(0,0,0,0.72)), eyebrow-sep (rgba(0,0,0,0.35)), lede-tail (rgba(0,0,0,0.65)) so the entire composition reads against the inverted ground. Cleanup folded in: removed dead `[data-confirming]`, `.cart-drawer-cta-default`, `.cart-drawer-cta-confirm`, `.cart-drawer-cta-glyph-arrow`, `.cart-drawer-cta-glyph-check` selectors (~30 LOC of CSS) — confirmed via grep that `data-confirming` is set nowhere in src/; orphan from earlier iteration. Reduced-motion guard at globals.css:~4106-4116 updated to drop the four removed selectors and add the six new transitioning ones (-eyebrow, -eyebrow-mark, -eyebrow-ord, -eyebrow-sub, -lede-tail, -glyph). Two files: src/components/cart-drawer.tsx (CTA JSX block at lines 310-330 — single replacement), src/app/globals.css (.cart-drawer-cta block at ~3880-4005 — full rewrite, net +41 lines; reduced-motion guard ~4106-4116). No new tokens, no new fonts (Instrument Serif + Inter already loaded via layout.tsx), no new keyframes, no new JS listeners, zero-overhead change on the JS side. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 routes (Turbopack 1134.2ms + 541.9ms), 0 anti-patterns. SSR class-grep on /: cart-drawer-cta JS 45× / CSS 35×, -body 2/1, -eyebrow 18/13, -eyebrow-mark/-ord/-sep/-sub 4/4/3/4 (CSS 3/3/2/3), -lede/-lede-tail 7/4 (CSS 5/3), -glyph 4/3; the removed -glyph-arrow / -glyph-check are 0/0 in both bundles (correctly excised). Cart-drawer mounts in layout via cart-island but the panel's filled-cart branch (where the CTA lives) is gated behind `lines.length > 0`, so SSR HTML on / and PDPs shows the drawer head (`005 · Manifest` intact) but the foot lives only in the JS bundle until a user opens the drawer with items — matches prior cart-drawer ship pattern. Adjacent surface chrome intact: cart-drawer-eyebrow (head) 5×, cart-drawer-title 3×, cart-empty-cta (empty-state, different class) 1×, nav-cta 4× (cart-island button unaffected), pdp-add-to-cart 0 in SSR (mounts on interaction); hero/chapter-rail/folio/spec-plate (×270 on /)/outro-wordmark/outro-disclaimer all preserved at expected counts. perf-a11y PASS-WITH-FOLLOWUPS — CSS-only delta +97/-56 lines (net +41 / ~1.0-1.3KB gzipped on the page chunk), 0 bytes JS delta, no new fonts, no new deps, no LCP risk (drawer is position:fixed off-canvas at rest, panel translateX on open), no CLS risk (clamp() type only, flex-shrink:0 on glyph, no async content swap), no INP risk (no new JS listeners; hover transitions are GPU-cheap transform/color/background), keyboard parity preserved (Link is lone tab stop, aria-hidden correctly scoped to decorative eyebrow + glyph spans, accessible name `Proceed to checkout` computed from row 2 plain-text descendants), reduced-motion override complete (all 6 new transitioning selectors gated; hover end-states render instantly under `reduce` which is correct semantics — no animation, but state still reflects hover), contrast verified — eyebrow `--color-fog` on `--color-ink` ≈ 4.74:1 (AA borderline at 10px caps-sans 0.3em tracking, passes because uppercase letterforms read at this contrast — flagged as low-severity follow-up cart-drawer-cta-eyebrow-10px-contrast-policy), eyebrow-ord #fff on #0a0a0a ≈ 20:1, eyebrow-sub/lede-tail `--color-text-muted` blended ≈ 9.4:1, hover state rgba(0,0,0,0.72|0.65) on white ≈ 9.0:1 / 7.0:1 (all AA-strong), decorative `·` separator at 1.27:1 at rest and 2.5:1 on hover is aria-hidden so contrast is informational. Focus-visible follow-up from perf-a11y mitigated by the global :focus-visible rule at globals.css:80-86 covering all elements site-wide via --color-ring + 4px offset + 6px halo. regression-spotter PASS — drawer head (eyebrow 5, title 3), drawer fineprint/totals (filled-branch only, JS chunk 2/9, not SSR), cart-empty-cta (1, empty-state unaffected), nav-cta (4, unaffected), pdp-add-to-cart (separate component, unaffected); the four removed -glyph-arrow/-glyph-check classes ship as 0/0 in both JS and CSS bundles (correctly excised); SVG well-formedness 100% in cart-drawer client chunk (the new ↗ glyph is a Unicode arrow inside aria-hidden span, no SVG element in the changed region). diff-reviewer PASS-WITH-NITS — net +41 LOC proportionate for a 2-row composition swap; 0 escape hatches (!important / as any / @ts-ignore / console.* all absent); 12 new selectors all verified used in JSX; eyebrow vocabulary deliberately mirrors `.cart-drawer-eyebrow-*` from head (vocabulary parity, not duplication); 2 low-severity nits both on hardcoded color literals — `#000` for mark fill/border (should route through `var(--color-text-strong)` / `var(--color-ink)` token; defer to existing spec-plate-ink-token-promotion sweep follow-up) and `rgba(0,0,0,0.72|0.65|0.35)` for hover ord/sub/sep/tail (deliberate local convention matching established .spec-plate-* block convention, also defers to the token-promotion sweep). Risk band: medium per risk-rules.md — shared primitive (CartDrawer mounts in layout via cart-island, visible on every route) in conversion path to /checkout. Direct-commit to main per medium-band default. Screenshots skipped — stale next-server PID 46706 still holds port 3000 (carry-over stale-next-server-on-3000-blocks-capture-ship, now 6 ships old). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per routine — focused single-surface ship with PASS verdicts across all 5 Phase 5 gates, perf-a11y follow-up (focus-visible coverage) verified mitigated by global :focus-visible rule; review value would be re-reading the same diff. Notion writeback: no task to flip this run (no override); Reports row 361af8d3-d3e2-8130-9d16-ea15e0a38fb3 appended via MCP API-post-page with Surface=cart, Mode=Shipped, Rubric="T3 M1 L2 I2 A3 D2 = 13 / 18", Risk=Medium, Commit=8771e67 (first MCP post-page attempt hit 400 on incomplete property shapes; second attempt with parent.type:data_source_id same error; third attempt with plain-string property values returned 400 'Couldn't find editable properties for date:Date:start'; fourth attempt retrieved the data-source schema via mcp__Notion__API-retrieve-a-data-source and used the standard Notion property shapes — Title:{title:[{text:{content:...}}]}, Date:{date:{start:...}}, Mode/Surface/Risk:{select:{name:...}}, rich_text:{rich_text:[{text:{content:...}}]} — succeeded). Schema-fix pattern: the post-page tool expects standard Notion property shapes (NOT the SQLite-style "date:Date:start" keys that the helper script uses); next ship can skip the trial-and-error by going directly to the standard shape. Closed-by-drift this run: none (historian's 6 existing closed-by-drift items remain correctly marked). Follow-ups logged in backlog.yaml: cart-drawer-cta-hardcoded-color-token-promotion (low, hygiene), cart-drawer-cta-eyebrow-10px-contrast-policy (low, a11y), drawer-cta-confirmation-state-truly-unused (low, hygiene — closure record), stale-next-server-on-3000-blocks-capture-ship (medium, infra, carry-over now 6 ships old), sotd-parser-fix (low, infra, carry-over), lighthouse-baseline-seed (low, infra, carry-over).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 55 — not a multiple of 10, calibrator does NOT fire. Next eligible at shipped_count=60 IF today − last_calibration_at >= 7 days at that point (so 2026-05-21 earliest).

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok this run — not exercised (risk: medium direct-commit, no PR opened); push to origin/main succeeded
playwright_available: unknown  # capture-ship.mjs skipped — port 3000 still held by stale next-server PID 46706
lighthouse_available: unknown  # perf-a11y skipped lighthouse — CSS-only + below-the-fold + no LCP surface + no prior baseline; signal-to-cost poor for this band
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 478000   # +20000 bytes for this run's IMPROVEMENTS.md entry (cart-drawer-cta-editorial-register); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy this run — Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 results; Reports notion-post-page succeeded after a 4-attempt schema-fix loop (1st: parent.type:database_id with abbreviated shapes → 400 invalid property shapes; 2nd: parent.type:data_source_id same shapes → same error; 3rd: parent.type:data_source_id with plain-string values + "date:Date:start" key → 400 "Couldn't find editable properties for date:Date:start"; 4th: retrieved data-source schema via mcp__Notion__API-retrieve-a-data-source and used standard Notion property shapes Title:{title:[{text:{content:...}}]}, Date:{date:{start:...}}, Mode/Surface/Risk:{select:{name:...}}, rich_text:{rich_text:[{text:{content:...}}]} — succeeded). Reports row 361af8d3-d3e2-8130-9d16-ea15e0a38fb3 created with all 9 schema fields populated. Schema-fix pattern noted for future runs: use standard Notion property shapes via mcp__Notion__API-post-page, NOT the SQLite-style "date:Date:start" or plain-string shapes used by notion-sync.mjs helper.
