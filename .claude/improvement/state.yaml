# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T04:00:00Z
last_run_commit: 9d73ea0
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 52   # +1: image-gen-subtask-3-of-3-journal-manifesto-colophon-imagery (task-driven, Notion subtask 360af8d3-d3e2-81d5-a53d-f21127250381 claimed fresh from To do → In progress with Started: 2026-05-15 via MCP notion-update-page in Phase 0; after commit completed via MCP with Status: Done, Commit: 9d73ea0, Completed: 2026-05-15. Closes the image-gen trilogy: all three subtasks ([1/3] section-divider commit 5f6ab9c-era homepage divider, [2/3] specimen-plate-frame commit df7cd8b PDP plates, [3/3] this ship wax-seal + press-stamp tray + colophon-press) now Done; parent task 35faf8d3-d3e2-8168-9026-c7ce8590e62b (Status: Split) is logically complete. Three new server components: WaxSealMark (~60 LOC, circular composition — two concentric hairline rings + 24-tick rotation array + central BFS monogram + ED·III legend, mounted top-right of /journal index header at clamp(96,11vw,144)px absolute, hidden < 720px); PressStampTray (~80 LOC, 4×3 orthogonal grid of italic-serif glyph cells reading like movable type — 'B F S · § & ¶ — i i i .' with sub-pixel cell offsets so glyphs read off-register, mounted under .manifesto-credo in the homepage manifesto sticky rail at clamp(180,18vw,240)px, hidden < 1000px to match sticky breakpoint); ColophonPress (~70 LOC, hand-press silhouette in single-stroke hairline — yoke + side posts + descending platen + guide rods + bed sheet + tapered base + swing-arm lever + foot rule with THE PRESS legend, mounted top-right of .outro-colophon-wrap at clamp(96,9vw,132)px, hidden < 900px). All three: inline SVG, currentColor strokes, aria-hidden, pointer-events:none, zero motion (no transitions, no animations), zero JS, zero CLS. Three independent components by design — each has own viewBox and composition language (circular/gridded/mechanical) and parent CSS positions each differently; premature abstraction would cost more than it saves at N=3 sibling pieces. Mount sites: src/app/journal/page.tsx +2 (<WaxSealMark /> first child of .journal-header), src/app/page.tsx +2 (<PressStampTray /> immediately after .manifesto-credo inside sticky aside), src/components/site-footer.tsx +2 (<ColophonPress /> immediately before <h3 id=outro-colophon-heading> in .outro-colophon-wrap). CSS +78 LOC across three blocks at globals.css:~6646 (wax-seal — adds position:relative to .journal-header), :~2302 (press-stamp-tray — hidden < 1000px to match manifesto sticky breakpoint), :~3261 (colophon-press — adds position:relative to .outro-colophon-wrap). Net component source ~6.7KB raw (well under +9KB brief budget). Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 static routes (unchanged), 0 anti-patterns. SSR class-grep confirms all three mount points: class=wax-seal-mark 1× on /journal, class=press-stamp-tray 1× on /, class=colophon-press 1× on /. perf-a11y PASS — 0 bytes JS delta, CSS delta ~1.6KB pre-min, no LCP risk (all three below initial viewport or beside content already rendered), no CLS risk (no animation of layout properties — zero transitions/animations in all three blocks), no INP risk (no JS), contrast OK (decorative-only at 0.34/0.42 alpha against matte-black, no essential information carried), reduced-motion not relevant (no motion to gate), keyboard unaffected (aria-hidden + pointer-events:none × 3). regression-spotter PASS — adjacent surface signatures preserved: journal-display/journal-display-outline/journal-lede/journal-entries on /journal; manifesto-credo/manifesto-list/manifesto-num on /; outro-colophon-label/outro-colophon-key/outro-colophon-val on /. diff-reviewer PASS — three new files + three single-line mount edits + three additive CSS blocks; no escape hatches, no dead code, no scope creep, no shared primitive modified, no new tokens, no new fonts, no new keyframes. Self-rated rubric: T:2 M:2 L:2 I:1 A:2 D:2 = 11/18 (Solid; clears the >8 abort threshold). T:2 — third inline-SVG asset closing the trilogy with circular/gridded/silhouette idiom flex. M:2 — three different composition strategies in one ship (rotation-array via Array.from for seal ticks, data-driven cell grid for tray, single-stroke outline for press). L:2 — 3 routes touched (journal index, homepage manifesto, every page rendering SiteFooter). I:1 — decorative, no interaction, visible at rest. A:2 — fully aria-hidden + pointer-events:none × 3, no contrast regression on essential content. D:2 — wax-seal + typesetter's tray + hand-press silhouette read as deliberate editorial-press house ornament; tray's italic-serif B F S · § & ¶ — i i i . is most specific to BFS register. Risk band: medium per brief — 3 surfaces touched, components independent, additive only, no shared primitive modified, no JS, no SEO surface. Direct-commit to main per risk-rules.md medium-band default. Screenshots skipped — stale next start PID still holds port 3000 (carried-over blocker stale-next-server-on-3000-blocks-capture-ship). SOTD compare skipped — sotd_parser_available:false. /review skill skipped per routine — additive decorative server components with PASS verdicts across all 5 Phase 5 gates and SSR-verified mount points; review value would be re-reading the same diff. Notion writeback: subtask flipped via MCP update-page Status: Done + Commit: 9d73ea0 + Completed: 2026-05-15 (Surface left as preset [manifesto, colophon, system] on the task row by design). Reports row appended via MCP create-pages with single-select Surface=system (the Reports DB's Surface is single-select unlike Tasks' multi-select; chose system as the primary tag — manifesto + colophon are also touched but recorded in the page body). Follow-ups: image-gen-parent-task-close (low, bookkeeping — parent 35faf8d3-d3e2-8168-9026-c7ce8590e62b is Status: Split with all 3 children Done; semantically complete, human pass can mark Done if desired); wax-seal-mark-mobile-variant (low, distinctive — currently hidden < 720px, future ship could add 64px corner variant); press-stamp-tray-cell-content-rotation (low, distinctive — 12 glyphs hardcoded, if second instance ever ships consider seed prop for distinct cell content); colophon-press-second-consumer-or-prove-rarity (low, hygiene — currently SiteFooter only, keep rare to preserve weight); stale-next-server-on-3000-blocks-capture-ship (medium, infra — carried over from prior 4 ships).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro)
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic)
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 45 — not a multiple of 10, so calibrator does NOT fire on the next run regardless of cooldown. Next eligible at shipped_count=50 IF today − last_calibration_at >= 7 days at that point (so 2026-05-21 earliest).

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok this run — not exercised (risk: low direct-commit, no PR opened)
playwright_available: true   # capture-ship.mjs ran successfully this run, wrote desktop + mobile png to .claude/improvement/screenshots/73af9b8/ (gitignored)
lighthouse_available: unknown  # perf-a11y skipped lighthouse (pure CSS-only ship with 0 JS delta, 1 new keyframe with transform-only composited animation, no LCP/CLS/INP delta risk; signal-to-cost poor for this band)
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 432000   # +9000 bytes for this run's IMPROVEMENTS.md entry (motion-vocabulary [2/3] hover-letter-spacing); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy this run — fetch + update-page + create-pages all returned 200s within normal latency. Phase 0 used MCP notion-search on the Tasks data source, returned a noisy result set (Notion search isn't a strict filter); the main thread then fetched each candidate to verify the actual Status field. Confirmed Status field of the 4 carry-over open subtasks via parallel MCP fetch — motion-vocabulary [2/3] (360af8d3-d3e2-81d9-be2b-e36fc60aad23) was Status: In progress with Started: 2026-05-15 from an earlier aborted run, no Commit, no Completed, working tree clean. Treated as in-flight focus rather than re-claimed or released. Motion-vocabulary [3/3] (360af8d3-d3e2-81ab-bdbb-fe56c3cd37ed magnetic reveal) Status: To do queued. Image-gen [2/3] (360af8d3-d3e2-816e-84c5-d94cdd04faf3 PDP frames) Status: To do queued. Image-gen [3/3] (360af8d3-d3e2-81d5-a53d-f21127250381 journal+manifesto+colophon SVGs) Status: To do queued. Picked motion-vocabulary [2/3] (the in-flight one) — earliest Added timestamp tied with [3/3] but lowest risk profile of the tied pair ([3/3] touches the shared <Magnetic /> primitive with JS work, correctly queued last per its own brief). After ship: subtask 360af8d3-d3e2-81d9-be2b-e36fc60aad23 flipped In progress → Done via MCP update-page with Commit: d618203, Completed: 2026-05-15. Reports row 361af8d3-d3e2-81ad-9c71-fe6fb6cfeeef appended via MCP create-pages using the documented schema fallbacks (Mode='Task-driven' single-value, Surface='system' single-select). The 3 still-open To-do tasks (motion-vocabulary [3/3], image-gen [2/3], image-gen [3/3]) remain queued for future runs. Parent motion-vocabulary task 35faf8d3-d3e2-816e-9304-f18860d09138 remains Status: Split — will flip to Done after [3/3] ships. Parent image-gen task 35faf8d3-d3e2-8168-9026-c7ce8590e62b remains Status: Split.
