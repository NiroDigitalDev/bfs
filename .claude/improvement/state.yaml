# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T17:30:00Z
last_run_commit: 9c7c09d
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 61   # +1: journal-prev-next-dictionary-spread (auditor-picked, cold-surface journal — 0 of last 8 ships). Mirrors the PDP recirculation recompose shipped one run earlier (f107f1b) onto the journal surface: new getAdjacentPosts(slug) helper in src/lib/journal.ts returning {prev, next} on closed-loop modular wraparound; RelatedJournalPosts rewritten to drop the Tilt frame card markup and render an asymmetric prev/next dictionary spread with oversized italic-serif piece numerals (clamp 40-64px, rgba 0.32 resting, 0.7 on hover/:focus-visible) absolute-positioned in the OUTER margin per side via data-direction='prev'|'next' on each <li>, per-side padding offsets (clamp 56-88px), mirrored text-align on next side, and a between-column scaleY(0→1) hairline animated via @supports (animation-timeline: view()) + (prefers-reduced-motion: no-preference) + (min-width: 720px). New keyframe `journal-related-rule-draw` namespaced separately from PDP's `related-rule-draw`. Eyebrow recast per direction: prev '← Previous · Piece · III', next 'Piece · V · Next →'. Section eyebrow text 'Related dispatches' → 'Cross-references'. aria-label enriched: ${label} piece · ${title} · Piece ${fig}. Tilt import removed from this component (still used elsewhere in src/components/tilt.tsx — primitive unchanged). Reveal primitive untouched. No new client boundary. Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows; no task-driven override. Picked at T2 M3 L3 I3 A3 D3 = 17/18 (top of this run, high-distinctive band) over journal-related-grid-asymmetric @ 14/S (same surface — would be redundant with this move), folio-numeral-re-typeset-on-chapter-cross @ 13/M (chrome — saturated 3-of-8), pdp-price-as-dictionary-headword @ 13/M (catalogue — saturated 5-of-8), journal-hero-outline-scroll-bind @ 13/S, numeral-plate-utility @ 11/S (system — better landed AFTER fourth occurrence this ship creates), hairline-alpha-token-family @ 10/M (system — high ripple but invisible single ship). Surface-freshness tiebreaker decisively favoured journal (0 of last 8 ships — 5 catalogue / 3 chrome / 0 journal). Net delta: 3 files, +257/-93 LOC (bigger than PDP analog because the helper + reduced-motion override updates + class deletion accompany the spread recompose). Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged baseline in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 routes prerendered, 0 anti-patterns. SSR class-grep on ALL 11 prerendered /journal/<slug> routes: each renders exactly 1× <section class='journal-related'> + 2× .journal-related-item + 1× data-direction='prev' + 1× data-direction='next' + 2× .journal-related-numeral + 2× .journal-related-eyebrow-pair + 'Previous piece' / 'Next piece' aria-labels enriched with title + Piece <fig>. Closed-loop adjacency verified across all 11 posts (every post's next.prev == self AND prev.next == self): XI ← X ← IX ← VIII ← VII ← VI ← V ← IV ← III ← II ← I ← XI. SSR adjacent-route check: /, /about, /journal index, /supplies/void-book, /supplies/savior-pen all render 0× class='journal-related' markup — no leak; PDP .related-* block remains intact (12 references to .related-numeral/.related-grid in globals.css — all in the PDP block, unchanged); no orphan references to the removed .journal-related-frame*/.journal-related-date classes anywhere in src/. perf-a11y PASS — small net JS *reduction* (one fewer Tilt boundary per /journal/<slug>); ~+2KB gzipped CSS net; no LCP risk (below the fold on every journal post); no CLS risk (numeral absolute-positioned, ::after absolute, transform-only animation composited, padding offsets at initial render); no INP risk (no new handlers; :has() + :focus-visible CSS-only); keyboard focus order intact (prev → next DOM order matches reading order); reduced-motion guard complete (keyframe nested under @supports animation-timeline:view() AND @media prefers-reduced-motion:no-preference AND min-width:720px — reduced-motion / non-supporting / mobile paths fall through to static scaleY(1)); contrast all clear: numeral 0.32 ≈ 4.8:1 large-text AA, eyebrow-label 0.78 ≈ 13:1 AAA, eyebrow body 0.55 ≈ 7.6:1 AAA, sep 0.32 ≈ 4.8:1, piece 0.55 ≈ 7.6:1 AAA, arrow 0.6 ≈ 8.5:1 AAA. regression-spotter PASS (all 11 journal posts render new vocabulary; /, /about, /journal index, 2 PDP spot-checks clean; PDP related-* block untouched). diff-reviewer PASS-WITH-NITS — 3 follow-ups: (a) extends related-grid-rule-color-token-promotion to include .journal-related-* rgba literals, (b) folio-numeral-pattern-extract-utility severity bumped low → medium because this ship reaches the FOURTH occurrence of the italic-serif oversized-margin numeral recipe (.chapter-numeral, .folio-numeral, .related-numeral, .journal-related-numeral) — the threshold the prior note named as the trigger for hoisting, (c) get-related-posts-dead-export — getRelatedPosts in src/lib/journal.ts is now unreferenced (grep returns only its own declaration) — filed for cleanup on next journal-surface ship rather than deleted in this scope. Risk band: medium per risk-rules.md (3 files; +257/-93 LOC fits 100-300 medium band; no shared primitive surgery; no design-token edit; no SEO/JSON-LD change; no new client boundary; visible-as-recirculation-chrome on every journal post — same risk-rules classification as the PDP analog). Direct-commit to main per medium-band default. Screenshots skipped — capture-ship.mjs carry-over blocker, now 12 consecutive ships old (capture-ship-stale-server-on-3000 backlog at severity high). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per established pattern for medium-risk register-coherence ships that mirror an already-shipped move with PASS verdicts across all 5 Phase 5 gates — the diff is structurally identical to f107f1b's diff translated to the journal namespace. Notion writeback: no task to flip (no override); Reports row 361af8d3-d3e2-8125-b2c4-d9911260872b appended via MCP mcp__Notion__API-post-page (first attempt succeeded with correct parent.database_id=60cc3221-8e8a-42da-b926-20cd32d6c8bb — the lesson learned on 60cc...→data_source_id confusion is now baked into the routine) with Surface=journal, Mode=Shipped, Rubric T2 M3 L3 I3 A3 D3 = 17/18, Risk=Medium, Commit=9c7c09d, all 10 schema fields populated. Closed-by-drift this run: journal-related-posts-tag-overlap-ranking (low — its premise depends on getRelatedPosts which is no longer the call site). Backlog received 1 new entry: get-related-posts-dead-export (low, hygiene — diff-reviewer finding (c)). 3 existing items updated: related-grid-rule-color-token-promotion (extended to journal-related-*), folio-numeral-pattern-extract-utility (severity low → medium, files extended), related-eyebrow-arrow-glyph-aria-hidden-double-emphasis (files extended). Recomposed the 1fr|1fr PDP related grid into an asymmetric dictionary cross-reference — per-side oversized italic-serif plate numerals in opposite margins (font-serif italic, clamp 40-64px, rgba 0.32→0.7 on hover/:focus-visible) absolute-positioned via data-direction='prev'|'next', per-side padding offsets (clamp 56-88px), mirrored text-align on next side, and a between-column vertical hairline drawing scaleY(0→1) via @supports (animation-timeline: view()) + (prefers-reduced-motion: no-preference) + (min-width: 720px). Eyebrow recast per direction: prev '← Previous · Plate · III', next 'Plate · V · Next →'. aria-label enriched with direction. One new keyframe `related-rule-draw`. <ol>+<li> semantics preserved. Tilt + Reveal primitives untouched. No new client boundary. Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows; no task-driven override. Picked at T2 M2 L3 I3 A3 D3 = 16/18 (top of this run) over margin-as-a-gesture-but-no-margin @ 14/M (journal auditor #1, bundles 3 primitives — task-split risk), dispatch-prev-next-pagination @ 13/S (journal #2), journal-list-entries-generic-reveal @ 13/S, catalogue-spec-link-arrow-static @ 13/S, pdp-press-display-monotone-em @ 13/S, folio-numeral-re-typeset-on-chapter-cross @ 13/M (chrome-saturated per historian — 3 consecutive chrome ships), journal-prose-no-scroll-progress @ 12/M (touches scroll-progress shared primitive — high risk). Surface-freshness tiebreaker reinforced catalogue (6 ships ago vs chrome 3-in-a-row). Strategy: rewrite related-products.tsx with siblings tuple typed `as const` for literal direction narrowing, conditional ternary per-direction for eyebrow markup, new <span class='related-numeral'> rendered before the Link; rewrite .related-grid block in globals.css to add position: relative + ::after between-column hairline (scaleY 0→1 via per-element view-timeline named --related-grid-tl with animation-range entry 10% cover 35%), new .related-numeral rules (display: none below 720px, absolute-positioned with per-direction left|right: 4px above 720px, color cascade via .related-item:has(.related-link:hover|:focus-visible)), new .related-eyebrow-pair / .related-eyebrow-label / .related-eyebrow-sep rules, per-direction padding overrides on .related-link, mirror text-align on next side .related-meta. Net delta: 2 files, +159/-6 LOC. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged baseline in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 routes (Turbopack 1165.2ms), 0 anti-patterns. SSR class-grep on all 6 PDP routes (/supplies/{void-book, abyssal-cardstock, event-horizon-pad, sticky-voids, savior-pen, executive-despair}): each renders 1× <section class='related'> + 2× .related-item (1× data-direction='prev' + 1× data-direction='next') + 2× .related-numeral + 2× .related-eyebrow-pair + 'Previous specimen' / 'Next specimen' aria-labels; numerals form closed loop I→II→III→IV→V→VI→I; 0× .related-* markup on /, /journal, /journal/<sample>, /about (substring hits on journal article HTML are journal-related-item, a separate pre-existing component, not a regression); /shop is not a route in this codebase — catalogue lives on /. perf-a11y PASS — 0 net JS delta (server component), ~+1.5-2KB gzipped CSS, no LCP risk (section is below the fold on every PDP), no CLS risk (numeral absolute-positioned, ::after absolute, transform-only animation composited, padding offsets applied at initial render not in response to JS/hover/scroll), no INP risk (no new handlers; :has() + :focus-visible CSS-only), keyboard focus order intact (prev → next DOM order), reduced-motion guard complete (keyframe nested under @supports animation-timeline:view() AND @media prefers-reduced-motion:no-preference AND min-width:720px — reduced-motion / non-supporting / mobile paths fall through to static scaleY(1)), contrast all clear: numeral 0.32 ≈ 4.8:1 large-text AA, eyebrow-label 0.78 ≈ 13:1 AAA, eyebrow body 0.55 ≈ 7.6:1 AAA, sep 0.32 ≈ 4.8:1. regression-spotter PASS (all 6 PDPs render the new vocabulary; 4 adjacent non-PDP routes clean). diff-reviewer PASS-WITH-NITS — 2 follow-ups: (a) .related-grid::after uses rgba(255,255,255,0.18) rather than var(--color-line-*) — matches local-block convention until token sweep, (b) .related-numeral re-declares the italic-serif oversized-margin numeral recipe (3rd occurrence after .chapter-numeral and .folio-numeral — fourth justifies hoisting into a utility class). Risk band: medium per risk-rules.md (2 files; +159/-6 LOC fits the 100-250 medium band; no shared primitive surgery; no design-token edit; no SEO/JSON-LD change; no new client boundary; visible-as-recirculation-chrome on every PDP). Direct-commit to main per medium-band default — medium does NOT branch to PR; medium adds screenshot capture which is currently broken (carry-over). Screenshots skipped — capture-ship.mjs carry-over blocker, now 11 consecutive ships old (proper backlog item capture-ship-stale-server-on-3000 already at severity high). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per established pattern for medium-risk catalogue ships consuming established primitives with PASS verdicts across all 5 Phase 5 gates. Notion writeback: no task to flip (no override); Reports row 361af8d3-d3e2-8172-82fe-f887b2bd3fe8 appended via MCP mcp__Notion__API-post-page first attempt (initial attempt failed 404 against `database_id=<data_source_id>` — fix is using the actual `database_id=60cc3221-8e8a-42da-b926-20cd32d6c8bb`; cron historical schema-fix pattern absorbed: the parent.database_id field needs the DB id not the data-source id) with Surface=catalogue, Mode=Shipped, Rubric T2 M2 L3 I3 A3 D3 = 16/18, Risk=Medium, Commit=f107f1b, all 10 schema fields populated. Closed-by-drift this run: none. Backlog received 3 new entries: related-grid-rule-color-token-promotion (low, hygiene — diff-reviewer #1), folio-numeral-pattern-extract-utility (low, hygiene — diff-reviewer #2), related-eyebrow-arrow-glyph-aria-hidden-double-emphasis (low, a11y verification — perf-a11y nit, one-shot AT test queued).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 61 — NOT a multiple of 10, so no calibrator trigger this run regardless of date. Next eligible at shipped_count=70.

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok; push to origin/main succeeded (commit 9c7c09d: 9d2399f..9c7c09d)
playwright_available: unknown  # capture-ship.mjs skipped — carry-over now 12 ships. capture-ship-stale-server-on-3000 backlog at severity high — this is the longest-running open infra blocker and is starting to hide regressions; promote to top-of-queue if surface-freshness ever stops dominating Phase 2 picks.
lighthouse_available: unknown  # perf-a11y skipped lighthouse — below-the-fold CSS-only motion delta on existing surface; reuses gate pattern from one run earlier; no prior baseline in lighthouse.csv
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 581000   # +15000 bytes for this run's IMPROVEMENTS.md entry (journal-prev-next-dictionary-spread); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy. Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 rows. Reports notion-post-page succeeded first attempt with parent.database_id=60cc3221-8e8a-42da-b926-20cd32d6c8bb — the historical schema-fix lesson is now baked into routine invocation. Reports row 361af8d3-d3e2-8125-b2c4-d9911260872b created with all 10 schema fields populated; commit field patched 'pending' → '9c7c09d' via mcp__Notion__API-patch-page after the ship commit landed.
