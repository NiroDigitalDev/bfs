# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T12:24:00Z
last_run_commit: 5e79950
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 56   # +1: about-display-figure-plate-printer-marks (auditor-pick from About/Colophon/Press cold-surface pass — Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows; no task-driven override). Picked at T3 M1 L3 I2 A2 D3 = 14/18 over journal-prose figure/caption (tied 14/M — only activates on image-bearing posts which current corpus lacks), colophon press SVG platen-tap @ 13/M, hero scroll-indicator scroll-bound @ 12/S; tiebreaker cold-surface freshness (no about ship in 12+ runs) + first-paint visible payoff. Wrapped <SplitText className=about-display> in <div className=about-display-frame> holding 2 decorative aria-hidden corner printer's-bug spans mirroring the catalogue specimen-plate's four-corner vocabulary at masthead scale: top-left .about-display-fig (italic-serif "Fig. —" 11px sup + 12px hairline rule + caps-sans "THE PRESS" 9px 0.32em sub), bottom-right .about-display-edition (caps-sans "Ed." + italic-serif "III" clamp 14px + decorative mid-dot sep + caps-sans "MMXXVI" roman year). Frame uses position:relative + padding-block clamp(28px,4vw,56px) clamp(40px,5vw,72px) to reserve vertical space so absolute corner marks never reflow the h1 baseline. @keyframes aboutDisplayMarkRise (0.6s opacity 0→1 + translateY 4px→0; delays 0.18s/0.28s) with full @media (prefers-reduced-motion: reduce) override pinning opacity:1 + transform:none. Mobile (<720px) hides .about-display-edition to avoid descender collision with .about-lede; .about-display-fig stays. Two files: src/app/about/page.tsx (wrap SplitText + 2 corner spans, +14 JSX lines), src/app/globals.css (new block after :7733, +114 lines net). Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 routes (Turbopack 1048.4ms + 517.9ms), 0 anti-patterns. SSR class-grep on /about: about-display-frame 2×, about-display-fig 2×, about-display-edition 4× (span + inner em + decoration), about-display 10× (SplitText word/char spans intact), about-lede/about-prose/about-header all unchanged, about-cta 2× labels (Send a note + Read the journal — responsive duplication ×2 each), journal-eyebrow/journal-breadcrumb/journal-header/outro-colophon intact, SVG well-formedness 1/1; scoping confirmed — 0 about-display-* hits on / and /journal SSR (correctly scoped to /about). perf-a11y PASS-WITH-FOLLOWUPS — CSS-only delta ~+114 lines (~+2.4KB raw, ~+0.4-0.6KB gzipped due to high dedupe vs existing spec-plate-* rules), 0 bytes JS delta (no new client component), no LCP risk (SplitText h1 remains LCP candidate untouched in markup; wrapping in position:relative div doesn't change paint cost), no CLS risk (frame padding-block reserves space BEFORE absolutely-positioned spans render so h1 baseline is fixed at first paint; translateY is compositor-only), no INP risk (no JS listeners), keyboard parity preserved (both spans are aria-hidden non-interactive non-tabIndex; tab order unchanged), reduced-motion override complete, contrast — color-text-muted ≈ 6.4:1 on #0a0a0a for 9px caps-sans (passes AA-body comfortable), color-fog #6b6b6b on #0a0a0a ≈ 3.5:1 for italic-serif "III" (below AA 4.5:1 but matches established catalogue spec-plate vocabulary which uses the same token; both marks are aria-hidden decorative chrome, page identity lives in SplitText h1 + JSON-LD), hairline ::before + .about-display-edition-sep at rgba(255,255,255,0.32) is purely decorative. regression-spotter PASS — adjacent surfaces all rendered, new classes correctly scoped to /about (0 occurrences in / or /journal SSR); journal-nav's missing nav-cta is structural to the variant (also missing on /journal /privacy /terms), not a regression. diff-reviewer PASS-WITH-NITS — 5 new selectors all used in JSX, 0 dead selectors, 0 escape hatches (no !important/as any/@ts-ignore/console.*), 0 scope creep (spec delivered exactly: FIG. top-left + edition bottom-right + reduced-motion + mobile break); 2 low-severity nits — rgba(255,255,255,0.32) literal for hairline rule + mid-dot sep (matches established BFS hairline convention; rolls into hairline-opacity-token-sweep follow-up), 9px/11px/14px sub-clamp font-sizes (intentional for printer's-bug marks at micro scale, no token equivalent exists). Risk band: low per risk-rules.md — single dedicated route /about, scoped .about-display-* classes, no shared primitive touched, no JS, no SEO surface, additive only. Direct-commit to main per low-band default. Screenshots skipped — stale next-server still holds port 3000 (carry-over blocker stale-next-server-on-3000-blocks-capture-ship, 7 ships now). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per routine — focused single-route low-risk ship with PASS verdicts across all 5 Phase 5 gates and SSR-verified scoping; review value would be re-reading the same diff. Notion writeback: no task to flip this run (no override); Reports row 361af8d3-d3e2-81a9-a274-e0502afbeb7a appended via MCP API-post-page (first attempt hit 400 — used multi_select for Surface + wrong field name "Files"; second attempt with select:{name:system} for Surface and "Files changed" field name succeeded; Surface=system because the schema's select options don't include "about" — system is the closest fit for editorial vocabulary extension work). Schema-fix pattern noted: Reports DB Surface is single-select with options [hero, catalogue, manifesto, field-notes, codex, colophon, faq, outro, chrome, nav, cart, 404, system, seo]; "Files" field name is "Files changed" not "Files". Closed-by-drift this run: drawer-cta-confirmation-state-truly-unused (historian-verified — commit 8771e67 excised data-confirming selectors). Housekeeping: motion-vocabulary-subtask-3-of-3-magnetic-reveal flipped open → shipped in backlog (already shipped in shipped.yaml). Follow-ups logged in backlog.yaml: about-display-fig-rgba-hairline-token-promotion (low, hygiene), about-display-edition-color-fog-decorative-only-doc (low, hygiene), about-display-mark-rise-keyframe-shared-with-spec-plate (low, system), stale-next-server-on-3000-blocks-capture-ship (medium, infra, carry-over 7 ships now), sotd-parser-fix (low, infra, carry-over), lighthouse-baseline-seed (low, infra, carry-over).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 56 — not a multiple of 10, calibrator does NOT fire. Next eligible at shipped_count=60 IF today − last_calibration_at >= 7 days at that point (so 2026-05-21 earliest).

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok this run — not exercised (risk: low direct-commit, no PR opened); push to origin/main succeeded
playwright_available: unknown  # capture-ship.mjs skipped — port 3000 still held by stale next-server
lighthouse_available: unknown  # perf-a11y skipped lighthouse — CSS-only + below-the-fold + no LCP surface + no prior baseline; signal-to-cost poor for this band
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 495000   # +17000 bytes for this run's IMPROVEMENTS.md entry (about-display-figure-plate-printer-marks); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy this run — Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 results; Reports notion-post-page succeeded on 2nd attempt after schema-fix (Surface is single-select not multi-select; field name is "Files changed" not "Files"). Reports row 361af8d3-d3e2-81a9-a274-e0502afbeb7a created with all 10 schema fields populated. Schema-fix pattern documented in shipped_count notes: Surface options [hero, catalogue, manifesto, field-notes, codex, colophon, faq, outro, chrome, nav, cart, 404, system, seo] — does NOT include "about", so pick "system" for editorial-vocabulary work on /about; field names exactly match schema (e.g. "Files changed" not "Files", "Follow-ups" not "Followups").
