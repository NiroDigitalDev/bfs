# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T05:00:00Z
last_run_commit: b23cc5a
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 53   # +1: manifesto-item-as-sans-card (standing-backlog pick — no Notion task-driven override this run; all 9 To-do rows checked via MCP fetch were Status: Done after the 6-ship task-driven sprint of the past two days). Picked over hero-type-instrumental @ 15/L and motion-vocabulary-beyond-reveal @ 13/L on highest non-disqualified total (T3 M2 L3 I2 A3 D3 = 16/18 Awwwards-grade); retro 2026-W19 adjustment #2 cold-surface tiebreaker confirmed (manifesto = lifetime cold surface, 0 ships at n=52 before this). Recast .manifesto-item h3 from Inter-800 uppercase clamp(22,2vw,30)px to italic-serif clamp(34,4vw,56)px with letter-spacing -0.01em; recast .manifesto-num from Inter-800 14px tracked-caps to italic-serif oldstyle numerals clamp(22,2vw,28)px with font-feature-settings:"onum" 1; added scroll-bound active state via new src/components/manifesto-list.tsx (~58 LOC, "use client") owning one shared IntersectionObserver across 4 li refs with rootMargin -25% 0px -25% 0px and threshold [0,.25,.5,.75,1] firing when entry.intersectionRatio > 0.5; items toggle data-active="true|false". Active state CSS rules at globals.css:~2316-2330: h3 color 0.7→1.0 + letter-spacing -0.01em→-0.025em (typographic re-typesetting) + translateX 2px; p color 0.55→0.85; num color 0.35→1.0 + translateX -2px; all transitions 480ms var(--ease-out-quart). Reduced-motion @media block at globals.css:~2332-2342: pins letter-spacing to -0.01em + transform: none + downgrades all transitions to color 240ms linear so glyph metrics never shift under reduce. JS layer short-circuits to all-active-on-mount under reduce. SSR-renders all four items with initial data-active="false" — hydration-safe, no flash. Replaced inline 4-item Reveal-wrapped map in src/app/page.tsx:497-528 with <ManifestoList items={[…]} /> (item copy verbatim). Net delta — 3 files: src/components/manifesto-list.tsx +58 NEW, src/app/page.tsx +2/-28, src/app/globals.css +50/-11. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings, bunx tsc --noEmit clean, bun run build 48/48 static routes unchanged, 0 anti-patterns. SSR class-grep on /: class="manifesto-list" 1×, class="manifesto-item" 4×, all four <h3> titles verbatim ("On privacy as default", "On tone as position", "On contrast as discipline", "On refusal as feature"), <span class="manifesto-num">01-04</span> sequential, every <li> emits data-active="false" initial SSR state (IO toggles client-side). perf-a11y PASS-WITH-FOLLOWUPS — bundle delta well under +1.5KB target (one IO + ref array replaces 4 Reveal wrappers, source delta ~+60 LOC TSX gzipped much smaller), CSS delta +39 net lines / ~+1.0KB raw / ~+0.4KB gzipped at the +1KB boundary, no LCP risk (manifesto mid-page CH.03 not above fold), no CLS at runtime (data-active animates color + letter-spacing + transform: translateX(2px) only — none affect block flow; SSR-rendered h3 size final on first paint), no INP risk (IO passive, single setAttribute per entry, zero scroll/pointer handlers), keyboard unaffected (no new focusable elements), reduced-motion complete in both layers, contrast h3 0.7→1.0 ≈ 10.0:1→20.1:1 (AAA-large) / p 0.55→0.85 ≈ 6.5:1→14.7:1 (AA-body) / num 0.35→1.0 ≈ 3.3:1→20.1:1 (passes 3:1 large-text rule for decorative 22-28px serif italic ordinal). regression-spotter PASS — adjacent surfaces preserved: .manifesto-sticky 2×, .manifesto-credo 2×, .press-stamp-tray 2× (just-shipped tray sibling intact), .section-title 8×, codex .codex-row/.codex-rule/.codex-eyebrow 6× each, CH.04 pullquote 4× via .pullquote-body, 5× .press-clipping, .outro-colophon-wrap + .colophon-press 2× each (footer mount preserved), .from-journal 1× + .from-journal-item 4×, PDP .related-grid + .related-item 2× + .related-plate 2× + .related-figure 2× intact on all 6 PDPs, 41× .spec-plate per PDP. diff-reviewer PASS-WITH-NITS — no any, no @ts-ignore, no console.*, no scope creep, reduced-motion guarded in both CSS and JS, SSR-safe typeof window check present; medium nit on possible dead Reveal import was non-issue (grep confirms 13 Reveal usages remain in page.tsx); low nits on rgba(244,244,244) inline literals (paper-alpha-ramp convention established across globals.css) and single-letter t/d prop names (matches original inline convention; type kept terse) — logged as backlog items rather than blocked. Risk band: medium per risk-rules.md — new client component + new transition (letter-spacing) affects glyph metrics. Direct-commit to main per medium-band default + screenshots target on both viewports. Screenshots skipped — stale next-server PID still holds port 3000 unresponsive (carry-over blocker stale-next-server-on-3000-blocks-capture-ship). SOTD compare skipped — sotd_parser_available:false. /review skill skipped per routine — focused single-surface ship with PASS verdicts across all 5 Phase 5 gates; review value would be re-reading the same diff. Notion writeback: no task to flip this run (no override); Reports row 361af8d3-d3e2-81fe-8162-e5290007d44b appended via MCP create-pages with Surface=manifesto (single-select). Closed-by-drift this run: add-to-cart-no-live-announcement (cart-drawer announces sitewide), from-journal-block-renders-1-item-currently (11 posts now exported). Follow-ups: manifesto-credo-active-correspondence (medium, distinctive — sticky credo could mirror active item's italic numeral as marginalia <sup>); manifesto-num-resting-contrast-policy (low, a11y — resting 0.35 ≈ 3.3:1 passes 3:1 large-text but below 4.5:1, document or bump); manifesto-p-body-alpha-drop-documentation (low, hygiene — p resting alpha dropped 0.65 → 0.55, still AA but worth noting); lighthouse-baseline-seed (low, infra — csv header-only); stale-next-server-on-3000-blocks-capture-ship (medium, infra — carry-over from 4 prior ships).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro)
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic)
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 53 — not a multiple of 10, so calibrator does NOT fire on the next run regardless of cooldown. Next eligible at shipped_count=60 IF today − last_calibration_at >= 7 days at that point (so 2026-05-21 earliest).

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok this run — not exercised (risk: medium direct-commit, no PR opened)
playwright_available: unknown  # capture-ship.mjs ran but skipped on server-readiness (port 3000 held by stale PID); playwright itself not exercised this run
lighthouse_available: unknown  # perf-a11y skipped lighthouse (CSS + tiny client component, no LCP surface, no above-the-fold change, no prior baseline in csv to compare against; signal-to-cost poor for this band)
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 442000   # +10000 bytes for this run's IMPROVEMENTS.md entry (manifesto-item-as-sans-card); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy this run — notion-search across Tasks DS + parallel notion-fetch on all candidate To-do rows + Reports notion-create-pages all returned 200s within normal latency. Phase 0 verified all 9 candidate task rows (motion [1/3]/[2/3]/[3/3], image-gen [1/3]/[2/3]/[3/3], legal [1/2]/[2/2], journal [1/3]/[2/3]/[3/3], /about, PDP-reviews, PDP-more-sections, site-footer-everywhere, journal-nav, display-type-vocabulary, write-10-journal-posts-parent) are Status: Done — no override. Picked standing-backlog manifesto-item-as-sans-card per rubric. After commit, MCP create-pages appended Reports row 361af8d3-d3e2-81fe-8162-e5290007d44b with Surface=manifesto (single-select), Mode=Shipped, Rubric="T3 M2 L3 I2 A3 D3 = 16 / 18", Risk=Medium, Commit=b23cc5a, all follow-ups listed.
