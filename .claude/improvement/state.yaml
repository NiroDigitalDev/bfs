# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-16T06:18:00Z
last_run_commit: 74f4fe3
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 63   # +1: outro-wordmark-dictionary-expansion (found-WIP completion mode, chrome surface — first chrome application of the dictionary register established on PDP recirc f107f1b → journal prev/next 9c7c09d → /about coda df006ad). Started the run with an uncommitted edit on src/components/site-footer.tsx that had already recomposed <div className='outro-wordmark'>BFS</div> into three children (<span outro-wordmark-major>Blacks</span> + <span outro-wordmark-conn><em>for</em></span> + <span outro-wordmark-major>Sale.</span>) plus an <span sr-only>Blacks for Sale. Edition III, MMXXVI.</span> sibling, but with NO supporting CSS — the three children would have collapsed into a single inline-rendered white-space:nowrap line, losing the dictionary-lemma compression idea entirely. Reverting the WIP would have discarded both the editorial intent (dictionary register extended to chrome, where the abbreviation literally becomes its own lemma) AND the a11y win (sr-only sibling gives ATs the closing identity — the .outro-wordmark itself remains aria-hidden as before, but the only 'BFS' mark on the site was previously a11y-silent). Completing it was the cleanest path. No Notion task override this run — Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows. Self-rated rubric T3 M2 L2 I1 A3 D3 = 14/18 (distinctive band — fourth surface for the dictionary register, FIRST chrome application; A=3 earned on the sr-only fix to a previously-silent mark; D=3 earned on cross-surface register extension to chrome). Strategy — rescope the existing .outro-wordmark base rule from a single nowrap-overflow word at clamp(40vw, 50vw, 60vw) to a stackable container at clamp(34vw, 44vw, 54vw); remove white-space:nowrap; raise max-height to 90vw; keep -webkit-text-stroke 1px rgba(255,255,255,0.1) + color:transparent on the parent (inherited by .outro-wordmark-major children); add .outro-wordmark-major (display:block) and .outro-wordmark-conn (display:block + position:relative + z-index:1 + font-family:var(--font-serif) + font-style:italic + font-weight:400 + font-size:clamp(36px, 6vw, 92px) + letter-spacing:-0.01em + line-height:1 + color:rgba(255,255,255,0.55) + -webkit-text-stroke:0 + margin:clamp(-9vw, -7.5vw, -5vw) 0 — the negative-margin lemma-compression move pulls the italic-serif connector into the line-gap between the two outline-stroke majors); rescope the @supports (animation-timeline: view()) + @media (prefers-reduced-motion: no-preference) and (min-width: 900px) block's target from .outro-wordmark to .outro-wordmark-major (the existing @keyframes outro-wordmark-settle body is preserved verbatim — only the target selector moves, so the existing outro-wordmark-settle-token-promotion backlog item's premise 'promote start values to tokens once a second settle instance lands' remains unchanged because this run did NOT add a second instance). Net delta: 2 files, +56/-13 LOC. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged baseline in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 routes prerendered (Turbopack OK). SSR class-grep on representative routes /, /journal/the-folio-as-an-instrument, /supplies/void-book: each renders 2× 'outro-wordmark' substring + 4× '-major' substring + 2× '-conn' substring (the 1:2:1 ratio per render is doubled by the RSC streaming chunk serializing className strings a second time for hydration). Sr-only sibling literal-grep on /, /about, /journal, /privacy — exactly 1 each (sitewide chrome confirmed). /_not-found SSR: .nf-wordmark renders 3× (unchanged — the 404 closing 404-as-wordmark block at globals.css:4680-4698 was deliberately not touched and remains structurally distinct from outro-wordmark); the same route renders the new outro-wordmark-major × 4 / outro-wordmark-conn × 2 / sr-only × 1 (chrome consistency holds even on 404). perf-a11y PASS — 0 net JS delta (CSS-only behavioral changes; the WIP JSX was already in place), ~+1KB gzipped CSS net, no LCP risk (wordmark is FINAL element of footer, below the fold), no CLS risk (clamp() values applied at initial render; settle only animates letter-spacing + -webkit-text-stroke-width which are non-layout properties), no INP risk (no handlers; settle is view-timeline CSS-only off main thread), keyboard focus order intact (no interactive elements added), reduced-motion path complete: settle keyframe is gated by @supports + prefers-reduced-motion:no-preference + min-width:900px — under reduced-motion / non-supporting browser / mobile, the majors render static at letter-spacing:-0.08em + -webkit-text-stroke-width:1px (the base rule values), and the connector is ALWAYS static regardless of motion preference. Contrast: connector at rgba(0.55) ≈ 7.6:1 AAA for normal-weight italic-serif at clamp(36-92px); majors at rgba(0.1) outline are decorative inside aria-hidden parent. NEW a11y signal: the previously aria-hidden 'BFS' mark now has an adjacent sr-only sibling reading 'Blacks for Sale. Edition III, MMXXVI.' — first time AT users on every route get the closing identity. regression-spotter PASS — chrome regression check clean: every adjacent route family (/, /journal/<slug>, /supplies/<slug>, /_not-found) renders the new vocabulary with consistent counts; .nf-wordmark block at globals.css:4680-4698 untouched; outro-wordmark-settle keyframe body preserved verbatim (only target moved from .outro-wordmark to .outro-wordmark-major). diff-reviewer PASS-WITH-NITS — 2 follow-ups: (a) the negative-margin lemma-compression on .outro-wordmark-conn is a NEW pattern on this surface, distinct from the dictionary-spread cross-reference recipe (the spreads use absolute-positioned-in-outer-margin numerals; this uses negative margin on an italic-serif connector). If a SECOND dictionary-expansion-of-abbreviation surface lands, the connector recipe should hoist to .lemma-conn utility — NEW backlog item lemma-connector-pattern-extract-utility (low, premature without a second instance). (b) outro-wordmark-settle-token-promotion trigger condition unchanged this run; no flip to closed-by-drift, just last_verified refresh. anti-patterns 0. Risk band: medium per risk-rules.md — touches sub-component (SiteFooter) visible on EVERY route, but no shared-primitive surgery, no design-token edit, no SEO change, no new client boundary, net 56/-13 LOC well under 250-line high-band threshold; chrome-on-every-route visibility is the decisive medium-band signal (not file count, not LOC). Direct-commit to main per medium-band default — commit 74f4fe3 (push to main: 9e2b5f6..74f4fe3). Screenshots skipped — capture-ship.mjs carry-over blocker, now 14 consecutive ships old (capture-ship-stale-server-on-3000 backlog at severity high — the longest-running open infra blocker; a chrome ship visible on every route is exactly where a visual diff against the prior chrome ship would have been most valuable). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per established pattern for medium-risk register-coherence ships with PASS verdicts across all 5 Phase 5 gates AND a structurally-small CSS-only behavioral diff (the JSX scaffold was pre-existing). Notion writeback: no task to flip (no override); Reports row 362af8d3-d3e2-817c-967c-d274ff9d8dca created via mcp__Notion__API-post-page (first attempt succeeded with parent.database_id=60cc3221-8e8a-42da-b926-20cd32d6c8bb — the historical schema-fix lesson stays baked into routine invocation) with Surface=chrome, Mode=Shipped, Rubric T3 M2 L2 I1 A3 D3 = 14/18, Risk=Medium, Commit=74f4fe3, all 10 schema fields populated. MCP path reliable three runs in a row. Closed-by-drift this run: none. Backlog: NEW item lemma-connector-pattern-extract-utility (low, hygiene — premature, needs a second dictionary-expansion-of-abbreviation surface before the .outro-wordmark-conn recipe is worth hoisting). All other backlog items unchanged this run — folio-numeral-pattern-extract-utility remains at high (top-priority system ship, still uncommitted — this ship intentionally did NOT touch numerals so leaves that queue intact), rule-draw-keyframe-consolidate remains at medium (no new identical keyframe this run), related-grid-rule-color-token-promotion remains at medium (scope unchanged), related-eyebrow-arrow-glyph-aria-hidden-double-emphasis remains at low (scope unchanged), outro-wordmark-settle-token-promotion remains open (trigger condition 'second settle instance lands' is unchanged — the rescope was a target-selector move, not a second keyframe instance).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 63 — NOT a multiple of 10, so no calibrator trigger this run regardless of date. Next eligible at shipped_count=70.

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok; push to origin/main succeeded (commit 74f4fe3: 9e2b5f6..74f4fe3)
playwright_available: unknown  # capture-ship.mjs skipped — carry-over now 14 ships. capture-ship-stale-server-on-3000 backlog at severity high — this is the longest-running open infra blocker and chrome-on-every-route ships like this one are exactly where the visual diff would have been most valuable; promote to top-of-queue if surface-freshness ever stops dominating Phase 2 picks (or if a critic/retro flags the missing visual record).
lighthouse_available: unknown  # perf-a11y skipped lighthouse — below-the-fold CSS-only motion delta on existing surface; reuses gate pattern; no prior baseline in lighthouse.csv
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 611000   # +15000 bytes for this run's IMPROVEMENTS.md entry (outro-wordmark-dictionary-expansion); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 4 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy three runs in a row. Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 rows. Reports notion-post-page succeeded first attempt with parent.database_id=60cc3221-8e8a-42da-b926-20cd32d6c8bb. Reports row 362af8d3-d3e2-817c-967c-d274ff9d8dca created with all 10 schema fields populated INCLUDING Commit=74f4fe3 set in the create call (no follow-up patch needed since the commit landed before the Notion write this run). MCP path reliable three runs in a row.
