# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T13:30:00Z
last_run_commit: f14fc01
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 57   # +1: journal-folio-on-journal-routes (cool-surface auditor pick — extends the editorial running-folio masthead onto /journal index + 11 post pages without touching shared running-folio.tsx or site-chrome.tsx primitives — Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows; no task-driven override). Picked at T2 M1 L2 I2 A3 D3 = 13/18 over chapter-cta-pill-1689 @ 12/S (hot-surface finding — register-pairs with cart-drawer CTA but lower I-impact than 12-page journal sweep), about-from-the-press-orphan-not-a-card @ 12/M (about just shipped same day — surface-freshness tiebreaker against), press-strip-stock-row-of-five @ 11/M. Cold-surface tiebreaker also favoured journal (11+ ships since last touch per historian's surface-freshness). Strategy chose NOT to modify high-risk shared primitives — instead created NEW server-component <JournalFolio> rendered inside main on each journal route via discriminated union (variant=index → '§ III · The Journal' + 'pp. I – XI · MMXXVI'; variant=post → '§ III · Piece <roman>' + 'p. <roman> / XI · MMXXVI'). Reuses every .folio-* class + both keyframes (folio-rise 700ms, folio-typeset 540ms) + reduced-motion guard at globals.css:4781-4787 — zero new motion vocabulary. JournalPostFrame gained `total: number` prop derived from getAllPosts().length in /journal/[slug]/page.tsx; romans from existing romanNumeral() helper in src/lib/journal.ts. CSS delta: +22 lines net at globals.css:~4791 (.folio[data-variant='journal'] .folio-piece-num + .folio-sep-fraction/range italic-serif 14px 0.6 opacity). Five files: NEW src/components/journal-folio.tsx (~65 LOC server component, no 'use client'), src/app/journal/page.tsx (+2 lines), src/components/journal-post-frame.tsx (+3 lines, accept total prop), src/app/journal/[slug]/page.tsx (+1 line, derive total), src/app/globals.css (+22 lines). Verified — bun run lint 0 errors + 7 pre-existing tooling warnings (unchanged set in .claude/improvement/scripts/*.mjs), bunx tsc --noEmit clean, bun run build 48/48 routes (Turbopack 1169.9ms), 0 anti-patterns. SSR class-grep: /journal renders 1× <aside class='folio' data-variant='journal'> + 2× folio-edge + 'The Journal' + 'I' + 'XI' + 'pp.' + 'MMXXVI'; sample /journal/against-the-saas-template renders 1× journal-variant folio with 4× 'Piece ' + 8× 'p.' + 3× ' / '; homepage / unchanged (1× <aside class='folio'> with NO data-variant); /about /privacy /terms /cookies render 0× .folio (correctly out of scope); all 6 PDPs render 0× .folio inside main.pdp (main.pdp ~ .folio selector still binds nothing — no regression). perf-a11y PASS — 0 bytes JS delta (server component), ~+0.5KB gzipped CSS (22 lines of inert typography), no LCP risk (folio is position:fixed bottom:16px, gated @media min-width:900px, not above-the-fold), no CLS risk (out-of-flow), no INP risk (zero listeners), keyboard parity intact (aria-hidden + inherited pointer-events:none), reduced-motion complete via existing guard (no new keyframes), contrast — main folio text rgba(255,255,255,0.55) under mix-blend-difference on matte-black ≈ 18:1 AAA-clear, new italic-serif separators ('/' and '–') at rgba(255,255,255,0.7) × opacity 0.6 ≈ 4.3:1 under difference blend (below AA 4.5:1 for text but separators are aria-hidden decorative — not a WCAG text-contrast failure). regression-spotter PASS — homepage's chapter-driven RunningFolio identical (no data-variant), journal index has 1× journal-variant folio + 2× folio-edge as designed, all 11 /journal/<slug> render post variant with correct piece roman, /about /privacy /terms /cookies have 0× folio, PDPs unchanged. diff-reviewer PASS-WITH-NITS — +56 / -1 LOC under 80 budget, no any/@ts-ignore/console.*/!important, no scope creep, discriminated union sound; 2 low-severity nits — (a) .folio-piece-num rule is no-op (italic inherited, lnum on Roman is null) — kept for future tuning per spec but flagged speculative, (b) inline 14px + rgba(255,255,255,0.7) on new fraction/range separators not tokenised (matches established .folio convention; rolls into folio-color-token-promotion sweep). Risk band: low per risk-rules.md — no shared primitive touched (running-folio.tsx + site-chrome.tsx unchanged), 5 files, ≤80 LOC, no new client boundary, no SEO/JSON-LD edit, no design-token block edit. Direct-commit to main per low-band default. Screenshots skipped — capture-ship.mjs failed with 'server did not become ready within 30s'; carry-over blocker now 8 ships old (severity promoted to high in shipped.yaml notes — no backlog item exists for it yet, historic documentation gap). SOTD compare skipped — sotd-compare.mjs exited 'skipped: could not parse SOTD entry — gallery markup may have changed'; sotd_parser_available:false carry-over. /review skill skipped per established pattern — focused new-component low-risk ship with PASS verdicts across all 5 Phase 5 gates and SSR-verified scoping on 14 routes (1 homepage unchanged + 1 journal index + 11 posts + 4 legal/about unchanged + 6 PDPs unchanged); review value would be re-reading the same diff. Notion writeback: no task to flip (no override); Reports row 361af8d3-d3e2-8190-abf6-c6c67e8a97a3 appended via MCP API-post-page first attempt with Surface=chrome (single-select; closest schema option — folio is bottom-edge masthead chrome), Mode=Shipped, Rubric T2 M1 L2 I2 A3 D3 = 13 / 18, Risk=Low, Commit=f14fc01, all 8 follow-ups listed. Schema-fix pattern from prior runs already absorbed: Surface single-select with options [hero, catalogue, manifesto, field-notes, codex, colophon, faq, outro, chrome, nav, cart, 404, system, seo]; Files field name is 'Files changed'; Follow-ups exact field name. Closed-by-drift this run: none. Housekeeping: none — backlog received 5 new follow-up entries (journal-folio-piece-num-speculative-selector, journal-folio-fraction-range-sep-hardcoded-colors, journal-folio-about-route-variant, journal-folio-legal-route-variants, journal-folio-scroll-bound-piece-swap-on-index) plus shipped.yaml entry. Follow-ups not in backlog yet but tracked in shipped.yaml notes: stale-next-server-on-3000-blocks-capture-ship (high, infra — should be promoted to a proper backlog item on next run; carry-over has grown from 3 → 8 ships without a fix attempt; the documentation gap is real), sotd-parser-fix (low, infra carry-over), lighthouse-baseline-seed (low, infra carry-over).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 57 — not a multiple of 10, calibrator does NOT fire. Next eligible at shipped_count=60 IF today − last_calibration_at >= 7 days at that point.

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok; push to origin/main succeeded (commit f14fc01: 4ec4398..f14fc01)
playwright_available: unknown  # capture-ship.mjs skipped — port 3000 still held; carry-over 8 ships now (promoted severity to high in this run's shipped.yaml notes)
lighthouse_available: unknown  # perf-a11y skipped lighthouse — CSS-only + below-the-fold + no prior baseline; signal-to-cost poor for this band
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 512000   # +17000 bytes for this run's IMPROVEMENTS.md entry (journal-folio-on-journal-routes); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy. Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 rows. Reports notion-post-page succeeded on first attempt this run (no schema-fix iteration needed — Surface=chrome single-select valid, Files changed field name correct, Follow-ups field name correct). Reports row 361af8d3-d3e2-8190-abf6-c6c67e8a97a3 created with all 10 schema fields populated.
