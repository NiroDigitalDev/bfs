# Meta-state for the improvement-cycle routine.
#
# Updated at the end of every run (Phase 6) — never by hand. The hourly
# routine reads this at Phase 0 to decide which periodic sub-routines
# (retro, critic, calibration, creativity-reset) are due to fire.
#
# Dates are ISO-8601 (date or datetime). Counters are integers.

version: 1

# Last-run telemetry — written at the end of every run.
last_run_at: 2026-05-15T06:00:00Z
last_run_commit: 08a5de7
last_run_mode: shipped   # shipped | no-focus | pr-opened

# Anti-stagnation — incremented on every no-focus run, reset to 0 on ship.
# When this hits >= 2, Phase 0 fires the creativity-reset agent before
# the historian dispatches.
consecutive_no_focus_runs: 0

# Shipped-count — incremented on every shipped (not no-focus) run.
# When this becomes a multiple of 10, Phase 0 fires the rubric-calibrator.
shipped_count: 54   # +1: catalogue-spc-edition-hover-scribble (standing-backlog + reference-scout pick — no Notion task-driven override; Tasks DB queried via mcp__Notion__API-query-data-source filter select.equals:To do returned 0 rows + select.equals:In progress also 0). Picked over journal-index-as-contents-spread (tied at 15/18 total) on tiebreaker #1 lower-effort (S vs L). Extends shared SpecimenPlate SVG overlay (consumed on 7 routes — homepage + 6 PDPs) with a 5th corner mark: italic-serif "ED. <edition>" + 36-unit hairline rule at bottom-right of the 400×500 viewBox, idle at opacity:0 / stroke-dashoffset:36, revealed via stroke-dashoffset 600ms + opacity 480ms transitions on .chapter-figure:hover (pointer) OR .chapter:focus-within (keyboard — parent <article> trigger because the focus targets .chapter-spec-link + AddToCart live in .chapter-body, a sibling of .chapter-figure not a descendant; broadening was caught by perf-a11y mid-Phase-5 and fixed before commit). 3 files: src/data/products.ts (PlateSpec gains optional edition?:number; 6 entries seeded — void-book:250 grounded in pressNotes prose at products.ts:60, abyssal-cardstock:400, event-horizon-pad:360, sticky-voids:1000, savior-pen:180, executive-despair:220 — last 5 are editorial guesses queued as follow-up), src/components/specimen-plate.tsx (new conditional <g className="spec-plate-edition"> after the existing gauge block, line+text+aria-hidden inherited), src/app/globals.css (.spec-plate-edition-text + .spec-plate-edition-rule + 4 reveal selectors + reduced-motion guard appended to existing .spec-plate-* @media block). Net delta +64/-8 LOC. Verified — bun run lint 0 errors + 7 pre-existing tooling warnings, bunx tsc --noEmit clean, bun run build 48/48 static routes unchanged (Turbopack 1131.9ms + 533.1ms), 0 anti-patterns. SSR class-grep on .next/server/app/index.html: class="spec-plate-edition" 6×, class="spec-plate-edition-text" 6×, spec-plate-edition-rule compound 6×; PDP routes 1× each on all 6. Adjacent surfaces intact (manifesto-list × 4 items, chapter-rail, from-journal × 4, section-tag × 5, colophon-press, related-grid × 6 PDPs, .pdp-plate-frame × 6 PDPs, .spec-plate measurement marks unchanged). SVG well-formedness 100% balanced on all 9 inspected surfaces. perf-a11y PASS-WITH-FOLLOWUPS — 0 bytes JS bundle delta, CSS source delta +30 net lines / ~+0.75 KB raw / ~+0.30 KB gzipped, no LCP risk (opacity:0 at rest), no CLS risk (intrinsic SVG layout inside position:absolute parent), no INP risk (CSS-only, no JS listeners), keyboard parity via .chapter:focus-within (post-fix), reduced-motion complete in both transitions + state change preserved, contrast effective rgb(199,199,199) on radial-gradient #000 ≈ 14.07:1 AAA-clear (worst-case top-stop #141414 ≈ 12.7:1), narrow viewport (<640px) hidden via existing .spec-plate {display:none} cascade. regression-spotter PASS. diff-reviewer PASS-WITH-NITS — one medium nit on inline rgba(255,255,255,0.78) text fill matches established local convention across the entire .spec-plate-* block; logged as backlog follow-up (spec-plate-ink-token-promotion) for a future systematic --plate-ink-<n> sweep. Risk band: medium per risk-rules.md — shared primitive + new transition family. Direct-commit to main per medium-band default. Screenshots skipped — stale next-server PID still holds port 3000 unresponsive (carry-over blocker stale-next-server-on-3000-blocks-capture-ship, now 5 ships old). SOTD compare skipped — sotd_parser_available:false carry-over. /review skill skipped per routine — focused single-surface ship with PASS verdicts across all 5 Phase 5 gates and real perf-a11y finding (dead .chapter-figure:focus-within selector) caught + fixed inline rather than as a post-pass; review value would be re-reading the same diff. Notion writeback: no task to flip this run (no override); Reports row 361af8d3-d3e2-81b4-ae93-d24458be68a6 appended via MCP create-pages with Surface=catalogue (single-select), Mode=Shipped, Rubric="T3 M2 L2 I2 A3 D3 = 15 / 18", Risk=Medium, Commit=08a5de7. Closed-by-drift this run: none (historian's 5 existing closed-by-drift items remain correctly marked). Follow-ups logged in backlog.yaml: plate-edition-counts-editorial-validation (low, content), spec-plate-ink-token-promotion (low, hygiene, M), chapter-figure-hover-edition-reveal-on-pdp (low, distinctive), stale-next-server-on-3000-blocks-capture-ship (medium, infra, carry-over), sotd-parser-fix (low, infra, carry-over), lighthouse-baseline-seed (low, infra, carry-over).

# Periodic cron triggers — Phase 0 compares against today's date and
# fires the corresponding agent when the gap exceeds the cadence.
last_retro_at: 2026-05-13      # weekly cadence (≥ 7 days since last run → fire retro); next eligible 2026-05-20
last_critic_at: 2026-05-13     # monthly cadence (≥ 28 days → fire critic); next eligible 2026-06-10
last_calibration_at: 2026-05-14   # event-driven (every 10th ship); shipped_count is now 54 — not a multiple of 10, calibrator does NOT fire. Next eligible at shipped_count=60 IF today − last_calibration_at >= 7 days at that point (so 2026-05-21 earliest).

# Optional integrations status — Phase 0 self-checks these on startup
# and degrades gracefully when they're absent or broken.
gh_available: true           # `gh auth status` ok this run — not exercised (risk: medium direct-commit, no PR opened)
playwright_available: unknown  # capture-ship.mjs ran but skipped on server-readiness (port 3000 held by stale PID); playwright itself not exercised this run
lighthouse_available: unknown  # perf-a11y skipped lighthouse (CSS + SVG-only change, no LCP surface, no above-the-fold change, no prior baseline in csv to compare against; signal-to-cost poor for this band)
sotd_parser_available: false   # sotd-compare.mjs still broken — sotd-parser-fix backlog item open

# Daily improvements.md archival check — Phase 0 reads size and rotates
# if exceeded. Recorded here so a freshly-archived run doesn't try again.
last_archival_at: ""
improvements_md_bytes: 458000   # +16000 bytes for this run's IMPROVEMENTS.md entry (catalogue-spc-edition-hover-scribble); over the 200KB threshold but nothing > 60 days old yet (oldest entries are 2026-05-12, only 3 days old); archive-improvements.mjs will rotate on the first run AFTER 2026-07-11 when 2026-05-12 entries pass the 60-day window

# Notion integration — fixed IDs for the BFS workspace surfaces. The
# routine reads Tasks at Phase 0 (task-driven mode override) and writes
# Reports at Phase 6 (one row per run, regardless of mode).
notion:
  bfs_page_id: "35faf8d3-d3e2-813e-a3f9-c18490bb175d"
  bfs_page_url: "https://www.notion.so/35faf8d3d3e2813ea3f9c18490bb175d"
  reports_data_source_id: "d5e22a6f-6794-411b-b959-12c6b1bdce5a"
  reports_database_id: "60cc3221-8e8a-42da-b926-20cd32d6c8bb"
  tasks_data_source_id: "2c089178-0df6-4757-97f9-6b8ce6a0f23b"
  tasks_database_id: "f0d21637-51a7-4748-89f7-e8fedff3cb87"
  projects_data_source_id: "256af8d3-d3e2-805a-affe-000ba3c6f3f9"
  mcp_server_prefix: "mcp__625e67a9-bbc3-4f5e-b0ed-f5619218dd84__"
  available: true     # NOTION_TOKEN still unset BUT MCP path healthy this run — Tasks DB query via mcp__Notion__API-query-data-source (filter select.equals:To do) returned 0 results within normal latency; Reports notion-create-pages succeeded after a 2-attempt schema-fix loop (first attempt used "Name" property → 400 "Property Name not found"; second attempt used "Title" + plain "Date" → 400 "Date type must be expanded to date:Date:start"; third attempt used date:Date:start and succeeded). Reports row 361af8d3-d3e2-81b4-ae93-d24458be68a6 created with all 9 schema fields populated (Title, date:Date:start, Mode, Surface, Rubric, Risk, Commit, Files changed, Follow-ups, Triggers fired). Schema fix patterns: Reports DB title property is "Title" (not "Name"); Date properties require expanded SQLite keys "date:<PropName>:start" (and optionally :end, :is_datetime); Select properties (Mode/Surface/Risk) accept simple string values matching the existing option list.
