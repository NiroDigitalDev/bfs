# Improvement-cycle backlog — source of truth for open work.
#
# IMPROVEMENTS.md is the prose history. This file is the working state.
# Every run: the historian VERIFIES each `open` item against current code
# before reporting. Items found "no longer real" are flipped to
# `status: closed-by-drift` with a verified-on date — never silently
# dropped. New follow-ups uncovered by a ship are appended here, not
# only buried in the prose log.
#
# Status values:
#   open               — still real, eligible to be picked as a focus
#   in-progress        — being shipped in the current run (transient)
#   shipped            — completed; should also appear in shipped.yaml
#   closed-by-drift    — verified as no longer real (already fixed
#                        elsewhere, removed, or scope evaporated)
#   wontfix            — explicitly declined; record the reason
#
# Severity: critical | high | medium | low
# Effort:   S (≤1h) | M (1–3h) | L (>3h)
# Class:    hygiene | a11y | seo | perf | distinctive | system
#
# Schema per item:
#   id:            unique kebab-case slug
#   title:         short imperative
#   ship:          one-line description of the concrete deliverable
#   severity:      …
#   effort:        …
#   class:         …
#   status:        …
#   files:         [list of file:line anchors as of opened_on]
#   opened_on:     ISO date
#   last_verified: ISO date (set by historian)
#   notes:         optional free text

version: 1
items:
  - id: index-menu-mobile-toc
    title: Editorial Index overlay replaces missing mobile navigation
    ship: |
      Italic-serif "Index" trigger in the nav (< 900px) opens a full-bleed
      Table of Contents overlay with dot-leader entries and folio page
      numbers matching the chapter-rail vocabulary. Focus-trapped dialog,
      scroll lock, scrim, esc, reduced-motion compliance — mirrors
      cart-drawer plumbing.
    severity: high
    effort: M
    class: distinctive
    status: shipped
    files:
      - src/components/index-menu.tsx
      - src/app/page.tsx:47-60
      - src/app/globals.css:367-374
    opened_on: 2026-05-13
    last_verified: 2026-05-13

  - id: index-menu-scroll-lock-leak
    title: IndexMenu can leak body.overflow on unmount-while-open
    ship: |
      Add an unmount cleanup to src/components/index-menu.tsx that always
      clears document.body.style.overflow regardless of `open` state, so a
      remount or hot-reload while the menu is open doesn't strand the lock.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/components/index-menu.tsx:29-72
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: Found by perf-a11y on ship of index-menu-mobile-toc.

  - id: dialog-scroll-lock-shared
    title: Centralise body.overflow lock between IndexMenu and CartDrawer
    ship: |
      Both dialogs set/clear `document.body.style.overflow` directly. If
      both are open at once, the second one closing strips the lock the
      other still needs. Replace with a counted helper (lib/scroll-lock.ts
      with lock/unlock pair) so only the last-to-close clears the body
      style.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/components/index-menu.tsx:30-33
      - src/components/cart-drawer.tsx:89-98
    opened_on: 2026-05-13
    last_verified: 2026-05-13

  - id: index-menu-focus-ring
    title: IndexMenu links missing site .focus-ring token on :focus-visible
    ship: |
      Index-menu link, num, folio, close, and trigger all rely on color-only
      :focus-visible. The site's .focus-ring token (used by .nav-cta etc.)
      should also be applied to .index-menu-link:focus-visible for keyboard
      visibility parity with the rest of the chrome.
    severity: low
    effort: S
    class: a11y
    status: open
    files:
      - src/app/globals.css:417-712
    opened_on: 2026-05-13
    last_verified: 2026-05-13

  - id: newsletter-reset-path
    title: Newsletter form locks forever after submit
    ship: Auto-reset submitted state after ~4s or add explicit "Send another" affordance
    severity: high
    effort: S
    class: hygiene
    status: open
    files:
      - src/components/newsletter.tsx:6-37
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      disabled={submitted} is set true on submit and never cleared.
      Returning visitors can't re-submit.

  - id: contact-surface
    title: No contact surface above the colophon
    ship: |
      Add a dedicated short contact slot mid-site (after manifesto)
      OR raise studio email into the nav. Decide per ship.
    severity: high
    effort: M
    class: hygiene
    status: open
    files:
      - src/app/page.tsx:638-648
      - src/app/page.tsx:674-680
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: Currently buried ~95% down the page in colophon and outro nav only.

  - id: og-image-serif-embed
    title: OG image still falls back to ui-serif
    ship: Embed Instrument Serif as base64 buffer in opengraph-image.tsx
    severity: medium
    effort: M
    class: distinctive
    status: open
    files:
      - src/app/opengraph-image.tsx
    opened_on: 2026-05-13
    last_verified: 2026-05-13

  - id: apple-icon-manifest
    title: Missing apple-icon.tsx + manifest.ts
    ship: |
      Add Next.js 16 programmatic apple-icon route + manifest route so
      iOS / Android home-screen pins use the dark BFS mark.
    severity: low
    effort: M
    class: hygiene
    status: open
    files:
      - src/app/
    opened_on: 2026-05-13
    last_verified: 2026-05-13

  - id: navcart-affordance
    title: NavCart button reads visually like a link
    ship: Add hover/focus cue distinguishing the cart button from <a> siblings
    severity: low
    effort: S
    class: a11y
    status: open
    files:
      - src/components/cart-island.tsx:82-96
    opened_on: 2026-05-13
    last_verified: 2026-05-13

  - id: lighthouse-baseline
    title: No Lighthouse baseline measured
    ship: |
      Run Lighthouse against the deployed homepage, capture LCP / CLS /
      INP / a11y / SEO / best-practices, commit a baseline CSV under
      .claude/improvement/lighthouse.csv. Subsequent runs append.
    severity: medium
    effort: M
    class: perf
    status: open
    opened_on: 2026-05-13
    last_verified: 2026-05-13

  # ── Reference-scout typographic / motion backlog ──
  # These are distinctive moves identified by the scout but not yet
  # shipped. The scout reads this section each run to know what NOT to
  # re-suggest, and to know what's already on deck for picking.

  - id: optical-axis-scroll
    title: Variable-serif optical-size axis swap on scroll
    ship: |
      Pair Instrument Serif with a variable companion (Fraunces opsz)
      on the hero masthead only; bind opsz to scroll velocity so the
      title tightens on settle and loosens on fast scroll.
    severity: medium
    effort: L
    class: distinctive
    status: open
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: Reference — Family New York. Risk — variable font payload + CLS.

  - id: live-kerning-on-hover
    title: Live kerning / ligature swap on hovered display words
    ship: |
      On hovered display words (hero, chapter titles), swap to stylistic
      alternates and tighten letter-spacing within a 200px pointer
      radius. Once-on-enter swash reveal as the mobile equivalent.
    severity: medium
    effort: M
    class: distinctive
    status: open
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: Reference — Bureau Borsche. Risk — sub-pixel jitter; touch fallback.

  - id: errata-layer
    title: Sitewide errata layer (struck-through corrections, baseline-break)
    ship: |
      A few words per chapter rendered with <del>/<ins> corrections,
      baseline-shift errata, or margin pencil notes revealed on
      scroll-into-view. Editorial flourish that deepens the
      "proof being corrected" conceit.
    severity: medium
    effort: M
    class: distinctive
    status: shipped
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Shipped 2026-05-13 (commit af0c8bb). Three placements added on
      the homepage: catalogue section-lede ("Stocked when stocked." →
      "Bound when bound."), codex section-lede ("None new." → "None
      borrowed."), manifesto credo ("brand" → "press"). Semantic
      <del>/<ins> markup + "§ corr." marginalia (aria-hidden), all
      hover/reduced-motion guarded. Reference — Pentagram editorial /
      Marina Willer. Follow-ups: extend grammar to .journal-prose
      (errata in long-form journal posts); evaluate baseline-break
      flourish on chapter eyebrows as a separate move.

  - id: view-transitions-page-turn
    title: View Transitions API page-turn between chapters
    ship: |
      Use the View Transitions API to make chapter-to-chapter movement
      read as a literal page-turn (spine pivot, folio flips, marginalia
      cross-fade). Non-VT fallback re-typesets folio only;
      prefers-reduced-motion falls back to a hard cut.
    severity: medium
    effort: L
    class: distinctive
    status: open
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Reference — Locomotive. Highest single-move impact in the scout's
      backlog but also the biggest. Save for a ship with the headroom.

  - id: hero-type-instrumental
    title: Make hero type behave — variable-axis + italic-roman swap + ligature alternates
    ship: |
      Promote "Dark Matter." from set to typeset. Variable-axis grade
      tied to scrollY, italic-roman swap on pointer-over the wordmark,
      ligature alternates cycling on a slow loop. SplitText already
      handles per-char; the work is in the per-char interaction state
      machine.
    severity: high
    effort: L
    class: distinctive
    status: open
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — 2026-05 critic ("hero is the only display moment, and
      it is conservative"). Reference — Bureau Borsche. Strongest
      single-surface lift on Awwwards rubric.

  - id: catalogue-spread-asymmetry
    title: Break every third chapter into a full-bleed double-truck spread
    ship: |
      Replace uniform even/odd `data-orientation` with single, single,
      spread, single, single, spread. Spread = left 4-column body with
      gutter marginalia, right full-bleed plate. Catalogue stops being
      a column pretending to be a spread.
    severity: high
    effort: L
    class: distinctive
    status: open
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — 2026-05 critic ("layout still respects the grid too
      much"). file:line page.tsx:202, 386, 448, 530, 587 — six
      identical section-head preambles to break.

  - id: motion-vocabulary-beyond-reveal
    title: Per-surface motion vocabulary — Reveal stops being the answer to everything
    ship: |
      Replace Reveal-as-default-section-enter with surface-specific
      motion: catalogue scroll-bound page-turn between chapters,
      manifesto holds masthead while list slides under, codex
      re-typesets numerals on enter. Motion becomes navigation,
      not decoration.
    severity: medium
    effort: L
    class: distinctive
    status: open
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — 2026-05 critic ("motion is a vocabulary of one word:
      reveal"). Reference — Locomotive. Overlaps with the existing
      `view-transitions-page-turn` backlog item — likely consolidate.

  - id: hero-period-will-change-cleanup
    title: Drop `will-change: opacity` from .hero-period once effect is stable
    ship: |
      The hero-period scroll-fade (shipped this run) declared
      `will-change: opacity` to hint the compositor. `will-change` is
      best applied transiently — keeping it set permanently can pin
      the element to its own layer indefinitely and waste memory.
      Once the effect proves stable across browsers, either remove
      the declaration or scope it behind a hover/scroll-start trigger.
    severity: low
    effort: S
    class: perf
    status: open
    files:
      - src/app/globals.css:814
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — perf-a11y agent follow-up on the hero-period-scroll-fade
      ship (commit pending). Non-blocking observation. Watch a few runs
      with the fade live, then drop the hint if no compositor jank
      surfaces.

  - id: skip-link-main-landmark
    title: Add skip-link + `<main>` landmark — first Tab target is currently a decorative element
    ship: |
      In `src/app/layout.tsx`, prepend a `<a class="skip-link"
      href="#main">Skip to content</a>` as the first body child, wrap
      `{children}` in `<main id="main" tabIndex={-1}>`, and add a
      `.skip-link` rule in globals.css using the existing focus-ring
      token (off-canvas by default, visible on focus). Closes a real
      WCAG hole — current first Tab on every route lands on
      `<Cursor />` / `<Loader />` / `<ScrollProgress />` decoratives.
    severity: high
    effort: S
    class: a11y
    status: open
    files:
      - src/app/layout.tsx:171-205
      - src/app/globals.css
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — motion-a11y auditor run on 2026-05-13. Scored only 8/18
      on the rubric (T1 M0 L1 I2 A3 D1) so it lost the focus race this
      run; held as a high-severity hygiene item because A jumps from
      1 → 3 and clears the disqualification gate. Pick this on a
      future low-rubric-quorum run or pair it with another a11y move.

  - id: magnetic-tilt-mq-subscription
    title: Magnetic + Tilt — subscribe to matchMedia change events and reset transforms on bail
    ship: |
      Magnetic (`src/components/magnetic.tsx:17-18`) and Tilt
      (`src/components/tilt.tsx:17-18`) read `(pointer: fine)` and
      `(prefers-reduced-motion: reduce)` once at mount. If the user
      toggles reduced-motion or hot-plugs a trackpad, the existing
      effect doesn't re-run and any `el.style.transform` set by the
      last pointer event sticks. Fix — subscribe to both
      `matchMedia(...).addEventListener("change", ...)` in the effect;
      on bail, clear `el.style.transform = ""` and the `--mx/--my`
      custom properties.
    severity: medium
    effort: S
    class: a11y
    status: open
    files:
      - src/components/magnetic.tsx:17-18
      - src/components/tilt.tsx:17-18
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — motion-a11y auditor 2026-05-13. Real correctness edge
      case (mid-session media-query flip) but very rare in practice.
      Score 9/18 — low rubric pick but cheap, isolated, and a
      defensible "do it because it's right" hygiene item.

  - id: hairline-opacity-token-sweep
    title: Tokenize hairline opacities — 0.32 / 0.42 / 0.55 used interchangeably across globals.css
    ship: |
      Introduce three new tokens (`--color-line-faint: rgba(255,255,255,0.32)`,
      `--color-line-mid: 0.42`, `--color-line-strong: 0.55`) in the
      :root token block; sweep `src/app/globals.css` replacing literal
      `rgba(255,255,255,0.32–0.55)` hairline / muted-ink uses with the
      token. Zero visible delta if the rounding stays in band.
    severity: low
    effort: M
    class: hygiene
    status: open
    files:
      - src/app/globals.css
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — motion-a11y auditor 2026-05-13 (found ~17 literal sites
      across hairline-heavy surfaces — codex, spec-plate, related grid,
      folio). Existing tokens `--color-text-muted` / `--color-text-faint`
      cover ink but not line. Single-PR sweep, no behavioural change.

  - id: outro-anchor-normalize
    title: Normalize outro nav anchors to `/#anchor` form
    ship: |
      `src/app/page.tsx:690-708` uses bare hash anchors (`#supplies`,
      `#manifesto`, `#cult`, `#faq`). On non-`/` routes these resolve
      to nothing. Switch to absolute `/#anchor` form so any future
      cross-route mount (footer/SiteChrome expansion) works.
    severity: low
    effort: S
    class: hygiene
    status: closed-by-drift
    files:
      - src/components/site-footer.tsx
    opened_on: 2026-05-13
    last_verified: 2026-05-14
    notes: |
      Source — colophon auditor 2026-05-13. Outro currently only
      renders on `/` so latent until the IA grows; cheap insurance.
      Closed by drift 2026-05-14 — the `site-footer-extract-mount-all-routes`
      ship moved the outro nav into `src/components/site-footer.tsx` and
      converted every `#supplies/#manifesto/#cult/#faq` anchor to
      `/#supplies` etc. (and the magnetic 'Return to the catalogue'
      button) as a natural consequence of the cross-route mount. No
      separate ship needed.

  - id: colophon-edition-token-sweep
    title: Source Edition / Lat-Lon / Year from `site.ts` across colophon, 404, OG image
    ship: |
      `src/lib/site.ts:17` already exports `site.edition = "Edition III
      · MMXXVI"` and related publisher fields, but three surfaces
      retype the string by hand: `src/app/page.tsx:610-612`,
      `src/app/not-found.tsx:130-132`, `src/app/opengraph-image.tsx:36`.
      Drift risk on the next edition bump. Also: add a missing
      "Edition" row to the colophon dl (`page.tsx:642-678`) so the
      live value is read in the masthead register.
    severity: medium
    effort: S
    class: hygiene
    status: open
    files:
      - src/lib/site.ts:17
      - src/app/page.tsx:610-612
      - src/app/not-found.tsx:130-132
      - src/app/opengraph-image.tsx:36
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — colophon auditor 2026-05-13. Single-S-effort move that
      touches three surfaces and adds a publisher-mark row.

  - id: press-disclaimer-aa-bump
    title: `.press-disclaimer` at 10px / 0.35 alpha fails WCAG AA — bump to 0.55 or 12px
    ship: |
      `src/app/globals.css:2231-2233` declares `.press-disclaimer` at
      10px and `rgba(255,255,255,0.35)` over `#050505` — contrast
      ≈ 3.5:1, fails AA body-copy (needs 4.5:1). Bump color to
      `rgba(255,255,255,0.55)` (≈ 6.7:1) OR raise to 12px (which would
      shift the threshold to the 3:1 large-text rule but the alpha is
      still tight).
    severity: medium
    effort: S
    class: a11y
    status: open
    files:
      - src/app/globals.css:2231-2233
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — motion-a11y auditor 2026-05-13. Real AA violation but
      tiny copy is currently a decorative disclaimer — pair with the
      next a11y-themed ship.

  - id: use-active-chapter-shared-observer
    title: ChapterRail + RunningFolio instantiate two IntersectionObservers
    ship: |
      Both `src/components/chapter-rail.tsx` and
      `src/components/running-folio.tsx` call `useActiveChapter()`,
      each spinning up its own IntersectionObserver on the same five
      chapter elements. Lift the observer behind a shared
      `useSyncExternalStore` (or a small singleton in
      `src/lib/use-active-chapter.ts`) so one observer feeds both
      consumers. Premature at n=2 consumers but the natural cleanup
      after the chapter-rail-scrollbound-fill ship lifted ratios into
      React state.
    severity: low
    effort: S
    class: perf
    status: open
    files:
      - src/lib/use-active-chapter.ts:18-46
      - src/components/chapter-rail.tsx:7
      - src/components/running-folio.tsx:7
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — perf-a11y on ship of chapter-rail-scrollbound-fill.
      Was tolerable pre-ship because the second observer only set
      activeId state; now both consumers also receive the ratios
      map object on every IO tick, doubling render churn.

  - id: chapter-rail-geometry-tokens
    title: Tokenize chapter-rail width / opacity literals
    ship: |
      The rail rule width range (14px / 32px / 18px delta) and
      opacity ramp (0.45 / 0.55) live as literals in
      `src/app/globals.css:3728-3776`. Promote to local custom
      properties at the `.chapter-rail-rule` rule (or to root tokens
      if any other rail-style chrome ever joins it) so the
      reduced-motion fallback and the calc() draw don't repeat
      magic numbers.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/app/globals.css:3728-3776
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — diff-reviewer on ship of chapter-rail-scrollbound-fill.
      Not blocking; pure tidy-up.

  - id: manifesto-item-as-sans-card
    title: Re-typeset CH. 03 / Manifesto items off the Inter-800 caps card pattern
    ship: |
      Manifesto's four items still render as uppercase Inter-800 cards
      with a tracked numeral. The sticky left rail (`.manifesto-sticky`,
      globals.css:2148+) sets a stage for re-typesetting on scroll but
      nothing happens. Recast `.manifesto-item h3` in `--font-serif`
      italic display, scroll-bind hue / translate / weight axis on the
      sticky title so it re-typesets between items as they pass through
      the read band. Closest peer ships: codex italic numerals + colophon
      mark + field-notes pull-quotes.
    severity: high
    effort: M
    class: distinctive
    status: open
    files:
      - src/app/page.tsx:464-484
      - src/app/globals.css:2148-2200
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — cold-surfaces auditor on press-clipping-register run.
      The manifesto is the last "agency three-card pattern" left on the
      cold half (CH. 03 — load-bearing narrative arc). Rubric T3 M2 L3
      I2 A3 D3 = 16/18, picked-second in this run on tied-D-lower-effort.

  - id: codex-row-active-microstate
    title: Codex rows reward only :hover — no scroll-band active state
    ship: |
      `.codex-row:hover .codex-rule { width: 100%; }` (globals.css:2075)
      is the only microstate the codex offers. The chapter-rail
      IntersectionObserver already runs on this surface; add a
      `data-active` flag mirroring `useActiveChapter`'s ratios so the
      row the running folio is naming also lifts its italic numeral
      weight / letter-spacing / opacity. Reduced-motion downgrade to
      opacity only.
    severity: medium
    effort: S
    class: distinctive
    status: open
    files:
      - src/app/globals.css:2075
      - src/components/codex.tsx
      - src/lib/use-active-chapter.ts
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — cold-surfaces auditor on press-clipping-register run.
      Pairs with use-active-chapter-shared-observer (open).

  - id: press-strip-real-press-citations
    title: Press clipping register currently cites fictional publications
    ship: |
      The five clippings in `.press-register` are placeholders ("Apollo
      Off-Hours", "The Reinhardt Review", "Vantablack Vogue", etc.).
      When BFS has real press to cite, swap the inline citation array
      in `src/app/page.tsx` for the real titles + Vol/№/p numbers and
      promote each `.press-clipping-name` to an `<a>` so the dormant
      `:focus-within` underline-draw becomes load-bearing.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/app/page.tsx:527-548
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — diff-reviewer on press-clipping-register ship. The
      register is structured to accept real citations without further
      CSS changes; just swap names + add hrefs.

  - id: add-to-cart-no-live-announcement
    title: AddToCart fires no polite live-region announcement on add
    ship: |
      `cart-island.tsx` flips `data-added` and bumps the off-screen
      `CartCount` digits but never announces "{productTitle} added —
      open cart" to AT users. Add an sr-only `role="status"` region
      adjacent to AddToCart that interpolates productTitle on each add.
    severity: high
    effort: S
    class: a11y
    status: open
    files:
      - src/components/cart-island.tsx:51-56
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — interactions auditor on press-clipping-register run.

  - id: checkout-form-floating-label-dead-selector
    title: Checkout floating-label sibling combinator silently never matches
    ship: |
      The `.checkout-input:focus ~ .checkout-label` rule
      (globals.css:5346-5350) targets a next-sibling label, but the
      JSX (`checkout-form.tsx:188-205`) renders the label BEFORE the
      input — `~` from input to label cannot match. Seven dead
      micro-interactions today. Either flip DOM order (label after
      input + flex `order: -1`) or switch the selector to
      `:focus-within > .checkout-label`.
    severity: medium
    effort: S
    class: distinctive
    status: open
    files:
      - src/app/globals.css:5346-5350
      - src/components/checkout-form.tsx:188-205
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — interactions auditor on press-clipping-register run.
      Cheapest craft-recovery: ~6 LOC repairs an obviously-intended
      micro-interaction across the entire checkout.

  - id: cart-line-qty-live-region-noise
    title: Cart-drawer per-line aria-live stacks redundant announcements
    ship: |
      `cart-drawer.tsx:242-248` renders one `aria-live="polite"` per
      line item with a redundant `aria-label`. With multiple items the
      cumulative "01 in cart, 02 in cart" stacks on every +/- click.
      Hoist a single polite live region per drawer (subtotal row or
      sr-only status) and drop the per-row `aria-live`.
    severity: medium
    effort: S
    class: a11y
    status: open
    files:
      - src/components/cart-drawer.tsx:242-248
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — interactions auditor on press-clipping-register run.

  - id: checkout-input-focus-ring-token-drift
    title: Checkout input :focus-visible uses --color-line-2 instead of --color-ring
    ship: |
      `.checkout-input:focus-visible` at globals.css:5326-5329 sets
      `outline: 1px solid var(--color-line-2); outline-offset: 4px;` —
      barely visible. The site's `.focus-ring` token and the
      `.checkout-radio-input:focus-visible` rule both use `--color-ring`
      at 2px. Same token drift the open `index-menu-focus-ring` flagged
      for the index. Align to `--color-ring` / 2px for parity.
    severity: medium
    effort: S
    class: a11y
    status: open
    files:
      - src/app/globals.css:5326-5329
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — interactions auditor on press-clipping-register run.

  - id: nav-cart-magnetic-class
    title: Replace `.nav > .magnetic` descendant selector with an explicit class
    ship: |
      The nav-cart-overlap-fix ship targets the cart's Magnetic wrapper
      via the bare descendant selector `.nav > .magnetic`. Today this
      is safe because the homepage nav contains exactly one Magnetic.
      But a future ship adding a second Magnetic into the nav (e.g. a
      magnetic on the nav-logo or on the index-trigger) would inherit
      `justify-self: end` + `display: inline-flex` accidentally. Pass an
      explicit `className="nav-magnetic-cart"` to the cart Magnetic in
      `src/app/page.tsx:66` and update the CSS rule accordingly.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/app/page.tsx:66
      - src/app/globals.css:347-352
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — diff self-review on nav-cart-overlap-fix run.

  - id: visual-diff-clip-to-band
    title: Visual-diff bails when page heights drift — clip to vertical band
    ship: |
      `scripts/visual-diff.mjs` rejects with `dimensions differ` whenever
      the prior and current full-page screenshots have unequal heights —
      meaning every ship that affects page height further down the page
      blocks nav / hero / chrome ships from getting a real delta number.
      Add a `--clip-top=<px>` or `--surface-band=<top:height>` argument
      and a corresponding crop step in `capture-ship.mjs` so nav-only
      ships compare a 200-px band rather than the full document.
    severity: low
    effort: M
    class: hygiene
    status: open
    files:
      - .claude/improvement/scripts/visual-diff.mjs
      - .claude/improvement/scripts/capture-ship.mjs
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — verification step on nav-cart-overlap-fix run. The diff
      reported `2952x38346 vs 2952x38388` desktop and `1173x46989 vs
      1173x48408` mobile — height drift from the PDP-quantity-selector
      ship affecting `/supplies/*` pages downstream.

  - id: sotd-parser-fix
    title: SOTD-compare script can't parse the gallery — re-selector pass
    ship: |
      `scripts/sotd-compare.mjs` has been emitting `could not parse SOTD
      entry — gallery markup may have changed` for several consecutive
      runs (`state.yaml` records `sotd_parser_available: false`). The
      gallery markup has likely shifted; re-investigate the awwwards
      SOTD HTML and update the selector chain.
    severity: low
    effort: M
    class: hygiene
    status: open
    files:
      - .claude/improvement/scripts/sotd-compare.mjs
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — verification step on nav-cart-overlap-fix run.

  - id: newsletter-id-collision
    title: Newsletter hardcodes id="email" — collides with checkout's useId() email
    ship: |
      `src/components/newsletter.tsx:20` uses `<input id="email">` which
      collides with the `<input id={emailId}>` (`useId()`) rendered by
      `CheckoutForm` (`src/components/checkout-form.tsx:196`). The
      newsletter sits in the footer on every route. Switch to `useId()`
      to match the rest of the codebase.
    severity: high
    effort: S
    class: a11y
    status: open
    files:
      - src/components/newsletter.tsx:20
    opened_on: 2026-05-13
    last_verified: 2026-05-13
    notes: |
      Source — interactions auditor on press-clipping-register run.

  - id: display-title-primitive-extract
    title: Extract <DisplayTitle> primitive consolidating hero / PDP / journal-post / journal-index titles
    ship: |
      The hero (`/`), PDP (`/supplies/[id]`), journal-post
      (`/journal/[slug]`), and journal-index (`/journal`) all render
      the same asymmetric two-word display-title composition (word 1
      solid, word 2 italic outline-stroked, SplitText reveals with
      staggered start). Each implements it independently with its own
      class prefix (`.hero-word-*`, `.pdp-word-*`,
      `.journal-post-word-*`, `.journal-display-word-*`). Extract a
      `<DisplayTitle text={...} outlineWord="first"|"second"|"none"
      scale="hero"|"chapter"|"index" />` primitive that consumes
      `SplitText`, owns the canonical word stack, and exposes a CSS
      module per scale. Three-route consumer refactor — keep the four
      existing class prefixes during the transition, then deprecate
      once consumers have migrated. Net delta likely ~200-300 LOC
      added + 4 CSS blocks consolidated.
    severity: low
    effort: M
    class: distinctive
    status: open
    files:
      - src/app/page.tsx:99-115
      - src/app/supplies/[id]/page.tsx:222-250
      - src/components/journal-post-frame.tsx:97-128
      - src/app/journal/page.tsx:101-119
      - src/app/globals.css:800-841,4995-5040,6459-6480,6093-6134
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Source — flagged by the journal-display-vocabulary-align task
      brief as an "extract if pattern stabilizes" follow-up.
      Pattern is now in 4 places — pattern is stable.

  - id: display-title-rgba-tokens
    title: Migrate raw rgba(255,255,255,…) literals in display-title outline rules to a shared --color-stroke-display token
    ship: |
      `.hero-outline`, `.pdp-outline`, `.journal-post-outline`, and
      `.journal-display-outline` each use raw rgba literals for the
      `-webkit-text-stroke` color (varying between
      `#f4f4f4`, `rgba(255,255,255,0.78)`, `rgba(255,255,255,0.1)`,
      `rgba(255,255,255,0.32)` etc). Define
      `--color-stroke-display-solid`,
      `--color-stroke-display-italic`, and consume across the four
      blocks. Companion to display-title-primitive-extract.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/app/globals.css:824-826
      - src/app/globals.css:4017-4018
      - src/app/globals.css:6477-6480
      - src/app/globals.css:6116-6119
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Source — diff-reviewer nit on journal-display-vocabulary-align run.

  - id: nav-consistency-supplies-journal
    title: /supplies and /journal navs diverge from homepage 5-item nav vocabulary
    ship: |
      After surface-journal-nav-and-homepage-block, the homepage nav
      carries 5 numbered items (01-05 inc. Journal). The /supplies/[id]
      nav at src/app/supplies/[id]/page.tsx:148-162 still carries the
      original 4 (01-04, no Journal). The /journal index nav at
      src/app/journal/page.tsx:80-93 and the /journal/[slug] nav at
      src/components/journal-post-frame.tsx:38-52 carry a different
      4-item layout (Journal first with a mid-dot, then 01-03 Catalogue/
      Position/Field Notes, NO On Record). Three different nav layouts
      across the site reads as inconsistency, not register variation.
      Decide whether to (a) lift a shared <SiteNav pathname=...> client
      component into chrome (cleanest — the deferred SiteChrome wrapper
      task would absorb this), or (b) update each route's nav inline to
      mirror the homepage's 5-item order with aria-current applied per
      route.
    severity: medium
    effort: M
    class: hygiene
    status: open
    files:
      - src/app/supplies/[id]/page.tsx:148-162
      - src/app/journal/page.tsx:80-93
      - src/components/journal-post-frame.tsx:38-52
      - src/app/page.tsx:50-66
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Source — surface-journal-nav-and-homepage-block ship surfaced this
      inconsistency. Pairs naturally with the deferred SiteChrome task
      (35faf8d3-d3e2-81f8-844a-cc87e0326f55).

  - id: from-journal-block-renders-1-item-currently
    title: "/from-journal homepage block renders 1 entry because only 1 post is seeded"
    ship: |
      The `.from-journal` block calls `getAllPosts().slice(0, 2)` but
      only `voliiiNo1` is in `src/data/journal/index.ts`. The block
      will automatically render 2 entries once the seeded post count
      reaches 2+. Direct dependency on the Notion task "Write 10 BFS-
      voice journal posts with SEO-tuned titles, descriptions, and
      Article JSON-LD" (Notion page 35faf8d3-d3e2-81e5-8b57-f446e4a5a226).
      No code fix required — track here so the AC #3 (>= 2 posts) is
      revisited automatically when that task lands.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/data/journal/index.ts
      - src/app/page.tsx:611-687
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Source — surface-journal-nav-and-homepage-block ship.

  - id: from-journal-block-no-fallback-empty-state
    title: "/from-journal block has no visible empty state if all posts are removed"
    ship: |
      Current code renders `null` (no markup at all) when
      `journalLatest.length === 0` so the homepage layout is fine but
      the absence is silent. Acceptable for the seeded-content case;
      reconsider if/when journal posts can be flagged
      `published: false` and dynamic empty states become a real concern.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/app/page.tsx:611-687
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Source — surface-journal-nav-and-homepage-block ship. Defensive
      backlog entry — not a current bug.

  - id: notion-create-pages-multiselect-csv-fix
    title: "notion-create-pages MCP rejects comma-separated multi-select Surface values"
    ship: |
      During Phase 0 task-splitting this run, the
      `mcp__625e67a9-...__notion-create-pages` call rejected a
      Surface value of `"system,seo,nav"` with: `Invalid multi_select
      value for property "Surface": "system,seo,nav". Value must be
      one of the following: ...`. The MCP layer is treating the
      comma-separated string as a single option key rather than
      splitting it. Workaround used this run: pass a single Surface
      string per subtask (`"system"` for [1/2], `"chrome"` for [2/2]).
      Real fix: either (a) call `notion-sync.mjs split-task` once
      NOTION_TOKEN is in env (the HTTP path correctly emits the multi_
      select array), or (b) escalate the MCP issue to whoever
      maintains the Notion MCP server so the SQLite-style "CSV-as-
      multi-select" syntax is supported. Until then, subtask Surface
      tagging is partial.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - .claude/improvement/scripts/notion-sync.mjs
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Source — legal-pages-privacy-terms-cookies split. Not a code
      bug in BFS — a workflow gap. Documented for the next run that
      hits Notion subtask splitting.

  - id: cookie-banner-subtask
    title: "[2/2] Cookie banner — sitewide consent UI + localStorage + layout mount + /cookies cross-link (Notion subtask of legal-pages)"
    ship: |
      Notion subtask 360af8d3-d3e2-81a8-a643-e546ec8038de
      (https://www.notion.so/360af8d3d3e281a8a643e546ec8038de) — the
      second half of the legal-pages parent task. Builds a
      `<CookieBanner />` client component mounted in `layout.tsx`
      with localStorage consent state and a slide-up motion gated
      on prefers-reduced-motion. Depends on this run's `/cookies`
      route. Will be picked up by a future task-driven cron run as
      the next-highest-priority To-do task.
    severity: medium
    effort: M
    class: feature
    status: shipped
    files:
      - src/components/cookie-banner.tsx
      - src/app/layout.tsx
      - src/app/globals.css
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Shipped 2026-05-14 — task-driven direct-to-main (medium risk).
      Sitewide chrome via useSyncExternalStore("unknown" server snapshot)
      so SSR renders nothing and the banner only mounts after client
      hydration reads the localStorage flag. Magnetic-wrapped buttons,
      hairline rules, italic-serif copy on a bottom-edge backdrop-blur
      strip at z-75 (above folio z-70, below cart-drawer z-80).
      Escape → declined. CustomEvent('bfs:cookie-consent-changed') and
      cross-tab `storage` listener both subscribed for future analytics
      hooks. Source — legal-pages-privacy-terms-cookies parent task.

  - id: cookie-banner-focus-ring-token
    title: CookieBanner buttons use inline outline rather than the shared .focus-ring token
    ship: |
      Swap globals.css:7325-7328 `.cookie-banner-btn:focus-visible {
      outline: 1px solid #fff; outline-offset: 3px }` for the project's
      shared `.focus-ring` token (or class) so the consent strip's
      focus state matches the rest of the chrome (cart drawer, nav,
      addToCart). Cosmetic consistency, not a a11y bug — the inline
      outline still works.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/app/globals.css:7325
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by perf-a11y on cookie-banner-subtask ship.

  - id: cookie-banner-ghost-border-contrast
    title: CookieBanner ghost-button border alpha 0.4 dips below 3:1 against the top of the gradient
    ship: |
      Bump `.cookie-banner-btn` border from rgba(255,255,255,0.4) to
      rgba(255,255,255,0.55) (globals.css:7294) so the non-text UI
      element clears WCAG 1.4.11 3:1 across the full
      rgba(0,0,0,0.94→0.55) gradient band, not just the dark bottom.
    severity: low
    effort: S
    class: a11y
    status: open
    files:
      - src/app/globals.css:7294
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by perf-a11y on cookie-banner-subtask ship. Borderline,
      not blocking — solid button next to it carries the primary
      affordance and is full-contrast.

  - id: cookie-banner-color-tokenize
    title: CookieBanner CSS uses raw #fff / #000 / rgba(...) literals instead of ink/paper tokens
    ship: |
      Replace #fff, #000, and rgba(255,255,255,*) / rgba(0,0,0,*)
      literals in the `.cookie-banner` block (globals.css ~7212-7365)
      with the project's existing ink/paper tokens (or define
      `--cookie-banner-bg` / `--cookie-banner-fg` once at the top of
      the block). Overlay alphas can stay literal — only the base
      colors need to resolve to tokens. Keeps the "tokens, not
      literals" discipline the rest of globals.css holds.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/app/globals.css:7212-7365
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by diff-reviewer on cookie-banner-subtask ship.

  - id: cookie-banner-zindex-token
    title: CookieBanner z-index 75 is a bare literal, not a layer token
    ship: |
      Promote `z-index: 75` (globals.css:7213) to a CSS custom property
      if the project has a layer-stack convention (the folio + cart
      drawer + chapter rail use 70/80/90 literals too — there's no
      shared --z-* token layer today, so this is a wider sweep rather
      than a one-line fix). Track here as a system follow-up.
    severity: low
    effort: M
    class: system
    status: open
    files:
      - src/app/globals.css:7213
      - src/app/globals.css:3272
      - src/app/globals.css:3947
      - src/app/globals.css:4418
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by diff-reviewer on cookie-banner-subtask ship.
      Sweep-shaped, not local — touches cart-drawer, chapter-rail,
      folio, and cookie-banner.

  - id: cookie-banner-storage-key-filter
    title: CookieBanner subscribes to all cross-tab storage events, not just bfs-cookie-consent
    ship: |
      Filter the window 'storage' listener in src/components/cookie-banner.tsx:23-29
      so it only re-runs readPhase when the changed key matches STORAGE_KEY.
      Pattern: `(e) => { if (e instanceof StorageEvent && e.key !== null
      && e.key !== STORAGE_KEY) return; cb(); }`. Currently every
      unrelated cross-tab write triggers a useSyncExternalStore re-read —
      cheap (one getItem), but defensive and avoids a wake-up storm
      if another tab is hot-writing some other key (e.g., the cart).
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/components/cookie-banner.tsx:23
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by Step 7.5 review on cookie-banner-subtask ship (M2).

  - id: cookie-banner-aria-live
    title: CookieBanner region has no aria-live so the slide-in lands silently for screen readers
    ship: |
      Add `aria-live="polite"` to the `<div role="region"
      aria-label="Cookie consent">` wrapper in
      src/components/cookie-banner.tsx so AT users hear the banner
      announce itself when it appears. Polite (not assertive) because
      this isn't urgent — the page is still usable while the banner
      is shown.
    severity: low
    effort: S
    class: a11y
    status: open
    files:
      - src/components/cookie-banner.tsx
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by Step 7.5 review on cookie-banner-subtask ship (L3).

  - id: cookie-banner-escape-input-gate
    title: CookieBanner Escape handler can fire while the user is typing in an input
    ship: |
      Gate the global keydown listener in src/components/cookie-banner.tsx:45-49
      on `document.activeElement?.tagName` not being INPUT / TEXTAREA / SELECT
      — otherwise a user pressing Escape to clear a form field while
      the banner is shown would inadvertently dismiss it as 'declined'.
      Low risk because the first-visit moment usually doesn't overlap
      with form input (the newsletter sits below the fold and the
      checkout route doesn't see the banner since consent is set by
      then), but cleanly defensive.
    severity: low
    effort: S
    class: a11y
    status: open
    files:
      - src/components/cookie-banner.tsx:45
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by Step 7.5 review on cookie-banner-subtask ship (L2).

  - id: cookie-banner-mobile-magnet-offset
    title: CookieBanner mobile magnetic translate may visually offset full-width button
    ship: |
      Visual verification only — at < 720px, .cookie-banner-magnet
      becomes `flex: 1` (globals.css:7280-7284) and the inner button
      stretches to `width: 100%`. The <Magnetic> wrapper's translate
      transform on hover/move could visually shift a full-width button
      a few pixels left/right of the gutter. Likely fine because
      Magnetic only fires on (pointer: fine) which is rare on the
      mobile breakpoint — confirm with a browser session.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/app/globals.css:7280
      - src/components/cookie-banner.tsx:74
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by diff-reviewer on cookie-banner-subtask ship. Cosmetic
      only — Magnetic is (pointer: fine)-gated, so touch devices won't
      see the translate. The actual visual risk is desktop window
      <720px wide with a mouse, which is uncommon.

  - id: cart-announce-ttl-token
    title: CartDrawer announce-clear timeout uses untokenized 4000ms literal
    ship: |
      Replace the magic 4000ms in CartDrawer's announce-state-clear
      setTimeout with a named const or motion-system token. Pair with
      the pre-existing 1800ms `added`-state-clear timer in cart-island
      (2 occurrences) so all cart timing literals tokenize together —
      either as a module-scope `ANNOUNCE_TTL_MS = 4000` const + a
      `ADDED_BADGE_TTL_MS = 1800` const, or as CSS-style `--dur-*`
      tokens read from a shared `cart-timings.ts` module.
    severity: medium
    effort: S
    class: hygiene
    status: open
    files:
      - src/components/cart-drawer.tsx:87
      - src/components/cart-island.tsx:56
      - src/components/cart-island.tsx:99
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by diff-reviewer on cart-autoopen-drawer-and-live-region
      ship. The 4000ms was introduced by this ship for clearing the
      polite live-region `aria-live` text; the 1800ms timers predate
      and clear the chapter-cta `data-added` momentary confirm state.

  - id: cart-drawer-fragment-indent
    title: CartDrawer JSX inside Fragment retains pre-wrap indentation
    ship: |
      The cart-drawer-root and ~180 lines of inner JSX were wrapped
      in a top-level Fragment without re-indenting. Closing </> sits
      flush with the inner </div>. Cosmetic only — Prettier on the
      next format pass will normalise. Worth applying when touching
      this file next so the diff stays small.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/components/cart-drawer.tsx:152-334
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by diff-reviewer on cart-autoopen-drawer-and-live-region
      ship. Re-indenting in this ship would have ballooned LOC; defer
      to the next CartDrawer-touching change.

  - id: cart-add-double-announce
    title: CartCount aria-live + new drawer announce may double-speak on add
    ship: |
      CartCount has its own aria-live="polite" on the count badge.
      The new CartDrawer announce region also fires "Added X to cart."
      on the same dispatch. NVDA/VoiceOver will queue: "Added Void Book
      to cart. Cart: 3 items." Acceptable and arguably useful — but
      verify in a screen-reader session. If a single utterance is
      preferred, downgrade the CartCount aria-live to a more reserved
      label or guard one of the regions while the other is live.
    severity: low
    effort: S
    class: hygiene
    status: open
    files:
      - src/components/cart-drawer.tsx:152-160
      - src/components/cart-island.tsx:30-39
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by perf-a11y on cart-autoopen-drawer-and-live-region
      ship. UX nuance only — perf-a11y verdict was PASS.

  - id: about-opengraph-image
    title: /about needs a dedicated opengraph-image.tsx — currently inherits the homepage OG
    ship: |
      Add `src/app/about/opengraph-image.tsx` using the same ImageResponse
      pattern as `/opengraph-image.tsx` and `/journal/[slug]/opengraph-image.tsx`.
      Subject — display 'About the press.' in italic-serif on the matte ground
      with a hairline rule under, plus the BFS wordmark at bottom-left. 1200×630.
      Per-route OG tightens share metadata so /about previews cleanly on
      X / Slack / iMessage instead of falling through to the generic home OG.
    severity: low
    effort: S
    class: seo
    status: open
    files:
      - src/app/about/page.tsx
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by the /about ship. Task brief explicitly called for a
      per-route opengraph-image; ship-as-medium-followup rather than
      inline so the route lands first.

  - id: about-display-outline-stroke-variant
    title: about-display could adopt the hero/PDP/journal-post asymmetric outline-stroke vocabulary
    ship: |
      The homepage hero ('Dark Matter.'), every /supplies/<id> PDP title, and
      every /journal/<slug> post title use an asymmetric two-word composition
      (word 1 solid italic-serif, word 2 outline-stroked via `-webkit-text-stroke`).
      .about-display currently renders 'About the press.' as solid italic across
      three words because the asymmetric word-1/word-2 split doesn't fit
      a 3-word title cleanly. Options: (a) restructure the title to a 2-word
      composition like 'About BFS.' or 'The press.' so the canonical
      outline-stroke vocabulary applies; (b) leave as solid italic and document
      the divergence as intentional (about is a destination, not a poster).
      Implementer's call.
    severity: low
    effort: S
    class: distinctive
    status: open
    files:
      - src/app/about/page.tsx:153-159
      - src/app/globals.css:7218-7240
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by the /about ship. Register-coherence vs. editorial-restraint
      trade-off; either resolution is defensible.

  - id: about-page-illustration-asset
    title: /about is type-only — pairs with the generate-distinctive-images task to add a press-stamp / typesetter's tray illustration
    ship: |
      The Studio section on /about reads as a colophon dl and an intro
      paragraph, no imagery. The Notion task `35faf8d3-d3e2-8168-9026-c7ce8590e62b`
      ('Generate distinctive images via image-gen skill') lists 'a press-stamp /
      typesetter's tray composition for the manifesto's sticky-rail area' —
      the same composition would deepen the /about Studio block. Generate via
      image-gen, save under `public/img/about/`, wire via `<Image>` with
      width/height to prevent CLS, alt='Studio · press stamp'.
    severity: low
    effort: S
    class: distinctive
    status: open
    files:
      - src/app/about/page.tsx:188-217
    opened_on: 2026-05-14
    last_verified: 2026-05-14
    notes: |
      Surfaced by the /about ship. Best landed alongside the broader
      image-gen task rather than in isolation so the visual register
      stays coherent across routes.
