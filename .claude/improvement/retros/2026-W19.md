# Retro — 2026-W19 · 2026-05-06 to 2026-05-13

> First retro on a 2-day-old routine. Window collapses to project
> lifetime (May 12–13). n=21 — read directionally.

## Summary
- Ships: **21** (shipped: 21 · no-focus: 0 · PR-opened: 0)
- `consecutive_no_focus_runs`: 0 · `shipped_count`: 21
- Rubric distribution: **insufficient data.** Only 2 / 21 ships log a
  parseable rubric line (`pdp-supplies-route` 17/18 · `specimen-plate`
  15/18). The other 19 omit it. **Biggest gap in the routine.**
- Lighthouse delta: **N/A** — `lighthouse.csv` is header-only after 21
  ships (`lighthouse-baseline` still open in backlog).
- Surfaces touched: chrome ×4, catalogue ×4, system ×2, seo ×2, hero,
  outro, colophon, codex, field-notes, faq, 404, nav, cart ×1.
- Surfaces NOT touched: **manifesto** (0 ships, cold whole project).

## What's working
- **Editorial-spread vocabulary is converging.** Hairline rules,
  italic numerals, dot-leader folios, `-webkit-text-stroke` titles
  read across hero, codex, field-notes, FAQ, 404, PDP. Both scored
  ships lean on it — distinctiveness is paying off.
- **Task-driven mode shipped clean.** `pdp-supplies-route` (Notion
  task, self-rated 17/18) added 6 SSG routes + 3 files and caught its
  own follow-ups inline.
- **Follow-ups land in `backlog.yaml`, not in prose.** 13 open items
  tracked; index-menu spawned 3 same-day hygiene items now
  picker-eligible.

## What's drifting
- **Rubric not being recorded.** 19 / 21 ships omit the
  `T M L I A D = N/18` line. Retro can't compute axis trends; picker
  can't tiebreak on weak axes; the 10th-ship calibrator (overdue) has
  nothing to calibrate against.
- **Periodic triggers deferred two runs running.** PDP ship's log
  admits retro, critic, and calibration were overdue and skipped "to
  keep focus on the substantial task." Big ships eating their own
  observability is what this meta-routine exists to prevent.
- **No Lighthouse baseline after 21 ships.** Every "Expected impact:
  SEO/UX" claim is currently unverifiable.
- **`manifesto` is a deliberately-cold surface** — on the homepage,
  never picked, while `chrome` and `catalogue` each got 4.

## Pattern findings
- **Hot surfaces** (≥ 3 ships): `chrome` (4), `catalogue` (4).
- **Cold surfaces** (0 ships): `manifesto`.
- **Rubric axes consistently underscored:** unknown (see drift #1). Of
  2 scored ships, **I (interaction grammar) hit 2 both times** —
  routine is composing on existing primitives (`Magnetic`, `Reveal`,
  `SplitText`), not inventing new verbs.
- **Failure modes recurring:** SOTD comparison silently skipped on the
  latest ship, no follow-up opened. Two consecutive dialog ships
  (IndexMenu, CartDrawer) both leak `body.overflow` — routine isn't
  cross-referencing new dialogs against shared `lib/` primitives that
  don't yet exist.

## Adjustments for the coming week
1. **Enforce a rubric line per ship.** Phase 6 refuses to commit until
   `IMPROVEMENTS.md` contains a literal `**Rubric.** T# M# L# I# A# D#
   = N/18` line. Self-rated is fine; missing is not.
2. **Picker tiebreaker → bias toward `manifesto`.** Only zero-ship
   surface. If next run's top two candidates are within 1 rubric
   point, force the manifesto pick.
3. **Picker tiebreaker → favour I ≥ 3.** Both scored ships hit I=2.
   On ties, prefer candidates claiming new pointer/keyboard/scroll
   grammar over another `Magnetic + Reveal` remix.
4. **Refuse to defer overdue periodic triggers twice.** If `state.yaml`
   shows retro/critic/calibration overdue *and* the previous run also
   deferred them, Phase 0 fires the overdue periodic *before* the
   historian — even at the cost of the ship slot.

## Meta-recommendations
- **Stand up the Lighthouse baseline as the next sub-ship.** One run
  against deployed homepage, append to `lighthouse.csv`, add a Phase 5
  step that re-runs on any `perf` ship or >200 LOC of CSS.
- **Ship `lib/scroll-lock.ts` counted-helper.** Collapses
  `index-menu-scroll-lock-leak` + `dialog-scroll-lock-shared`,
  prevents recurrence on the next dialog. 1-hour pattern-debt fix.
- **Silent SOTD parser failure is a routine bug, not a ship bug.** On
  parse failure, the historian should write a stub
  `.claude/improvement/sotd/<sha>.md` with raw markup, not log-and-skip.

